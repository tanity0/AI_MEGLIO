# AI Meglio — ドット絵アニメーション AI編集スタジオ 設計書

## 1. コンセプト

ブラウザで動くドット絵アニメーションエディタ。ユーザーはキャンバス上で「細かい部分」（矩形範囲・フレーム）を指定し、日本語で修正指示を出す。Claude API がその範囲のドットをピンポイントで書き換え、ループ再生中のプレビューに即座に反映される。手描き編集とAI編集を行き来しながら仕上げるツール。

**ドット絵がAI精密編集に向く理由**: 小さいグリッド × 限定パレットなので、画像を「パレット番号のテキストグリッド」として完全に表現できる。Claudeには理解用のPNG画像と編集用のテキストグリッドの両方を渡し、出力は構造化JSON（差分パッチ）で受け取る。再生成ではなく確定的な差分編集になる。

## 2. 技術構成

- **サーバー**: Node.js 18+ / ESM / `node:http`（Expressなし）。依存は `@anthropic-ai/sdk` のみ。
- **フロント**: ビルドステップなしの vanilla JS + Canvas API。`public/` 配下に配置。
- **CDN・外部リソース禁止**（フォント・ライブラリすべてローカル/自前実装）。

### ファイル構成

```
package.json          # type: module, dep: @anthropic-ai/sdk, scripts.start: node server.js
server.js             # 静的配信 + /api/edit (SSE) + /api/config
public/
  index.html
  style.css
  app.js              # エントリ・状態管理・Undo/Redo
  editor.js           # キャンバス描画・ツール・選択
  timeline.js         # フレーム一覧・再生プレビュー
  ai.js               # AIパネル・SSE受信・パッチ適用
  gif.js              # GIFエンコーダ（自前実装、依存なし）
README.md             # セットアップ・使い方（日本語）
DESIGN.md             # 本書
```

（ファイル分割は目安。合理的な範囲で統合・分割してよいが、1ファイル2000行を超えない）

## 3. データモデル

```js
project = {
  width: 32, height: 32,        // 8〜96。デフォルト 32×32
  fps: 8,                        // 1〜24
  palette: ["#00000000", "#1a1c2c", ...],  // 最大32色。index 0 は常に透明
  frames: [
    { pixels: Uint8Array(width*height) },  // 値 = パレットindex
    ...
  ]
}
```

- Undo/Redo はプロジェクト全体のスナップショットスタック（JSON化して保持、上限50件）。
- 保存/読込: プロジェクトをJSONでダウンロード / ファイル選択で復元。

### テキストグリッド表現（プロンプト用）

1ピクセル1文字。透明(index 0) = `.`、index 1〜31 = `1`〜`9`, `a`〜`v`（base36的割当て、大文字不使用）。1行 = グリッド1行。フレームごとに行ブロック。この表現はプロンプトとレスポンスの両方で共通に使う。

## 4. UI レイアウト

```
┌──────────┬──────────────────────────┬──────────────┐
│ ツール      │  キャンバス（ズーム表示・       │ AIパネル       │
│ ・ペン      │  グリッド線・オニオンスキン）    │ ・対象表示      │
│ ・消しゴム   │                          │ ・指示入力      │
│ ・塗りつぶし │                          │ ・実行ボタン     │
│ ・矩形選択   │                          │ ・履歴ログ      │
│ ・スポイト   │                          │ ・再生プレビュー  │
│ パレット     │                          │  (実寸+ループ)   │
├──────────┴──────────────────────────┴──────────────┤
│ タイムライン: フレームサムネイル列 [+追加][複製][削除][←→並替] fps▸ 再生/停止 │
└─────────────────────────────────────────────────────┘
```

### エディタ要件

- ズーム: セルサイズ自動（キャンバス領域にフィット）+ ホイールで調整。グリッド線はズーム時のみ。
- ツール: ペン / 消しゴム / 塗りつぶし(flood fill) / 矩形選択 / スポイト。ドラッグ描画対応。
- 矩形選択: 点線でハイライト。選択中はAIパネルに「対象: フレームN の (x,y)〜(x2,y2)」と表示。選択なし = フレーム全体が対象。
- オニオンスキン: 前フレームを半透明表示（トグル）。
- 市松模様の透明背景。
- パレット: 色クリックで選択、ダブルクリックで `<input type="color">` により変更、[+]で追加（32色まで）。
- 再生プレビュー: AIパネル下部に実寸〜2倍表示で常時ループ再生（トグル可）。**編集が適用された瞬間に反映される**こと（フレームデータ共有で自然に実現）。

### AIパネル要件

- 対象スコープ選択: 「選択範囲のみ」「現在のフレーム」「全フレーム」ラジオ。
- 指示テキストエリア（Cmd/Ctrl+Enter で実行）。
- 実行中: ストリーミング進捗（受信文字数 or スピナー）+ 中断ボタン（fetch abort）。
- 適用時: 変更されたセルを1秒程度フラッシュハイライト。
- 履歴ログ: 指示文と結果（適用セル数 / 追加フレーム数 / エラー）を時系列表示。
- 適用は自動。気に入らなければ Undo (Cmd/Ctrl+Z)。

### 指示例（プレースホルダに記載）

「腕の振りをもっと大きく」「輪郭のジャギーを滑らかに」「フレーム1と2の間に中割りを追加」「全体を夕暮れの色調に」「この範囲の剣を炎の剣にして」

## 5. サーバー API

### `GET /api/config`
`{ model, effort, mock }` を返す。

### `POST /api/edit` → SSE

リクエストJSON:

```js
{
  project: { width, height, fps, palette, framesGrid: ["....\n1122\n...", ...] },  // テキストグリッド
  scope: "selection" | "frame" | "all",
  frameIndex: 0,                       // scope=selection/frame のとき対象フレーム
  selection: { x, y, w, h } | null,    // scope=selection のとき
  instruction: "腕の振りをもっと大きく"
}
```

SSEイベント（`data: {...}\n\n`）:

- `{ type: "delta", text: "..." }` — 進捗表示用（JSONの断片。クライアントは表示のみに使い、蓄積して最後にパースはサーバー側で行うため使わない）
- `{ type: "result", patch: {...}, usage: {...} }` — パース・検証済みパッチ
- `{ type: "error", message: "..." }`

サイズ上限 5MB。バリデーション（範囲・型）必須。

### パッチ形式（Claudeの構造化出力スキーマ）

```json
{
  "type": "object",
  "properties": {
    "edits": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "frame": { "type": "integer" },
          "x": { "type": "integer" }, "y": { "type": "integer" },
          "rows": { "type": "array", "items": { "type": "string" } }
        },
        "required": ["frame", "x", "y", "rows"],
        "additionalProperties": false
      }
    },
    "newFrames": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "insertAfter": { "type": "integer" },
          "rows": { "type": "array", "items": { "type": "string" } }
        },
        "required": ["insertAfter", "rows"],
        "additionalProperties": false
      }
    },
    "paletteChanges": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "index": { "type": "integer" },
          "color": { "type": "string" }
        },
        "required": ["index", "color"],
        "additionalProperties": false
      }
    },
    "note": { "type": "string" }
  },
  "required": ["edits", "newFrames", "paletteChanges", "note"],
  "additionalProperties": false
}
```

- `edits[].rows`: パッチ矩形の行テキスト（グリッド表現と同じ文字割当て。`?` = そのセルは変更しない）。
- `newFrames[].rows`: フルサイズのフレーム。
- サーバー側で検証: frame範囲、座標がキャンバス内、rows の文字が正当、scope=selection のときは選択矩形外への edits を**切り捨てて**適用（警告をresultに含める）。ただし `newFrames` と `paletteChanges` は scope に関係なく許可。
- `note`: 何をしたかの一言（履歴ログに表示）。

## 6. Claude API 呼び出し（server.js 内・重要）

SDK は `@anthropic-ai/sdk` 最新（0.110系で動作確認済みのAPI形状）。**以下の形を正とし、古い記憶で書き換えないこと**:

```js
import Anthropic from "@anthropic-ai/sdk";
const client = new Anthropic(); // ANTHROPIC_API_KEY を環境変数から解決

const stream = client.messages.stream({
  model: MODEL,                              // 既定 "claude-opus-4-8"（env MODEL で上書き可）
  max_tokens: 64000,
  thinking: { type: "adaptive" },            // budget_tokens は使わない（400になる）
  output_config: {
    effort: EFFORT,                          // 既定 "medium"（env EFFORT）
    format: { type: "json_schema", schema: PATCH_SCHEMA },  // 構造化出力
  },
  system: [
    { type: "text", text: SYSTEM_PROMPT, cache_control: { type: "ephemeral" } },
  ],
  messages: [{ role: "user", content: userContent }],
});

stream.on("text", (delta) => /* SSE delta 転送 */);
const final = await stream.finalMessage();
// final.stop_reason === "refusal" / "max_tokens" はエラーとしてSSEで返す
// 正常時: content の text ブロックを JSON.parse → 検証 → { type:"result", patch } を送出
```

- `temperature` / `top_p` / `top_k` は**送らない**（対象モデルでは400）。
- アシスタントprefillは**使わない**（400）。
- クライアント切断時 `stream.abort()`。
- エラーは SDK の型付き例外で分岐（`Anthropic.AuthenticationError` → 「ANTHROPIC_API_KEY を設定してください」等の日本語メッセージ）。

### userContent の構成

`content` は配列で、**画像ブロック + テキストブロック**:

1. 対象フレーム（scope=all なら全フレーム、それ以外は対象フレームと前後1フレーム）を**8倍最近傍拡大したPNG**の base64 画像ブロック。サーバー側で生成する必要はない — **クライアントがcanvasで生成して data URL をリクエストに含め、サーバーはそれを** `{ type: "image", source: { type: "base64", media_type: "image/png", data } }` **に変換**する（サーバーに画像ライブラリを入れないため）。リクエストJSONに `images: [{ frame: n, dataUrl } ]` を追加。
2. テキストブロック: パレット一覧（index: hex）、全フレームのテキストグリッド、キャンバスサイズ、fps、対象スコープと選択矩形、編集指示。

### SYSTEM_PROMPT の要点（日本語で書く）

- 役割: ドット絵アニメーションの精密編集エンジン。
- グリッド文字割当ての説明（`.` = 透明、`1`-`9a-v` = パレット index、`?` = 変更しない）。
- 最小差分の原則: 指示に必要なセルだけを edits に含める。無関係なセルは `?`。
- アニメーションの一貫性: 複数フレームにまたがる編集では動きの連続性を保つ。中割り指示では前後フレームを補間。
- 画像はプレビュー参考、**正はテキストグリッド**。
- 出力はスキーマに従うJSONのみ。

## 7. MOCKモード

`MOCK=1` で起動するとAPIを呼ばず、選択範囲（なければフレーム全体の中央8×8）をパレット最後の色で塗るパッチを2秒かけて疑似ストリーミングで返す。UI・SSE・パッチ適用の動作確認用。READMEに記載。

## 8. GIFエクスポート

- `gif.js` に GIF89a エンコーダを自前実装（LZW圧縮含む。数百行で書ける定番実装）。パレット≤32色・256色以下なのでグローバルカラーテーブルで単純に実装可。透明は disposal method 2 + transparent color flag。
- 「GIF書き出し」「スプライトシートPNG書き出し」（横並び1枚、canvas.toBlobで生成）「プロジェクトJSON保存/読込」をヘッダのメニューに置く。

## 9. 見た目

- ダークテーマ（エディタ系ツールの定番）。アクセント1色。システムフォントでよいが等幅数字。
- 素朴でよいが、ボタン・パネルの余白と階層は整えること。日本語UI。

## 10. 初期データ

起動時に 32×32・8色・2フレームのサンプル（簡単なキャラが上下にバウンドする程度。ハードコードした配列でよい）を読み込み、すぐ再生・AI編集を試せる状態にする。

## 11. 検証（実装後に必ず行う）

1. `npm install && MOCK=1 npm start` でサーバー起動。
2. `curl` で `/api/config` と `/api/edit`（MOCK）のSSE応答を確認。
3. Playwright（`/opt/pw-browsers/chromium`、`PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers`。`playwright install` は実行しない）でページを開き、コンソールエラーがないこと、キャンバス・タイムライン・AIパネルが描画されること、MOCK編集を実行してパッチが適用されることをスクリーンショットで確認。
4. GIF書き出しが有効なGIFヘッダ（`GIF89a`）を持つBlobを生成すること（ページ内で検証）。

## 16. 【v4】ゲーム制作特化レイヤー

ツールを「単発のGIF作成」から「ゲームアセットの量産ライン」に特化させる。対象ゲーム（例: tanity0/zombie）への接続は §16.6 のプロファイルで吸収し、本体はエンジン非依存に作る。

### 16.1 アニメーションタグ（最重要・Aseprite方式）

1キャラ = 1プロジェクトの中に複数モーションを持てるようにする。

```js
project.tags = [ { name: "walk", start: 0, end: 3, fps: 8, loop: true }, ... ]
```

- フレーム列は従来どおり単一の配列。タグは範囲参照（start/end はフレームindex、両端含む）。
- タイムライン上部に**タグバー**を追加: タグの作成（現在の選択範囲 or 範囲入力）、リネーム、fps/loop個別設定、削除（フレームは消さない）、タグクリックでそのタグだけをループ再生・書き出し対象に。
- モーション生成・リグ生成の結果は**新しいタグとして末尾に追加**（タグ名はプリセット名から自動命名、重複時は walk_2）。
- フレーム挿入/削除時はタグ範囲を自動補正。タグに属さないフレームがあってもよい。
- 保存JSONに含める。既存プロジェクトの読込時は tags 無し→「all」タグを自動生成。

### 16.2 パレットスワップ量産（mode: "palette"）

色違いの敵バリエーション（通常ゾンビ→毒ゾンビ→炎ゾンビ等）を高速量産する専用AIモード。

- 新しい `mode: "palette"`: AIへは**パレットとベースフレーム1枚のグリッドのみ**を送り、出力は paletteChanges のみの専用スキーマ（edits/newFrames なし）。トークンが小さいのでCLIバックエンドでも数秒〜十数秒で返る。
- UI: AIパネル「修正」タブに「配色バリエーション」ボタン → 指示（例「毒々しい緑基調に」）→ 結果は**プレビュー付きの候補として表示**し、「別名で書き出しに追加」or「このプロジェクトに適用」を選ぶ。
- バリエーションはプロジェクトに `variants: [{name, palette}]` として保存し、書き出し時に全バリエーション分のシートを出力できる。

### 16.3 ゲームプロファイル

書き出し先とフォーマットの規約を JSON で定義し、UIがそれに従う。

```js
// public/profiles/ 以下 + ユーザー定義（プロジェクトJSONに埋め込み可）
{
  "name": "zombie",
  "exportDir": "assets/sprites",        // ゲームリポジトリ相対
  "format": "sheet+json",               // "sheet+json" | "strip-per-tag" | "frames"
  "scale": 1,                            // 整数拡大して書き出し
  "naming": "{char}_{variant}",         // ファイル名テンプレート
  "requiredTags": ["idle", "walk", "attack", "death"],  // キャラに必要なモーション一覧
  "mirror": "runtime"                    // "runtime"(ゲーム側で反転) | "export"(左右反転タグも書き出す)
}
```

- UI: ヘッダにプロファイル選択。**必要タグのチェックリスト**を表示（idle✓ walk✓ attack✗ death✗）— 「このキャラに足りないモーション」が一目で分かり、✗クリックでそのプリセットのモーション生成/リグ生成に直行。
- `mirror: "export"` の場合、各タグの左右反転版（walk_left 等）を自動生成して書き出しに含める（単純水平ミラー。非対称キャラ向けにタグ単位でON/OFF可）。

### 16.4 書き出し形式（ゲームが読める形）

- **sheet+json**: 全タグを1枚のシートPNG（行=タグ、列=フレーム）+ メタデータJSON。JSONは Aseprite の `--format json-array` 互換形状（frames[{filename, frame:{x,y,w,h}, duration}], meta.frameTags[{name, from, to, direction}]）。大抵のJSエンジン・Godot・Unityのインポータがこの形を読める。
- **strip-per-tag**: タグごとに横並びストリップPNG（`{char}_{tag}.png`）。
- **frames**: 連番PNG（`{char}_{tag}_{n}.png`）。
- `scale` 指定時は最近傍整数拡大で書き出し。
- **ゲームリポジトリへの直接書き出し**: `POST /api/export`（multipart不要、body に files: [{path, dataUrl}]）。サーバーは `EXPORT_ROOT` 環境変数（未設定なら機能無効）配下のみに書き込みを許可（パストラバーサル拒否）。これで `EXPORT_ROOT=C:\...\zombie BACKEND=cli npm start` と起動すれば「書き出し」ボタン一発でゲームのアセットフォルダが更新される。ブラウザダウンロード方式も従来どおり残す。

### 16.5 ゲームビュープレビュー

- 背景画像（ゲームのスクリーンショット等）を読み込み、その上で選択タグのアニメを**ゲームスケール（×1/×2/×3/×4）で移動ループ再生**する簡易プレビューパネル（歩きなら左右に往復）。実機の見え方（小ささ・視認性）の確認用。背景はプロジェクトJSONに含めない（セッション限り）。

### 16.6 zombieリポジトリ固有プロファイル（確定版）

実機調査結果: zombie は Pixi 主体で、`public/sprites/<name>.png` の**単体PNG**を `spritePath(name)` 経由でロードし、`src/pixi/pixiTextures.ts` の standalone 配列がロード対象の台帳。`atlas.png` は矩形切り出し（`ATLAS_RECTS` / `spriteAtlas.ts`）。キャラのフレームアニメは現状ゲーム側に無い（静止画1枚をエンティティごとに使用）。カテゴリ別サブフォルダあり（`stage3-enemies/`, `fx/`, `npc/` 等）。

これを踏まえた `profiles/zombie.json`:

```json
{
  "name": "zombie",
  "exportDir": "public/sprites",
  "format": "frames",
  "scale": 1,
  "naming": "{char}",
  "requiredTags": ["idle", "walk", "attack", "death"],
  "mirror": "runtime",
  "primaryExport": "first-frame-of-idle"
}
```

- `primaryExport: "first-frame-of-idle"`（プロファイル新フィールド）: 書き出し時に **idleタグの先頭フレームを `{char}.png` として単体出力**する。現行ゲームの「1エンティティ=静止PNG 1枚」にそのまま差し込める（`EXPORT_ROOT=<zombieリポジトリ> ` で `public/sprites/enemy2.png` などを直接更新）。
- 同時に全タグの連番PNG（`{char}_{tag}_{n}.png`）と sheet+json も出力しておき、ゲーム側が将来フレームアニメ対応したときにそのまま使えるようにする。
- 注記（READMEに記載）: 新規キャラ名で書き出した場合、ゲーム側では `pixiTextures.ts` の standalone 配列への追記が必要（ツールの範囲外。書き出し完了メッセージに「ゲーム側で pixiTextures.ts への登録が必要な場合があります」と表示する）。

---

## 17. 【v5】トンマナ参照（スタイルアンカー）

「世界観・デフォルメ感・ドットの打ち方」を既存の1枚の参考画像に合わせるための機能。ゲームの既存スプライト（例: zombie の player.png や atlas.png の一部）をアップロードし、以降のAI編集・生成すべてのトンマナ基準にする。

### 17.1 データモデル

```js
project.styleRef = {
  imageDataUrl: "data:image/png;base64,...",   // 参考画像（縮小済み・最大256×256に正規化して保持）
  guide: "頭身: 2頭身 / 輪郭: 黒1px常時 / ...", // スタイルガイド（テキスト。編集可能）
  enabled: true
}
```

プロジェクトJSONに含めて保存する。

### 17.2 スタイル解析（mode: "style"）

参考画像をアップロードした直後に1回だけ実行する軽量AI呼び出し。

- 入力: 参考画像（8倍拡大せず原寸のまま画像ブロックで送る。CLIバックエンド時は画像が送れないため、クライアント側で参考画像を**グリッド化できる場合**（≤96×96・≤32色に量子化可能）はテキストグリッド+パレットとして送り、できない場合はこの解析機能を「APIバックエンド時のみ有効」とし、ガイドの手書き入力を促す）
- 出力スキーマ: `{ guide: string, note: string }`。guide は以下の観点を1行ずつ: 頭身・デフォルメ度 / 輪郭線の有無・太さ・色 / シェーディング段数とディザの有無 / ハイライトの入れ方 / 彩度・明度の傾向 / 代表色 / ピクセルの打ち方の癖（角の丸め方、1pxディテールの密度）
- 結果はUIのテキストエリアに表示し、ユーザーが自由に編集できる（**ガイドテキストが正**であり、以降のリクエストにはこのテキストを使う）

### 17.3 リクエストへの組み込み

- `styleRef.enabled` のとき、すべての edit / motion / segment / cleanup / palette リクエストに:
  - テキスト: 「## トンマナ基準（厳守）」としてガイドテキストをシステム相当部に追記
  - 画像: APIバックエンドでは参考画像ブロックを1枚追加（CLIバックエンドではテキストのみ）
- システムプロンプトに追記: 「トンマナ基準が与えられた場合、ベースフレームのテイストとトンマナ基準が矛盾するときは**編集対象の絵をトンマナ基準へ寄せる方向**で修正する。ただし指示が明示的に求めない限り、一度の編集で全面的に描き直さず、指示された範囲内でのみ寄せる」
- 新規キャラをトンマナに合わせて起こす用途のため、「修正」タブの指示例に「この空のキャンバスに、トンマナ基準に合う◯◯を描いて」を追加。

### 17.4 UI

- AIパネル上部に「トンマナ」折りたたみセクション: 参考画像サムネイル / アップロードボタン / 「AIで解析」ボタン（スピナー付き）/ ガイドテキストエリア（編集可）/ 有効トグル。
- 有効中は実行ボタン付近に小さく「トンマナ適用中」バッジ。

### 17.5 検証

- MOCK の style モード（固定ガイドを返す）でアップロード→解析→ガイド表示→編集可能を Playwright で確認。
- styleRef.enabled で edit リクエストのプロンプトにガイドが含まれること（MOCKサーバー側でリクエスト内容を検査 or ダミーCLIで受信プロンプトをファイルに落として確認）。
- CLIバックエンドで画像なし・テキストのみになること。
- プロジェクトJSON保存/読込で styleRef が往復すること。
- 既存経路の退行なし。

### 16.7 検証

- タグ: 作成・範囲再生・生成結果の自動タグ化・フレーム削除時の範囲補正を Playwright で確認。
- palette モード: MOCK（固定の paletteChanges を返す）で候補プレビュー→適用/バリエーション保存の両経路。
- 書き出し: sheet+json の JSON が frameTags を含み、フレーム矩形がシート上の実位置と一致すること（ページ内で読み戻して検証）。strip-per-tag / frames / scale=2 / mirror:"export" も各1ケース。
- /api/export: EXPORT_ROOT 配下への書き込み成功、`../` を含むパスの拒否、EXPORT_ROOT 未設定時は 403 を確認。
- 必要タグチェックリストの ✓/✗ 表示と ✗→生成タブ直行。
- 既存機能（§11/§13/§14/§15 スモーク）の退行なし、コンソールエラー0件。

## 18. 【v6・最重要】高忠実度変換スタジオ（ドット絵風→本物ドット絵）

**このツールの最重要課題。品質に妥協しない。** AI生成などの「ドット絵風画像」（不均一グリッド・数百色・アンチエイリアス入り）を、雰囲気をできる限り保ったまま本物のドット絵（整グリッド+インデックスパレット）に変換する。32×32では崩れる素材のために、**解像度は最大128×128、色数は最大256色**まで対応する。

### 18.1 カラーモデル/キャンバスの拡張（土台）

- パレット上限を 32 → **256色**（index 0=透明は不変）に拡張。`Uint8Array` のフレームデータはそのまま。GIFエンコーダは既に256色対応（要確認・調整）。パレットUIはスクロール式グリッドに。
- キャンバス上限を 96 → **128** に拡張。
- **グリッド文字表現の拡張**: パレットが32色以下のプロジェクトは従来の1文字表現（後方互換）。**33色以上のときは1ピクセル=2文字の16進表現**（`..` = 透明、`01`〜`ff` = index 1〜255）に自動切替。サーバーはリクエストの `palette.length` で判定し、システムプロンプトの「グリッド表現」節を該当表現の説明に差し替える。パッチ `rows` の `?` は2文字表現では `??`。検証・変換関数（`charForIndex`/`indexForChar` 相当）を両対応に。
- 既存プロジェクトJSONは無変更で読める（32色以下はすべて従来どおり）。

### 18.2 変換スタジオ（インポートウィザードv2）

「画像を開く」を、確定前に調整できる**変換スタジオ画面**に置き換える（従来の単純インポートは32色以下の真ドット絵で自動判定成功時のショートカットとして残す）。

処理パイプライン:

1. **背景除去**: 外周フラッドフィル（近黒・近白・単色背景の自動判定 + スポイトで背景色を手動指定可）。フチ光彩（例: 紫グロー）は「背景に隣接する彩度の浮いた1〜3px帯」として除去オプション。
2. **疑似グリッド推定（品質の要）**: セルサイズ `s`（2.0〜40.0、0.25刻み）と位相オフセット `(ox, oy)` を総当たりし、**セル内分散の総和が最小**になる組を選ぶ（ダウンサンプルした作業解像度で粗探索→近傍を細探索）。これにより「描かれた時のドット」に格子を合わせる。信頼度が低い場合はその旨表示し、手動スライダー（サイズ・オフセット）で微調整できる。
3. **セル代表色**: セル内の**支配色**（最頻色。同率は中心寄り優先）を採用。平均は使わない（濁りの原因）。セルの非背景率<40%で透明。
4. **減色**: k-means（RGB距離でよいが、初期値は頻度加重の k-means++）で指定色数に量子化。**色数スライダー: 8〜256**。肌・差し色の保護のため、量子化前に「使用頻度は低いが色相が孤立している色」をシード優先する。ディザは行わない（ドット絵の文法に合わないため）。
5. **候補ギャラリー（選ぶ→追い込むの起点）**: 「候補を生成」で、パラメータ空間からバリエーションを**9候補（3×3グリッド）**一括生成して並べる（例: 解像度3段 × サンプリング/減色の組合せ3種。各候補にパラメータをラベル表示）。候補クリックで元画像との**並列比較ビュー**（同倍率・同期ズーム/パン）に拡大。「これをベースにする」でその候補のパラメータが全つまみに反映される。
6. **つまみで微調整（確定前ならいつでも候補に戻れる）**: すべてのつまみはリアルタイム再変換（粗探索結果をキャッシュして使い回し）。つまみ一覧:
   - **解像度**（高さ16〜128、または「1:1（グリッド推定サイズそのまま）」）
   - **色数**（8〜256）
   - **グリッド微調整**: セルサイズ（±0.25刻み）・オフセットX/Y（±1px刻み）— 自動推定がズレたときの追い込み
   - **代表色の取り方**: 支配色⇔平均のブレンド率（0=カリカリ〜1=なめらか）と、セル中心重視度
   - **輪郭保護**: セル内に暗い輪郭色が一定割合あれば代表色より優先する強さ（0でOFF）— 線の途切れ対策
   - **彩度保護**: 減色時に孤立色相（肌・差し色）をシード優先する強さ
   - **背景除去**: 閾値と、フチ光彩除去の帯幅（0〜3px）
7. **確定**: プロジェクト化（ベースフレーム設定、§18.3のメインパレット抽出を自動実行）。**確定後は通常のペン・消しゴム・スポイトでそのまま手動修正できる**（ここが最終仕上げの主戦場。ツール側は何も特別なことをしなくてよい—既存エディタがそのまま使える解像度・色数になっていることが§18.1の要件）。プロジェクトには選択した全パラメータを `conversionParams` として保存し、「元画像から再変換」で同じつまみ状態から再開できる。

- 元画像はプロジェクトに `sourceImage`（縮小版dataURL、最大512px）として保存し、後から変換スタジオで**再変換**できる（「元画像から再変換」メニュー）。
- **AI輪郭リファイン（任意・変換後のボタン）**: 変換で甘くなった輪郭・ハイライトを、選択範囲不要の専用清書として実行。allowedMask = 「透明と非透明の境界から2px以内」に限定した cleanup 呼び出し（既存機構の再利用）。

### 18.3 メインパレット抽出（トンマナの核）

256色のフルパレットから、**トンマナの主役となる「メイン色」（既定32色、8〜64で可変）を自動抽出**する。

- アルゴリズム: フルパレットを使用ピクセル数で重み付けし、k-means でメイン色数にクラスタリング。各フルカラー index はいずれかのメイングループに属する（`project.mainPalette = { colors: [hex...], groups: [mainIndex per full index] }`）。
- UI: パレットパネルに「メイン」段（抽出結果のストリップ）と「全色」段の2段表示。メイン色クリックで所属グループの全色をキャンバス上でハイライト。「再抽出」ボタンと色数指定。
- 用途:
  1. **AIプロンプト**: 33色以上のプロジェクトでは、システムプロンプトにフルパレット全列挙の代わりに「メインパレット（index→hex、所属グループの代表）+ 総色数」を渡し、トークンを節約しつつ色の意図を伝える（グリッド自体は16進2文字でフル情報）。
  2. **トンマナ解析（§17）**: スタイルガイド生成の入力にメインパレットを使う。
  3. **グループ単位パレットスワップ**: `mode:"palette"` を拡張し、AIは**メイングループ単位の色変更**を返す（`groupChanges: [{mainIndex, color}]`）。サーバーがグループ内の全色へ**相対シフト**（HSLで hue/sat/lum の差分を各色に適用）で展開。「赤系统をまとめて青系へ」が階調を保ったまま一手でできる。従来の個別 paletteChanges も併存。

### 18.4 AI連携の運用ガイド（高解像度・多色時）

- 128×128・256色のフレーム全体をAIで直接編集するのは実用外（トークン・時間）。UIで誘導する: 33色以上または65px以上のプロジェクトでは、修正タブに「大きな素材では矩形選択での指示が高速です」のヒントを常設表示。モーション生成タブには「この解像度ではリグの使用を推奨」のバナー。
- AI清書・選択範囲修正・グループパレットスワップは高解像度でも実用（対象が小さい/出力が小さいため）— ここが主戦場になる。
- リグ・タグ・書き出し・ゲームビューは色数/解像度に依存しないため無変更で動くことを確認する。

### 18.5 検証

- **受け入れテスト（実素材）**: `/root/.claude/uploads/6658078f-4eaf-56cc-ac9f-ba16c5ddeeba/c8541e06-IMG_6478.png`（ガンマンキャラ・1580×2304のドット絵風）を変換スタジオで高さ64・128色に変換し、(a) グリッド推定が位相まで合いエッジが立っていること、(b) 肌色・赤ベスト・白シャツが別色として残ること、(c) 旧簡易変換（ブロック平均+上位28色）との並列比較スクリーンショットで明確に改善していること。
- 2文字グリッド表現: 33色以上のプロジェクトで edit/cleanup のMOCK+ダミーCLE往復（`??` の扱い含む）、32色以下プロジェクトの従来表現が不変であること。
- メインパレット: 256色変換後に32メイン色抽出、グループハイライト、グループスワップ（MOCKで固定groupChanges→全色相対シフトの数値検証）。
- 変換スタジオUI: スライダー変更でのライブ再変換、並列比較、手動グリッド調整、再変換メニュー、AI輪郭リファイン（MOCK）。
- 退行なし: 32色以下の既存プロジェクト（§11/§13〜§17スモーク）、GIF/ゲーム書き出し（256色GIFの実デコード検証を1ケース追加）。

## 19. 【v6】部分仕上げ（mode: "refine"）

選択した範囲だけを「トンマナに合った綺麗なドットの打ち方」に清書する機能。変換直後の粗い部分の仕上げにも、**アニメ生成後にペンで大雑把に直した部分（例: 髪の動き）の清書**にも使う。ワークフローの要: 「生成 → ペンでラフに意図を描く → 整えるボタン → 完成」。

### 19.1 意味論（cleanup との違い・最重要）

- **ユーザーのラフ編集が「意図」であり、正である。** ベースフレームに引き戻してはならない。refine ではベースフレーム・アンカリングを適用せず、代わりに「**現在のフレームの選択範囲内のシルエット・形の意図を保ったまま、打ち方（輪郭の連続性、シェーディング段数、ハイライト、ジャギー）だけをトンマナと周囲に合わせて清書する**」という原則をシステムプロンプトに与える。
- 変更許可セル = 選択矩形 + 外周1px（境界のなじませ用）。許可外への edits はサーバーで破棄（cleanup と同じ機構）。paletteChanges / newFrames は破棄。
- 入力: フレーム全体のグリッド + 選択矩形 + （あれば）トンマナガイド(§17) + **前後フレームの同じ矩形の切り出し**（アニメーションの流れを保つため。存在するフレームのみ）。指示テキストは任意（省略時は「輪郭とシェーディングを整えて」相当の既定文）。

### 19.2 UI

- 矩形選択中、修正タブの実行ボタンの隣に「**整える**」ボタンを表示（選択が無いときは非活性 + ツールチップ「範囲を選択してください」）。
- 適用範囲オプション: 「このフレームのみ」/「現在のタグの全フレーム（同じ矩形）」。全フレーム時はフレームごとに並列リクエスト（既存キュー・進捗表示・部分失敗は警告）。
- 指示欄が空でも実行可能（既定文を使用）。書けばニュアンス指定になる（「髪の流れを進行方向へ」等）。
- 結果は通常どおり差分ビュー・逸脱メーター・Undoが効く。

### 19.3 実装

- サーバー: `mode:"refine"` を追加。リクエストは cleanup と同形（allowedMask をクライアントが選択矩形+1pxで生成）+ `neighborContext: [{frame, rows}]`（前後フレームの矩形切り出し、0〜2件）。スキーマは PATCH_SCHEMA の edits のみ利用（cleanup と同じ運用）。システムプロンプトに §19.1 の原則を「部分仕上げ（refineモードのとき適用）」節として追記。
- MOCK: 選択範囲内の非透明セルをそのまま返す（配管確認用）+ note「MOCK: refine」。
- CLIバックエンドでも対象が小さいため実用速度が見込める（選択矩形が主戦場）。

### 19.4 検証

- MOCK で「選択→整える」の単フレーム/タグ全フレーム両経路、許可外破棄の警告、指示欄空でも実行できること。
- ダミーCLIで受信プロンプトに (a) refine 原則、(b) 前後フレームの矩形切り出し、(c) トンマナガイド（有効時）が含まれることを実測。
- ペンでラフに描いた領域が refine 後もベースに引き戻されていないこと（MOCKエコーで機械的に確認 + 実CLIで1ケース目視）。
- 退行なし（cleanup・修正タブ・§18スモーク）。

## 20. 【v7】マルチポーズシート分割インポート

AI画像生成が出力する「同一キャラの複数ポーズが横（または格子状）に並んだ1枚画像」を、**ポーズごとに分割してアニメーションフレームとして読み込む**。実例: 紫背景に歩き3ポーズが並んだ1676px幅のシート。

### 20.1 検出

- 変換スタジオで画像を開いた時、背景除去後のアルファから**連結成分のバウンディングボックス**を抽出（8近傍・面積が最大成分の5%未満のノイズは無視）。横並び・格子いずれも対応（boxを行にクラスタリング→行内でx順）。
- 2体以上検出されたら、スタジオ上部に選択UI: 「**1枚として変換 / N体をフレームとして変換**」（既定はフレーム）。自動検出がうまくいかない素材向けに「均等分割: 横N×縦M」の手動指定も併設。

### 20.2 フレーム化の要件

- **共通キャンバス**: 全ポーズのbox最大幅・最大高さ + 既存のモーション用パディングで1つのキャンバスサイズを決める（上限128に §18 のクランプ適用）。
- **接地アライン**: 各ポーズは**下端中央合わせ**で配置（歩きポーズの足位置が揃う。オプションで中央合わせ）。
- **共通パレット**: 減色は全ポーズのピクセルをまとめて k-means（フレーム間で色がブレない）。
- **共通グリッド**: セルサイズ・位相は最大ポーズで推定し全ポーズに適用。
- 候補ギャラリー・つまみは従来どおり動作（プレビューは選択中ポーズ or 1体目。ギャラリーのサムネは1体目でよい）。
- 確定時: Nフレームのプロジェクト（frame 0 = 1体目 = ベースフレーム）+ 全フレームを含む「all」タグ。conversionParams に分割情報（mode, boxes）も保存し再変換可能に。

### 20.3 検証

- ImageMagickでガンマン単体画像から「紫背景に3体横並び」の合成シートを作り（受け入れ素材の再現）、(a) 3体が自動検出されること、(b) 「3体をフレームとして変換」で3フレーム+共通パレットのプロジェクトになること、(c) 下端中央アラインでプレビュー再生したとき足元が揃っていること、(d)「1枚として変換」経路と手動均等分割も動くこと。
- 1体のみの画像では従来どおり選択UIが出ないこと（退行なし）。

## 21. 【v7】設定ヘルプ（？アイコン）

すべての設定項目・つまみ・ボタンの意味を、その場で確認できるようにする。

### 21.1 仕様

- 各設定項目のラベル横に小さな「**?**」アイコンを付ける。**マウスオーバーで表示、クリック/タップでも表示**（タッチ環境対応。もう一度タップ or 外側タップで閉じる。同時に開くのは1つ）。
- 実装: 共通コンポーネント `public/help.js`（新規）。`data-help="キー"` を持つ要素の横に ? を自動挿入し、ポップオーバー（吹き出し、画面端で位置反転）を出す。ヘルプ文は `help.js` 内の辞書1箇所に集約（キー→テキスト）。CSSは既存テーマに合わせる。
- 文体: 1〜3文の日本語。「何が変わるか」+「どういう時に使うか」。例—「輪郭保護: セル内に暗い輪郭色が含まれるとき、代表色より輪郭色を優先します。線が途切れる・細部が消えるときに上げてください」。

### 21.2 対象（漏れなく）

変換スタジオの全つまみ（解像度/色数/グリッド微調整/代表色ブレンド/輪郭保護/彩度保護/背景除去閾値/フチ光彩除去/1:1）、分割インポートの選択肢（§20）、トンマナ欄（参考画像/解析/ガイド/適用トグル）、修正タブ（スコープ/ロック/整える/配色バリエーション）、モーション生成タブ（プリセット/フレーム数/振り幅/バウンス/向き/反映方法）、リグタブ（パーツ登録/pivot/z/親/AI自動分割/プリセット/振り幅/バウンス/フレーム数/AI清書/微調整）、タグ（fps/ループ）、書き出しパネル（形式/scale/ミラー/バリエーション/書き出し先）、プロファイル/チェックリスト、オニオンスキン/差分ビュー/逸脱メーター（タイムラインの%表示にも）。

### 21.3 検証

- Playwrightで (a) 代表的な5箇所のホバー表示とクリック/タップ表示・排他動作、(b) 画面端での位置反転、(c) `data-help` を持つ全要素に ? が付与されヘルプ辞書にキーが存在すること（ページ内で網羅チェックを実行し、辞書漏れ0を確認）、(d) コンソールエラー0。

## 22. 【v8】ポーズガイド再描画（リグ=設計図、絵はAIが描き直す）

リグ合成の機械的な回転は、描き込みの多いキャラでは破綻する（回転した直線の脚 ≠ 膝の曲がった脚）。そこでリグ出力を**ポーズの設計図**と位置づけ、最終の絵は**AIがベースフレームのテイストでポーズに合わせて描き直す**モードを追加する。

### 22.1 mode: "redraw"

- 入力: ベースフレームのグリッド（テイストの正）+ 現フレーム（リグ合成のラフ。**ポーズ・シルエット・関節位置の正**）+ 対象領域マスク + トンマナガイド（あれば）+ 前後フレームの対象領域切り出し（動きの連続性）。
- システムプロンプト原則: 「ラフのシルエット・重心・関節位置に従い、線の太さ・シェーディング段数・ディテールの描き込みはベースフレームに従って、対象領域を**描き直す**。回転による崩れ（ジャギー・つぶれた模様）はベースの該当部位を参照して正しい向きに描き起こす。パレット厳守」。§19 refine（ユーザーのラフが絵としても正）とは原則が異なる点に注意。
- 対象領域は2モード:
  - **「動いた部位のみ」（既定）**: ベースと現フレームの差分セルを5px膨張した領域 ∪ 移動した各パーツの矩形。出力が小さく速い
  - **「全身」**: キャラの非透明bbox全体。ポーズ変化が大きいときに
- サーバー: allowedMask 強制（既存機構）。newFrames / paletteChanges 破棄。フレームごとに並列リクエスト可（キュー2・進捗・部分失敗警告）。
- UI: リグタブの生成後ボタン列を「**AI清書**（軽量: 継ぎ目±2pxのみ）」と「**AI描き直し**（ポーズガイド: 動いた部位を丸ごと）」の2つに。ヘルプで使い分けを明記（まず描き直し→仕上げに清書、が推奨ルート）。
- 対象タグの全フレームへ一括適用オプション（refine同様）。

### 22.2 パーツ「固定」オプション

「腕は動かさない」用途。パーツ行に「固定」トグルを追加し、ONのパーツはプリセットのロール変換を適用せず**胴と同じ変換のみ**（バウンス追従）にする。visible（合成から除外＝消える）とは別物であることをヘルプに明記。

### 22.3 検証

- MOCK: redraw はラフのエコー（配管確認）。マスク外 edits の破棄警告。
- ダミーCLI: プロンプトに (a) ベース、(b) ラフ、(c) 領域指定、(d) redraw原則文、(e) 前後フレーム切り出しが含まれることを実測。「動いた部位のみ」でプロンプト・許可マスクが全身版より有意に小さいこと。
- 実CLI: 赤髪級（73×96・多色）の歩き1フレームを「動いた部位のみ」で実測し所要秒数をレポート。
- 固定トグル: 腕パーツ固定時、keyframes の腕変換が胴と一致することを数値検証。visible との独立性。
- 退行なし（§14清書・§19 refine・リグ生成）。

### 22.5 実測フィードバック対応（描き直しが空応答になる問題）

クラウド実測（73×96・128色・歩きF1・「動いた部位のみ」・sonnet）の結果: **846秒かかった末に適用セル数 0**（警告なし＝マスク破棄ではなく、モデルが空 edits または全 `??` を返した）。原因は2つと推定: (1) システムプロンプトの「最小差分の原則」が redraw の「描き直せ」と衝突し、現フレーム（ラフ）が既にポーズの正であるためモデルが保守的に無変更を選ぶ、(2) プロンプト60KB（フルグリッド×2+マスク+前後文脈）で思考が発散。以下で対策する。

1. **redraw プロンプトの強制力を上げる**: redrawSection を次の趣旨に書き換える —「マスク '1' の領域は機械的な回転合成による**ドラフト品質**（ジャギー・パーツの分離・つぶれた模様を含む）である。この領域を**そのまま残すのは失敗**。ベースの該当部位を参照して必ず描き直した rows を返すこと。`?`/`??`（変更なし）はマスク外と、描き直した結果たまたま同じ値になるセルにのみ使う。**この指示は「最小差分の原則」より優先する**」。空応答の逃げ道を明示的に塞ぐ。
2. **マスクbboxクロップでプロンプトを縮小**: redraw ではベースフレーム・ラフ・マスク・前後文脈をフルキャンバスではなく**マスクbbox（+2pxマージン）の切り出し**で送る。プロンプトには「この切り出しはキャンバス座標 (x0,y0) 起点」と明記し、**モデルは切り出しローカル座標で edits を返す**。サーバーが受信後に e.x/e.y へオフセットを加算してから既存のマスク検証に流す（リクエストに `cropRect: {x,y,w,h}` を追加。redraw のときのみ有効）。狙い: 入力を数分の1にして思考時間と応答時間を圧縮する。
3. **連結成分ごとの並列分割**: 「動いた部位のみ」マスクを連結成分（8近傍）に分け、成分ごとに独立した redraw リクエストを発行する（既存キューの並列2に乗せる。成分が3つ以上でも順に流れる）。各リクエストは自分の成分のbboxクロップだけを持つ。左脚と右脚が別成分になり、1リクエストあたりの領域がさらに小さくなる＋壁時計時間も短縮。成分数と進捗は `描き直し中… 2/3領域` のように表示。
4. **ゼロ適用ガード（クライアント）**: 適用セル数が 0 のとき、成功トーンの「完了…おすすめです」ではなく警告表示「AIが変更を返しませんでした（ラフのまま）。もう一度実行するか、対象領域を『全身』に切り替えてお試しください」にする。
5. **CLIデバッグログ**: `CLI_DEBUG=1` のとき、CLI呼び出しごとにプロンプトと生の stdout/stderr を `./cli-logs/<timestamp>-<mode>.txt` に保存（ディレクトリは自動作成、.gitignore に追加）。今回のような「なぜ空応答か」を実測で追えるようにする。

検証: ダミーCLI（ローカル座標の edits を返す版）でクロップ→オフセット加算→マスク検証の座標系が正しいこと（境界セルで±1のずれがないこと）。連結成分分割で2成分が並列に流れ、進捗と部分失敗警告が出ること。MOCK・§19 refine・§14 清書の非クロップ経路に退行がないこと。実CLIで同じ歩きF1を再計測し、**適用セル数 > 0 と所要秒数**をレポート（目標: 300秒以内。超える場合はその実測値を README の CLIモード注記に明記）。

### 22.6 第2回実測フィードバック対応（マスクが全身に融合する問題）

§22.5 適用後の再計測（同条件・sonnet）: プロンプトは 60640B→49578B に縮小したが、**900秒タイムアウト・出力ゼロ**。CLI_DEBUG ログで確認した原因: 歩きプリセットはバウンスで**全身が平行移動**するため「ベースとの差分セル+5px膨張」が体全体に広がり、マスクが 62×76 の単一成分に融合。プロンプト強化（§22.5-1）で逃げ道を塞いだ結果、モデルは約4700セルの真面目な描き直しを試みて時間切れになった。

鍵となる洞察: **平行移動はドット絵を劣化させない**（ピクセルがそのまま移動するだけ）。崩れるのは**回転（rot≠0）を適用したパーツ**と、その継ぎ目だけ。描き直し対象をそこに絞る。

1. **対象領域を3択に変更**（ラジオ）: 「**回転した部位のみ**（新・既定）/ 動いた部位すべて（旧moved）/ 全身」。
   - 回転した部位のみ: 対象キーフレームで rot≠0 のパーツについて、移動前後の矩形の和+2px をマーク。さらに各回転パーツの矩形と親パーツ矩形の重なり帯（継ぎ目）±2px も含める。バウンス・純平行移動パーツは含めない。
   - 対象キーフレームに rot≠0 のパーツが1つも無い場合は旧 moved 方式へ自動フォールバック（トーストで通知）。
2. **差分化の放棄を明示**: redrawSection に「クロップ内は `??` を使わず**全セルを丸ごと書き直した rows を返してよい**。差分の最小化に推論時間を使わないこと（§15.5-2 と同方針）」を追記。小さいクロップなら丸ごと書き直しが最速。
3. **大領域ガード**: CLI系バックエンド（cli/codex）で1リクエストのマスクセル数が閾値（既定1800、環境変数 `REDRAW_MAX_CELLS`）を超える場合、送信前に確認ダイアログ「対象領域が大きくタイムアウトの可能性が高い。パーツの固定や『回転した部位のみ』で領域を絞るのがおすすめ。続行する？」を出す。
4. README / ヘルプ更新: 描き直しの所要時間の実測目安と「固定トグルで回転パーツを減らすと速くなる」を明記。

検証: 歩き4フレームのキーフレームで「回転した部位のみ」マスクが脚（+振る腕）に限定され、バウンスの全身平行移動が含まれないことを数値検証（マスクセル数が旧方式より大幅減であること）。継ぎ目帯が含まれること。rot≠0 なしフレームのフォールバック。ダミーCLIで3択の配管。実CLIで歩きF1を「回転した部位のみ」で再計測し、**成分ごとの秒数・適用セル数・before/after スクショ**をレポート（目標: 1成分300秒以内）。

## 12. スコープ外（今回は作らない）

- レイヤー機能、Asepriteファイル入出力、APNG、タイルマップ、共同編集
- 128×128超のキャンバス（UIの上限で96に制限）

---

## 13. 【コア要件・最優先】元画像ベースのモーション生成とテイスト保持

**このセクションは §1〜12 に追加される要件であり、優先度は最上位。** ユースケースの中心は「手持ちのドット絵キャラ画像をアップロードし、そのテイストを一切崩さずに歩き・走り・攻撃などのモーションを生成し、おかしい部分（足の左右など）を範囲指定で高速に直す」こと。

### 13.1 PNG画像インポート（必須機能）

ヘッダメニューに「画像を開く」を追加。PNG/GIF(1枚目)/WebP を受け付け:

1. canvasに描画してピクセルを読む。
2. **拡大率の自動検出**: 元画像が例えば 320×320 でも実体が 32×32 のドット絵である場合が多い。各行・各列の同色ラン長のGCDからドット1個のブロックサイズを推定し、最近傍で実寸グリッドにダウンサンプルする（検出結果はダイアログで「32×32として読み込みます」と確認、手動上書き可）。
3. **パレット抽出**: 出現色を頻度順に列挙。33色以上なら頻度上位31色+透明に量子化（各ピクセルは最近色へ。RGB距離）。抽出結果が実寸≦96×96・パレット≦32色に収まらない場合は縮小を促すメッセージ。
4. 読み込んだ画像は **frame 0 = ベースフレーム** としてセットし、`project.baseFrame`（Uint8Arrayのコピー）としても保持する。ベースフレームはタイムライン上で「基準」バッジ表示。

### 13.2 テイスト保持の5つの機構（このツールの存在理由）

1. **パレットロック（構造的保証）**: AI出力はパレットindexのグリッドのみ。色は定義上1色も変わらない。モーション生成モードでは `paletteChanges` をサーバー側で**破棄**する。
2. **ベースフレーム・アンカリング**: すべてのAIリクエストにベースフレームのグリッドを「唯一の正」として含める。システムプロンプトに追記: 「新規フレームは白紙から描くのではなく、ベースフレームのコピーから始めて、動きに必要なピクセルだけを移動・変更する。輪郭の太さ、シェーディングの段数、ドットの打ち方の癖を厳密に踏襲する」。
3. **ロック領域（変更禁止マスク）**: 矩形選択ツールに「この範囲をロック」ボタンを追加。ロック領域（複数可、半透明赤で表示、解除可）は `project.lockedRects` に保持し、リクエストに含める。サーバーはロック領域内セルへの edits / newFrames の該当セルを**ベースの値で強制上書き**して適用する（例: 顔はロックして体だけ動かす）。
4. **逸脱メーター**: 各フレームサムネイルの下に「ベースフレームとの差分率 %」を常時表示。モーションとして妥当な差分（手足周辺）か、テイスト崩れ（全面書き換え）かを一目で判別できる。40%超は警告色。
5. **差分ビュー**: トグルで「現在フレーム − ベースフレーム」の変更セルだけをハイライト表示（変更 = マゼンタ枠）。どこをAIが触ったかを即確認できる。

### 13.3 モーション生成モード（AIパネルに追加）

AIパネルを2タブにする: 「修正」（既存の §4 のパッチ編集）と「モーション生成」。

モーション生成タブ:

- **プリセット**: 歩き / 走り / 攻撃 / 待機 / ジャンプ + 自由入力欄
- **つまみ**（すべてプロンプトに文章として織り込む）:
  - フレーム数: 2〜12（既定: 歩き4、走り6、攻撃3、待機2、ジャンプ4）
  - 動きの大きさ: 小 / 中 / 大
  - 上下バウンス: あり / なし
  - 向き: そのまま / 横（右向き）/ 横（左向き）
- 実行するとベースフレームを基に `newFrames` で全フレームを生成し、既存フレーム（frame 0 のベース以外）を置き換えるか追記するかを選べる。
- 生成後は既存の「修正」タブ + 手描きツールで逐次修正するワークフロー。

**歩きモーション用のプロンプト知識をシステムプロンプトに埋め込む**（ユーザーが毎回指示しなくて済むように）: 4フレーム歩行 = コンタクト→ダウン→パッシング→アップ、左右の足は前後が入れ替わること、接地フレームで体が最も低いこと、腕は足と逆位相、など定石を明記。走り・攻撃・待機・ジャンプも同様に1〜2行の定石を書く。

### 13.4 リクエスト形式の変更

`POST /api/edit` のボディに追加:

```js
{
  mode: "patch" | "motion",
  baseFrameGrid: "....\n...",       // ベースフレームのグリッド（常に送る）
  lockedRects: [{x,y,w,h}, ...],
  motion: {                          // mode="motion" のとき
    preset: "walk" | "run" | "attack" | "idle" | "jump" | "custom",
    customText: "",
    frames: 4, magnitude: "medium", bounce: true, facing: "keep"
  },
  // 既存: project, scope, frameIndex, selection, instruction, images
}
```

画像ブロックは mode="motion" のときベースフレームのPNG（8倍拡大）を必ず含める。

### 13.5 期待値の明示（READMEに書く）

一発でプロ品質のモーションは出ない前提のツール設計であること。価値は「テイストが構造的に崩れない土台の上で、生成 → 逸脱メーターで確認 → 範囲指定して修正指示 → 手描き微調整、のループを数秒単位で回せる」ことにある、と README に明記する。

---

## 14. 【v2】2Dパーツリグモード（v1完成・検証後に着手）

動きの機構的な正しさを骨格（リグ）で保証し、絵は元素材のドットをそのまま使い、AIには継ぎ目の清書だけをさせるモード。「AIが足の左右を描き間違える」問題を原理的に排除する。**v1のコミット・検証が完了してから実装を開始すること。**

### 14.1 データモデル追加

```js
project.rig = {
  parts: [
    {
      id: "arm_r", name: "右腕",
      patch: { x, y, w, h, pixels: Uint8Array },  // ベースフレームから切り出したドット（切り出し元はロック領域同様に記録）
      pivot: { x, y },      // 回転の支点（パッチ内座標。肩・股関節など）
      z: 0,                 // 描画順（小さいほど奥）
      parent: "torso" | null
    }, ...
  ],
  // モーション = フレームごとの各パーツの変換
  keyframes: [ { partId: { dx, dy, rot } , ... }, ... ]   // rot は度。15°刻みに丸める
}
```

### 14.2 パーツ切り出し

- 矩形選択 →「パーツとして登録」。名前・pivot（キャンバスクリックで指定）・z順・親を設定するミニダイアログ。
- **AI自動分割**（補助機能）: ベースフレームのグリッド+画像を送り、「頭/胴/右腕/左腕/右脚/左脚/武器」のパッチ矩形とpivot候補をJSONで返させる（新スキーマ、`mode: "segment"`）。結果はユーザーが調整可能な下書きとして反映。
- 切り出したパーツの重なり・隙間はそのままでよい（合成時にz順で解決。隙間はAI清書対象）。

### 14.3 モーションプリセット（キーフレームテーブル）

歩き(4f)・走り(6f)・攻撃(3f)・待機(2f)・ジャンプ(4f)を **ハードコードされたキーフレームテーブル**（フレーム×パーツ→{dx,dy,rot}）として実装する。歩きの定石（左右脚の逆位相、接地フレームで胴体が最低位置、腕は脚と逆位相）をテーブル値として直接埋め込む。つまみ:

- 振り幅: テーブルの rot/dx を 0.5×/1.0×/1.5× にスケール
- バウンス: 胴体の dy 成分の有効/無効
- フレーム数: テーブルの補間（線形）で増減

### 14.4 レンダリングと清書

1. 各フレーム: パーツをz順に、親の変換を継承しつつ pivot 周りに回転（最近傍）・平行移動して合成 → 通常のフレームグリッドに書き出す（リグはあくまで生成器。生成後は普通のフレームとして v1 の全機能で編集可能）。
2. 回転で生じるジャギー・パーツ継ぎ目の隙間は、**AI清書パス**（`mode: "cleanup"`）で修正: 変更許可セルを「合成時に変化したセルの周囲2px」に限定し、パレット内・最小差分で継ぎ目を繋ぐ。サーバー側で許可セル外の edits を破棄。
3. 清書はフレームごとに並列リクエストしてよい（進捗表示）。

### 14.5 UI

- タブ「リグ」を追加: パーツ一覧（表示/名前/z/親/pivot編集・削除）、プリセット選択+つまみ、「フレーム生成」ボタン、生成後に「AI清書」ボタン。
- フレームごとの手動微調整: リグタブでフレームを選び、パーツをドラッグ移動 / ハンドルで回転 → 再合成。
- 生成結果はタイムラインに newFrames として入り、逸脱メーター・差分ビュー・修正タブがそのまま使える。

### 14.5.5 AI自動分割の軽量化とフォールバック（実機タイムアウト対策）

実測: 51×60・29色で約88秒、H96変換後の大型・多色（2文字hex表現）素材では300秒超でタイムアウト。対策:

1. **分割専用の軽量グリッド**: mode:"segment" ではフル解像度グリッドを送らない。クライアント（rig.js）がベースフレームを**高さ≤48に最近傍で間引き、色を最大8色に量子化した1文字グリッド**に変換して `segmentGrid` + `segmentScale`（縮小率）として送る。サーバーはsegment時に segmentGrid をプロンプトに使い、縮小率を明記。**返ってきた矩形・pivot はクライアントで元解像度へスケール**（丸めはbox外接方向、pivotは比率維持）。プロンプトが数分の1になり、CLIバックエンドでも実用速度になる。
2. **簡易分割（AIなし）ボタン**: リグタブに追加。非透明bboxから人型ヒューリスティックで即時に下書きを生成——頭=上25%、胴=中央40%（bbox中央帯）、右腕/左腕=胴帯から左右にはみ出す領域、右脚/左脚=下35%を中央で左右分割。pivot は定石位置（首元・肩・股関節）、z・親も定石通り。1秒未満で下書きが出るのでAI不要・オフラインでも使える。生成後は従来どおり一覧で調整可能。
3. segment のタイムアウト・エラー時のメッセージに「簡易分割（AIなし）をお試しください」を追記。

検証: (a) ダミーCLIで segment リクエストのプロンプト文字数がフル送信時より大幅減（1/3以下）になること、(b) 縮小→復元で矩形が元解像度のキャラを正しく覆うこと（ガンマン実素材）、(c) 簡易分割がガンマンで6パーツの妥当な下書きを即時生成すること、(d) 既存のsegment MOCK経路の互換。

### 14.6 API追加

`POST /api/edit` の `mode` に `"segment"` と `"cleanup"` を追加。それぞれ専用の構造化出力スキーマ（segment: parts配列、cleanup: v1と同じedits形式）。システムプロンプトに用途別の追記。

---

## 15. 【v3】Claude Code CLI バックエンド（APIキー不要モード）

Claudeサブスクリプション（Claude Code）ユーザーがAPIキーなしでAI機能を使えるようにする。サーバーのAI呼び出し部分を抽象化し、環境変数 `BACKEND` で切り替える。

### 15.1 切り替え

- `BACKEND=api`（既定）: 現行どおり `@anthropic-ai/sdk`。
- `BACKEND=cli`: ローカルにインストール・ログイン済みの `claude` CLI を `child_process.spawn` で呼ぶ。
- `MOCK=1` は従来どおり最優先。
- `/api/config` に `backend` を追加し、UIのフッター等に表示（「バックエンド: API / Claude Code CLI / MOCK」）。

### 15.2 CLI呼び出し仕様

```js
spawn("claude", [
  "-p",                        // 非対話モード
  "--output-format", "json",   // 結果をJSONエンベロープで受ける
  "--model", CLI_MODEL,        // env CLI_MODEL、既定 "sonnet"
], { stdio: ["pipe", "pipe", "pipe"] })
// プロンプト全文（システム相当 + ユーザー相当を連結したもの）を stdin に書いて閉じる
```

- 構造化出力APIは使えないため、プロンプト末尾に「出力は次のJSON Schemaに厳密に従うJSONのみ。コードフェンス禁止」としてスキーマ(JSON文字列)を埋め込む。
- 応答: `--output-format json` のエンベロープから `result` フィールド（テキスト）を取り出し、コードフェンス除去 → `JSON.parse` → **既存のパッチ検証をそのまま通す**（検証機構は共通なので、モデル出力が乱れてもサーバーで守られる）。パース失敗時は1回だけリトライ（「JSONのみで再出力」と付けて再実行）、それでも失敗ならエラーをSSEで返す。
- 画像は渡さない（テキストグリッドのみ）。プロンプト組み立て関数に `includeImages` フラグを追加して分岐。
- ストリーミング: CLIモードでは本文ストリームは不要。SSEでは30秒ごとに `{type:"delta", text:"…"}` 相当の進捗ハートビートだけ流し、完了時に `result` を返す（クライアント側は現行実装のまま動く）。
- タイムアウト 180秒。`claude` コマンドが見つからない場合（ENOENT）は「Claude Code CLI が見つかりません。`npm install -g @anthropic-ai/claude-code` の上 `claude` にログインしてください」を返す。プロセス終了コード非0は stderr の先頭200文字を添えてエラー。
- 同時実行: cleanup の並列リクエストが来るため、CLIモードではサーバー側で同時実行数を2に制限するキューを入れる。

### 15.3 実装配置

- `server.js` の Anthropic 呼び出し部を `callBackend({ systemText, userText, images, schema, onDelta })` に抽出し、`api` / `cli` / `mock` の3実装を持たせる。既存のバリデーション・SSE整形・モード分岐（edit/motion/segment/cleanup）は一切変えない。
- README に CLI モードのセットアップ（Claude Code のインストール・ログイン・`BACKEND=cli npm start`）と、サブスク利用上限を消費する旨・画像を渡さない旨を追記。

### 15.5 実機フィードバック対応（広範囲修正のタイムアウト対策）

Windows実機での検証結果（1セル修正=約18秒で成功 / 「キャラの輪郭を黒くして」=180秒タイムアウト）を受けた改善。

1. **タイムアウトの延長と環境変数化**: 既定を 300 秒に延長し、`CLI_TIMEOUT`（秒）で上書き可能にする。（改: Windows実機でAI分割160秒超・描き直し数分などブレが大きいため、既定を 900 秒に再延長した）
2. **広範囲変更のプロンプト緩和**: システムプロンプトの「最小差分の原則」に追記 —「変更が対象領域の大部分に及ぶ場合は、`?` による差分化に固執せず、対象矩形全体（またはフレーム全体）を書き直した rows を返してよい。差分の厳密さより応答の速さを優先する」。`?` の差分計算がモデルの推論時間を浪費するのを防ぐ。
3. **scope=all のフレーム分割**: `scope:"all"` かつ mode が edit のリクエストは、サーバー側で**フレームごとの個別バックエンド呼び出しに分割**する（既存の同時実行キューで並列2）。各フレームの指示文には「全フレーム共通の指示を、このフレームに適用せよ。他フレームとの一貫性を保て」と付記。結果の edits をフレーム番号を差し替えて統合し、1つのSSE resultで返す。1呼び出しあたりの出力量と推論時間を分割する狙い。分割時は進捗を `{type:"delta"}` で「フレーム 2/5 完了」のように流す。newFrames を含む応答は分割モードでは破棄して警告（中割りは scope=frame で行う運用）。
4. **ハートビート強化**: CLIモードの進捗ハートビートを10秒間隔にし、経過秒数を含める（クライアントは受信テキストをそのまま進捗表示にできる）。UI側は実行中に経過秒を表示。
5. **タイムアウト時のエラーメッセージ改善**: 「選択範囲を狭める / CLI_TIMEOUT を延ばす / CLI_MODEL=haiku を試す」の3つの対処を含める。
6. README の CLIモード注記に「広範囲の修正は時間がかかる。矩形選択で範囲を絞るのが最速」を追記。

検証: ダミーCLI（応答に2秒かかる版）で scope=all 分割の並列実行・進捗delta・結果統合を確認。タイムアウト（CLI_TIMEOUT=1 で強制発火）時のメッセージ内容を確認。既定バックエンド(api)の非分割経路が退行していないこと。

### 15.4 検証

- `claude` が無い環境でも検証できるよう、**ダミーCLI**（`scratchpad/fake-claude`: stdin を読み、固定パッチJSONを `--output-format json` エンベロープで出力するNodeスクリプト）を作り、`PATH` 先頭に置いて `BACKEND=cli` の全経路（edit/motion/segment/cleanup、パース・検証・SSE完了）を curl で確認する。
- ENOENT経路（PATHから外して起動）でエラーメッセージが返ることを確認。
- コードフェンス付きJSONを返すダミーで除去→パース成功を確認。
- 既存の §11 / §13 / §14 検証が `BACKEND=api`（既定）で退行していないことをスモーク確認。

### 22.7 パーツ「役割」ドロップダウン（名前正規表現の隠し仕様を明示化）

現状、プリセットモーション（歩き等）とパーツの対応付けは `roleOf()` の名前/id正規表現マッチという**画面に説明のない隠し仕様**（「脚」だけだと胴扱い、未分類も胴扱い＝バウンスのみ）。ユーザーが手動でパーツを作る際に脚が脚として動かない事故が起きる。明示的に選べるようにする。

1. **データ**: パーツに任意フィールド `role`（"head"|"torso"|"arm_r"|"arm_l"|"leg_r"|"leg_l"|"weapon"|"other"）を追加。未設定（既存プロジェクト・AI/簡易分割の出力）は従来どおり `roleOf(part)` で名前から推定。`role` が設定されていればそれが**常に優先**。"other" は胴と同じ扱い（バウンスのみ）だが、名前推定に戻らない明示的な選択。
2. **UI**: リグタブのパーツ行に「役割」セレクトを追加（表示名: 頭/胴/右腕/左腕/右脚/左脚/武器/その他）。初期表示値は `role` があればそれ、なければ名前推定の結果（推定値には「（自動）」を付けたオプション表示にせず、単に推定結果を選択状態にして、ユーザーが一度変更したら `role` として保存）。変更時トースト「次のフレーム生成から反映されます」。
3. **AI/簡易分割との関係**: 分割結果のパーツは id が英語ロール名なので名前推定で足りる。分割実行時に `role` は設定しない（推定に任せる）。ユーザーが上書きしたら以降はそれが正。
4. **プロジェクトJSON**: 保存/読込で `role` を透過的に維持（未知の値は無視して推定にフォールバック）。
5. **ヘルプ**: パーツ一覧のヘルプに役割の意味（歩き/走り等での動き方: 脚=前後スイング逆位相、腕=脚と逆位相、武器=腕に追従、頭/胴/その他=バウンスのみ）と「名前からの自動推定より役割の選択が優先」を記載。README のリグ手順にも1行追記。

検証: 役割を明示したパーツが名前と無関係にキーフレーム変換を受けること（例: 名前「マント」role=leg_r が脚として振れる）を数値検証。未設定パーツの後方互換（名前推定のまま）。保存→読込で role 維持。固定トグル（§22.2）との併用（固定が最優先）。ヘルプ表示。退行なし（リグ生成・§22.6 rotated マスク — role 上書きが rot≠0 判定にも効くこと）。

### 22.8 パーツ矩形・pivot のドラッグ編集（作成後の調整）

手動登録・AI/簡易分割で作ったパーツの矩形は、現状「削除して作り直し」でしか直せない。リグタブ表示中にキャンバス上で直接ドラッグ調整できるようにする（フロントのみ・サーバー変更なし）。

1. **パーツ選択**: パーツ行クリック、またはキャンバス上のパーツ枠線（±4pxヒット判定）クリックで「選択中パーツ」。選択中は枠を強調表示（太線+ハンドル描画）し、パーツ行もハイライト。Esc または空クリックで解除。
2. **ドラッグ操作**（選択中パーツのみ・セル座標スナップ）:
   - 枠内ドラッグ = 矩形の移動（キャンバス内クランプ）
   - 四隅・四辺の8ハンドル（小さい四角を枠上に描画）ドラッグ = リサイズ（最小1×1、キャンバス内クランプ。pivot が矩形外に出る場合は矩形内にクランプ）
   - pivot 十字のドラッグ = 支点移動（パッチ内ローカル座標にクランプ）
   - ドラッグ中はステータス（またはツールチップ）に `(x,y) w×h` / `pivot(x,y)` をライブ表示
3. **既存操作との競合回避**: リグタブ表示中のみ有効。パーツ枠・ハンドル・pivot 以外の場所は従来どおり（パン・ペン・矩形選択）。ドラッグ開始判定はパーツ側を優先。
4. 変更は part.patch / part.pivot に即時反映（アンドゥ対象）。ステータスに一度だけ「次のフレーム生成から反映されます」。
5. ヘルプ（パーツ一覧・キャンバス）に操作説明を追記。

検証: Playwright で移動・8方向リサイズ・pivot移動の patch/pivot 数値検証、クランプ（キャンバス外・最小サイズ・pivot矩形外）、アンドゥで戻ること、選択解除、非リグタブで無効、既存のパン/ペン/矩形選択と非干渉、フレーム生成に反映。

### 22.9 描き直しプロンプトの生成的リフレーミング（GPT実機失敗対応）

Windows実機（BACKEND=codex / GPT）での初の実戦投入で、描き直しが**ポーズをベースの立ちポーズに戻す**失敗。原因分析: 現行の redrawSection は「ドラフト領域の修正」という corrective な枠組みで、「ラフをそのまま残すのは失敗」とは言うが「**ベースのポーズに戻すのも失敗**」とは言っていない。絵柄見本として渡しているベースの丸写しが、モデルにとって最も楽で「正解らしい」出力になっている。ユーザー方針: 失敗検知→リトライは効率が悪い。**一発目の精度を上げる**。指示を「修正」ではなく「そのポーズの新規描き起こし」に変える。

1. **redrawSection 全面改稿**（要旨。文言は実装時に調整可、以下の要素を必ず含む）:
   - 役割付け: 「あなたはこのキャラクターの新しいポーズを1枚描くドット絵師です」
   - **ラフ = ポーズのあたり（下書き）**。傾き・位置・シルエット・関節の曲がりはラフに従う
   - **ベース = 絵柄の見本**（線の太さ・シェーディング段数・色使い・ディテール密度）。**ポーズをベースから取るのは失敗** — ベースと同じ姿勢に戻さない。ベースとラフで姿勢が違うのは意図的（アニメーションの1コマ）である
   - **欠損の補完**: ラフで欠損・崩壊している部分（回転で千切れた/分離したパーツ、つぶれた模様、マント・髪・体の一部の欠け）は、ベースの該当部位を参照して**ラフの向き・位置に合わせて描き起こす**
   - ラフをそのまま残すのも失敗（機械合成のドラフト品質）
   - クロップ全域の rows を丸ごと返す（`??` 差分化に推論時間を使わない。既存 §22.6-2 を統合）
2. **事後チェックは警告のみ**（自動リトライ・自動差し戻しはしない）: 適用後、マスク領域内の一致率で「結果がラフよりベースに近い」かつ「ラフとベースの差が有意（>N セル）」のとき、ステータスに「ポーズがベースに戻された可能性があります。指示欄に補足を書いて再実行してください」と警告。単純なセル一致率比較で実装（追加のAI呼び出しなし）。
3. **フォールバック経路の再確認**: 「回転した部位が無いため…」の自動フォールバック時に実行が止まったという実機報告があるため、Playwright で rotated→フォールバック→送信完了の順路を再検証し、止まる経路（例: jobs 0件判定との相互作用）があれば修正。フォールバックのトーストがエラー調に見えないことも確認。
4. 検証: ダミーCLIでプロンプトに上記要素（役割・ポーズ逆戻り禁止・欠損補完・丸ごと出力）が含まれること。事後チェック警告の発火/非発火の数値ケース（ベース丸写し応答→警告あり、ラフ準拠応答→警告なし）。§19 refine / §14 清書のプロンプト非影響。退行なし。

### 22.10 描き直しラボ（プロンプトを実データで改善していくループ）

描き直しはこのツールの本丸であり、プロンプトの当たり外れが品質を決める。ユーザー方針: この部分を重点拡充し、**実際の失敗例・成功例からプロンプトを学習していく**。そのための3点セット。

1. **プロンプト補足欄（実験用ノブ）**: リグタブの描き直しボタン付近に折りたたみ「プロンプト補足（上級者向け）」textarea を追加。入力があれば redrawSection の末尾に「## 追加の指示（ユーザー）」として付加。localStorage に永続化（プロジェクト非依存）。これでコード変更なしに、PC上でプロンプト仮説を即試せる。ヘルプに「うまくいった文言は報告してもらえれば標準プロンプトに取り込む」と記載。
2. **ワンクリック評価**: 描き直し完了後のステータス行に「👍うまくいった / 👎ダメだった」ボタンを表示（次の操作まで）。押すと POST /api/redraw-feedback に {timestamp, backend, model, verdict, プロンプト補足, ジョブごとの base/rough/result のクロップグリッド+mask+cropRect, 警告} を送り、サーバーが `./redraw-feedback/<timestamp>-<verdict>.json` に保存（ローカルのみ・.gitignore、EXPORT_ROOT 不要）。👎時は任意の一言コメント入力（prompt() で簡易に）。
3. **評価データの活用ループ（運用）**: たまった feedback JSON をユーザーがオーケストレーターに共有 → 失敗パターンを分類し redrawSection を改稿 → 過去の feedback を疑似リプレイ（保存済み base/rough/mask で再実行し、結果の傾向比較）して改善を確認 → push。DESIGN.md に「プロンプト変更履歴」を §22.10.1 として追記していく（どの失敗パターンにどの文言で対処したかの台帳）。

検証: 補足欄入力→ダミーCLIでプロンプト末尾に付加されること・空なら付加されないこと・localStorage 復元。評価ボタン→ファイル生成と内容（グリッドが再実行に足る完全性）、.gitignore。連続実行時に評価対象が直近ジョブに正しく紐づくこと。退行なし。

### 22.11 実変化セル数の表示とエコー検出（GPT実機第2ラウンド対応）

実機GPT（v4+ユーザー補足「edits空禁止」）の結果: 「適用セル数 6995」なのに見た目が無変化 = **ラフの丸写し（エコー）**。現行カウントは「書き込んだセル数」なので丸写しでも大きな数になり、成功に見えてしまう。

1. applyEditsToFrames が「書き込みセル数」と**「実際に値が変わったセル数」**を分けて返すようにし、ステータスは「適用 6995 / 実変化 312」の形式に（清書・描き直し共通）。
2. 実変化 0（かつ書き込み>0）のとき警告「AIがラフをそのまま返しました（エコー）。プロンプト補足で『ラフの写しではなく描き直す』ことを強調するか、モデルを変えてお試しください」。ゼロ適用ガード（§22.5-4）・ベース逆戻り警告（§22.9-2）と排他で、優先順: ゼロ適用 > エコー > ベース逆戻り。
3. 👍👎フィードバック JSON に writtenCells / changedCells を追加。
4. 検証: ダミーCLI（ラフ丸写し応答）→ エコー警告。実変化ありの応答 → 「適用/実変化」の数字が正しい。§22.9-2 の数値ケースが退行しないこと。

### 22.12 描き直し v5: パーツ回転清書（タスクの再設計）

v4 の実測結論（クラウドClaude・実機GPT、§22.10.1 参照）: 領域サイズ・文言をどう変えても、「文字グリッドから回転後の部位を描き起こす」というタスク設計そのものが両モデルの能力レンジ外。真面目にやると考え込んでタイムアウト（Claude）、雑にやるとエコーか崩壊（GPT）。プロンプトの言い回しではなく**渡す情報の構造**を変える。リグは回転角・pivot・元パーツの絵をすべて知っているので、「無から描く」を「**お手本を回して清書する**」に変換する。

1. **rotationInfo の付加（クライアント）**: area=rotated のとき、連結成分がちょうど1つの回転パーツに対応する場合（成分bboxがそのパーツの移動前後矩形の和+マージン内に収まる場合）、リクエストに `rotationInfo` を付ける: `{ partName, angle（キーフレームのrot。親の回転は加算済みのワールド角）, pivot: {x,y}（クロップ局所座標）, basePartRows: ベースフレームからのパーツ矩形切り出しグリッド }`。対応が曖昧な成分（複数パーツ融合など）は rotationInfo なし（v4文面のまま）。
2. **redrawSection v5（サーバー、rotationInfo があるとき）**: 構成 —
   - 「素材: ベースのパーツ『右脚』の絵（WxH）」= basePartRows
   - 「このパーツを pivot (x, y) を中心に時計回り N° 回転させた1枚を描く」
   - 「フレームNのグリッド（クロップ）は機械回転による**下敷き**。シルエット・位置はこれが正解（±1px以内）。ただしジャギー・穴・千切れを含むドラフトなので、線・シェーディングは素材パーツを参考に手描き品質で仕上げる: 輪郭は途切れない線でつなぐ、シェーディングの帯は素材と同じ段数・同じ側、孤立ピクセルを残さない、パレット厳守」
   - 「クロップ全域の rows を丸ごと返す」（座標系・マスク検証は既存のまま）
3. rotationInfo が無い経路（moved/full・融合成分）は v4 のまま。§22.10 プロンプト補足・§22.11 実変化カウント・👍👎はそのまま乗る。フィードバック JSON に rotationInfo の有無と angle を追記。
4. 検証: ダミーCLIでプロンプトに（素材パーツ・角度・pivot・下敷き・±1px制約）が含まれること／融合成分で v4 文面に戻ること。**実CLI（クラウドClaude sonnet）で §22.12 実験と同一条件（右脚のみ・1147セル級）を再計測**し、v4 の「実変化9 / 545秒」「丸写し禁止でタイムアウト」に対して所要秒数と実変化・見た目（スクショ）がどう変わるかをレポート。台帳 §22.10.1 に v5 結果を追記。

### 22.13 ユーザー手描きセルの意図保護（機械ラフとの区別）

実機の bad.json 分析で判明: ラフの一部（コートのベタ塗り）は機械回転の崩れではなく**ユーザーがペンで塗り足した意図**だった。現行の redraw はラフ全体を「機械合成のドラフト」として扱うため、手描きの意図まで描き替え対象になる。§19 refine（ラフ=意図）と §22 redraw（ラフ=ゴミ）の原則が1フレーム内で混在するケースへの対応。

1. **手描き記録**: リグの「フレーム生成」実行時点を起点に、以降のペン/塗りつぶし/消しゴム編集で変更されたセルをフレームごとに記録（`frame.userPaintedMask`、Uint8系。フレーム再生成でリセット。保存/読込で維持）。
2. **redraw プロンプトでの扱い**: 対象領域内に userPainted セルがある場合、マスクとは別に「ユーザー加筆マスク」をプロンプトに追加し、「'1' のセルはユーザーが手で描いた**意図**である。シルエット・形を保ち、打ち方だけ周囲と馴染ませる（refine 原則）。それ以外の領域は従来どおり描き直す」と指示。
3. UI: 描き直し実行時に加筆セルが含まれるとき、進捗に「手描き部分は意図として保護」と1行表示。ヘルプ追記。
4. 検証: 生成→ペンで数セル加筆→redraw リクエストに加筆マスクが含まれ、ダミーCLI プロンプトに refine 原則文が出ること。加筆なしでは従来プロンプトのまま。保存/読込で維持。再生成でリセット。

#### 22.10.1 プロンプト変更履歴（台帳）

- v1（§22.1）: 初版（refine と対になる「描き直し」原則）→ 失敗: 846秒無変更（§22.5で対処）
- v2（§22.5-1）: 「そのまま残すのは失敗」強制 → 失敗: 全身マスク融合で900秒タイムアウト（§22.6で対処）
- v3（§22.6-2）: 丸ごと書き直し許可 → 失敗: GPTがポーズをベースに戻す
- v4（§22.9）: 生成的リフレーミング（ポーズ逆戻り禁止・欠損補完）→ 実測: Claude=ほぼエコー（実変化9/1147・545秒）、丸写し禁止補足で900秒タイムアウト。GPT=エコー（大小領域とも）、補足で実変化1420だが描画崩壊
- v5（§22.12）: タスク再設計「パーツ回転清書」（素材＋角度＋下敷き）→ 実測（クラウドClaude sonnet・右脚のみ1147セル・prompt 20513B）: 758秒経過時点で計測側の事故によりSSE切断・計測不能（少なくとも v4 の545秒より長考。応答は未着）
- 参考: §25 motionframe 実測（クラウドClaude sonnet・75×96/128色のフルフレーム1枚・prompt 21372B）: **900秒タイムアウト・出力なし** → グリッド系の生成タスクは cli/Claude では実用外。§25 は codex（GPT）専用+画像経路（§25.6/§25.8）を主経路とする判断を裏付け

## 23. 【v8】Codex CLI バックエンド（ChatGPTサブスクでGPTモデルを使う）

`BACKEND=cli` が Claude Code CLI のサブスク認証を使うのと同様に、**OpenAI Codex CLI（`codex`）のChatGPTサブスク認証**で GPT 系モデルを推論に使うバックエンドを追加する。APIキーは不要（ユーザーは `codex login` でChatGPTアカウントにログイン済みであること）。

### 23.1 サーバー（BACKEND=codex）

- 環境変数: `BACKEND=codex` で有効化。`CODEX_PATH`（既定 `codex`。Windowsでの実体パス指定用 — CLI_PATH と同じ動機）、`CODEX_MODEL`（既定は空 = codex 側の既定モデル。指定時は `-m <model>` を付与）。タイムアウトと同時実行数は既存の `CLI_TIMEOUT` / `CLI_CONCURRENCY` を共用する（意味が同じ設定を増やさない）。
- 呼び出し: `codex exec` の非対話モードを使う。
  - 引数: `codex exec --sandbox read-only --skip-git-repo-check --output-last-message <一時ファイル> -`（`-` で stdin からプロンプトを読む）。作業ディレクトリはサーバーの cwd でよい（read-only サンドボックスなので書き込みはされない）。
  - プロンプトは CLI バックエンドと**同一**（スキーマ埋め込み済みの buildCliPrompt 出力を再利用）。画像は送れない（cli と同様 includeImages=false）。
  - 応答: `--output-last-message` のファイルを読み、コードフェンス除去→JSONパース→検証。失敗時は1回だけ「JSONのみで返せ」を付けて再試行（cli と同じリトライ方針）。一時ファイルは `os.tmpdir()` 配下に作り、読み取り後に削除。
  - spawn には `windowsHide: true` と `[codex] spawn ...` のログ行（cli と同形式）。ENOENT 時は「codex が見つかりません。`npm i -g @openai/codex` でインストールし、`codex login` でログインしてください。パスが解決できない場合は CODEX_PATH を設定」を返す。
  - `CLI_DEBUG=1` のログ保存（§22.5-5）は codex バックエンドにも適用する。
- 既存の cli 用タイムアウト診断（stderr/stdout 先頭300字をエラーに含める）と同時実行キューを共用。

### 23.2 UI / README

- UI変更は最小: ヘッダのバックエンド表示（既存があれば）に `codex` を出す程度。モード分岐はサーバー側で完結しているため、フロントは無変更で動くこと。
- README: バックエンド表に `codex` 行を追加。セットアップ手順（`npm i -g @openai/codex` → `codex login`（ChatGPT Plus/Proでログイン）→ `set BACKEND=codex` → 必要なら `set CODEX_PATH=...`）。注記: 「モデルの得手不得手により、ドット絵パッチの品質・所要時間は Claude バックエンドと異なる場合がある。まずは1セル修正など小さな指示で試すこと」。

### 23.3 検証

- **ダミーcodex**（`scratchpad/fake-codex`: 引数から `--output-last-message` のパスを拾い、固定パッチJSONをそこへ書いて正常終了するNodeスクリプト）で、BACKEND=codex の全経路（edit/segment/refine/redraw の代表、パース・検証・SSE完了）を確認。
- コードフェンス付きJSONをファイルに書くダミーで除去→パース成功、壊れたJSONで1リトライ→エラーメッセージ、ENOENT 経路（CODEX_PATH=/nonexistent）のガイダンス文言を確認。
- 実 codex はこのクラウド環境では認証できないため（ChatGPTログインが必要）、実機確認はユーザーのPCで行う。可能なら `npm i -g @openai/codex` だけ行い `codex exec --help` でフラグ名の実在を確認する（ネットワーク不可ならスキップし、その旨をレポート）。
- BACKEND=cli / api / MOCK の退行なし。

### 23.4 Windows 実機フィードバック対応（spawn EPERM）

Windows実機で BACKEND=codex 実行時に「spawn EPERM」。原因: npm グローバルインストールの `codex` の実体は `codex.cmd`（バッチシム）で、Node の `spawn` は `.cmd`/`.bat` を直接起動できない（Node のセキュリティ修正以降 EINVAL/EPERM になる）。対応:

1. **シム自動対応**: 起動コマンド（CODEX_PATH / CLI_PATH の解決結果）が `.cmd` / `.bat` で終わる場合、**または拡張子なしのコマンド名の場合**（改: CODEX_PATH 未設定の素の `codex` が Windows 実機で EPERM になったため。cmd.exe は PATHEXT 解決で PATH 上の codex.cmd を見つけられる）、Windows では `cmd.exe /d /s /c ""<path>" <args...>"` 形式で起動する（各引数はスペースを含む場合に二重引用符で囲む。プロンプトは従来どおり stdin 渡しなのでエスケープ面積は小さい）。それ以外は従来どおり直接 spawn。
2. **EPERM/EINVAL のエラーメッセージ改善**: spawn エラーが EPERM または EINVAL のとき、「Windows では npm 版 codex/claude の実体が .cmd シムのため直接起動できないことが原因の可能性が高い。CODEX_PATH（または CLI_PATH）に .cmd のフルパスか、実体の .exe（`npm root -g` 配下の @openai/codex 内の *.exe）を設定してください。ウイルス対策ソフトのブロックの可能性もあります」というガイダンスを返す。
3. README の codex セットアップに Windows 注記を追記（CODEX_PATH の探し方: PowerShell で `Get-Command codex` の Source、それが .cmd の場合はそのままでよい（サーバーがシム対応する）こと、実体 .exe を直接指定する選択肢）。

検証: ダミー .cmd シム（`fake-codex.cmd` → node スクリプトを呼ぶ）を CODEX_PATH に指定して Linux では検証できないため、シム判定と cmd.exe 引数組み立てを単体関数に切り出して**引数組み立ての文字列をユニットテスト**（スペース入りパス・二重引用符の位置）。EPERM を人工的に投げるダミーで新ガイダンス文言。非 Windows 経路の退行なし。

## 24. フッターのバージョン表示

「git pull が反映されたか分からない」問題の解消用。package.json の version と起動時点の git コミットID（`git rev-parse --short HEAD`。git 不在なら省略）をサーバー起動時に取得し、`/api/config` の `version` / `commit` で公開。フッターのバックエンド表示に「 · v1.8.0 (9a3cb9c)」の形で併記し、title に「git pull 後はサーバー再起動+ブラウザ再読込で更新」の注記。version は機能追加のたびに手で上げる（設計の【vN】と揃える運用: §22系=1.8.x）。

## 25. 【v9・最優先】モーション候補スタジオ（生成→選別→確定）

背景: リグ+AI描き直し路線の実測（§22.10.1 台帳）により、「機械回転の崩れをAIに修理させる」方式は収束が遅すぎると判断（1パーツ1フレームで数分〜タイムアウト×多数の試行）。ユーザー決定により発想を転換する: **変換スタジオ（§18）の「候補ギャラリー→選別→微調整」ワークフローをモーション生成に適用する**。リグ機能は削除せず、微調整・ポーズ下敷き用の道具として残す。

### 25.1 生成

- 入力: ベースフレーム（元絵のコンテキストの正）+ トンマナ（§17、有効時は必ず同梱）+ プリセット（歩き/走り/攻撃/待機/ジャンプ/カスタム自由文）+ フレーム数N（プリセット既定値）+ 候補数K（フレームごと。既定3、1〜4）。
- **N×K 個の独立した単フレーム生成リクエスト**を発行（既存キュー並列2）。新モード `mode:"motionframe"`:
  - プロンプト: ベースのグリッド + トンマナ + 「このキャラの{preset}アニメーション（全Nフレーム）の第{i}フレームを描く: {phaseHint}」+ 候補ごとのバリエーション文（例: 歩幅広め/標準/控えめ、腕の振り大きめ/小さめ）+ 「キャンバス全体を1枚、newFrames で返す。パレット厳守。キャラの頭身・配色・輪郭の太さはベースを維持」。
  - phaseHint はプリセット×フレーム位置の定型文テーブル（歩き4f: 「右脚を前に踏み出し接地、体はわずかに沈む」/「両脚が交差するパッシング、体は高め」/ 左右反転 /…。走り6f・攻撃3f・待機2f・ジャンプ4f も同様に定義。リグの keyframe テーブルの知見を言語化して流用）。
  - 出力: newFrames 1枚のみ。edits / paletteChanges は破棄+警告。
- 進捗: 「生成中… 7/12（経過 4:12）」。失敗スロットはエラー表示+単発「再生成」。

### 25.2 ギャラリー（選別）

- モーダル（§18と同系のUI）: 列=フレーム1..N、行=候補1..K。各セルは拡大ドット描画。
- 候補ごとの操作: **採用**（フレームごとに1つ、枠強調）/ **削除** / **描き直し**（任意の追記指示を添えて単発再生成。**採用済みの前後フレームがあればグリッドを前後文脈として同梱**=連続性を上げる）。フレーム列ごとに「追加生成」（K+1個目）。
- モーダル内に**採用中セットの連続再生ミニプレビュー**（現プロジェクトfps）。
- 全フレーム採用で「確定」→ タイムライン末尾に追加+プリセット名のタグ付与。
- 確定後の各フレームに「エディタで開く」（モーダルを閉じてそのフレームを選択）。

### 25.3 仕上げ（ピンポイント部位修正）

確定後は既存機能がそのまま最終調整レイヤーになる（ヘルプで推奨ルートとして明記）:
- **矩形選択 + 指示**（mode patch, scope selection）: 部位を囲って「この腕をもう少し上げて」等 → その範囲だけ描き変え
- **ペンでラフ → 部分仕上げ**（§19）: 形を自分で決めたいとき

### 25.4 バックエンド注記

- 単フレーム生成はフルサイズ出力のため重め。73×96・128色で1枚あたり: codex/GPT 実測目安1〜2分、cli/Claude は5分超の可能性。K=3×N=4=12枚を並列2で、初回バッチは10〜30分想定。**候補生成は codex 推奨**と README/ヘルプに記載（放置できるバッチなので、対話的な修理より実用的）。
- REDRAW_MAX_CELLS 系のガードは適用外（意図的なフルフレーム出力のため）。CLI_DEBUG は対象。👍👎は流用しない（ギャラリーでの目視選別が評価そのもの）。

### 25.5 検証

- MOCK: motionframe（ベースのエコー1枚）で N×K ギャラリー・採用/削除/再生成/追加生成・確定→タグ付き追加・ミニプレビュー。
- ダミーCLI: プロンプトに（ベース・トンマナ・第i/N・phaseHint・バリエーション文・newFrames指示）が含まれること。前後文脈が「描き直し」時のみ入ること。newFrames以外の破棄。
- 実CLI（クラウド）: 1枚の所要実測 → README の目安を実数化。
- 退行なし: §18 スタジオ・リグ系・既存 motion モード・§22 系。

### 25.6 画像からの候補追加（画像AI経路の一級市民化）

ユーザーの実運用では ChatGPT の画像生成が最高品質の供給源（ポーズ・スタイル維持が文字グリッドLLMより圧倒的に強い）。§25 ギャラリーの候補ソースを2本立てにする。

1. ギャラリーモーダルに「画像から候補追加」: 画像ファイル（単発 or シート）を受け取り、シートなら §20 の分割を通し、各コマを §18 変換に流す。ただし通常変換と違い**プロジェクトパレットへのスナップ**を必ず行う（各セルをプロジェクトパレットの最近色に量子化。新色は追加しない）。
2. **位置合わせ**: 変換後、ベースフレームと (a) 足元基準（最下段の非透明行を一致）(b) 水平は重心一致 で自動整列し、矢印ボタンで±ナッジ可能。整列プレビューはベースとの半透明重ね。
3. 取り込んだコマは指定フレーム列の候補としてギャラリーに並ぶ（グリッド生成候補と同格。採用/削除/差分採用マージの対象）。
4.5改補足（依頼キットの文面方針・実機フィードバック反映）: 依頼文は「画風を参照と完全に同じに」と要求**しない**。画像モデルにピクセル忠実性を強制するとポーズが崩れる実害が出たため、キットの文面は「**ポーズが最優先。ドットの細かさ・質感は参照と違ってよい（ツール側で変換・パレット統一する）。守るのはシルエット・頭身・おおまかな配色・装飾の有無だけ**」という優先順位を明記する構成にする。

4. **ミラー補完**: 横向きサイクル用に「反転で補完」ボタン — 採用済みフレーム i の左右反転をフレーム j の候補として生成（歩き4f: 3=flip(1), 4=flip(2) の対応表）。非対称部（武器等）は後段の部位修正で直す前提をヘルプに明記。
4.5. **GPT依頼キット**: ギャラリーに「GPT用依頼キットを書き出し」ボタン — (a) ベースフレームを8倍最近傍拡大のPNGでダウンロード、(b) 依頼文をクリップボードへコピー。依頼文はツールが組み立てる: プリセットのコマ数・「横一列・等間隔・キャラの大きさと足元の位置は固定・背景は単色（発光やフチ無し）・画風は参照画像と完全に同じ」+ 各コマの phaseHint（§25.1 のテーブルを流用）+ トンマナ設定（§17）が有効ならその本文。カスタムプリセットでは自由文を反映。ヘルプに往復手順（キット書き出し→ChatGPTに貼る→出てきたシートを「画像から候補追加」へ→リテイクは差分採用マージで部位だけ取り込む）を明記。
5. 検証: 3ポーズシート→分割→スナップ（パレット外色が0であること）→整列（足元行一致）→候補化→採用→確定。ミラー補完の対応表。既存 §18/§20 の退行なし。

### 25.8 共有フォルダ連携（GPT往復の摩擦最小化）

ChatGPT はローカルパスを読めないため完全自動は不可（APIキー無し条件）。ツール側の工程をすべて共有フォルダで自動化し、手作業を「ChatGPT内の3操作（参照PNGドラッグ・依頼文ペースト・画像を in/ に保存）」まで削る。

1. プロジェクト直下に `gpt-exchange/out/` と `gpt-exchange/in/`（サーバー起動時に自動作成、.gitignore 追加）。**置き場所は環境変数 `EXCHANGE_DIR` で上書き可能**（例: Google Drive for Desktop 配下を指定すると、スマホの ChatGPT で生成→Drive保存→PCに同期→自動取り込み、という非同期ワークフローが成立する。README にこの運用例を記載）。
2. **キット書き出し先の変更**: §25.6-4.5 の依頼キットは out/ に保存（reference.png + prompt.txt。依頼文はクリップボードにもコピー）。保存完了トーストにフルパスを表示。
3. **in/ の監視**: サーバーが `fs.watch` で in/ を監視し、新規画像（png/jpg/webp）を検出したら SSE またはポーリング（GET /api/exchange-inbox）でフロントへ通知。フロントはギャラリーが開いていれば §25.6 の取り込みパイプライン（分割→変換→スナップ→整列）へ自動投入し「新しい候補を取り込みました」トースト。ギャラリーが閉じていればバッジ表示。取り込み済みファイルは in/done/ へ移動（再取り込み防止）。
4. セキュリティ: 監視対象はプロジェクト直下の gpt-exchange のみ（パス固定・シンボリックリンク不追跡）。ファイルサイズ上限 20MB。
5. 検証: out/ へのキット生成（内容の完全性）。in/ に画像を置く→自動取り込み→done/ 移動。非画像ファイル無視。ギャラリー閉時のバッジ。退行なし。

### 25.7 差分採用マージ（画像AIラウンドトリップ対策の本命）

画像モデルは「原型を留めろ」と言っても細部を描き変える。対策はツール側で決定論的に行う: **取り込んだ候補の変化を塊ごとに選んで採用する**。

1. 対象: ギャラリーの任意候補 vs そのフレームの現在値（または確定済みフレーム）。パレットスナップ・整列済みが前提。
2. セル差分を取り、**連結成分（8近傍、少数セルの飛び地は近接統合）で塊に分割**。各塊をキャンバス上に色付きオーバーレイ+チップ（「塊A 214セル」等）として表示。
3. 塊をクリック（またはチップ）で**採用/却下トグル**。ブラシで塊マスクの手動加減も可能（ペン式に足す/引く）。採用中の見た目は合成プレビューで常時反映。
4. 「適用」で採用塊のセルのみ候補から取り込み、他は不変。アンドゥ対象。
5. これにより「GPTが全身を描き変えて返す」問題は無害化される（採用しない変化は存在しないのと同じ）。ヘルプに推奨フロー明記: GPT再生成 → 取り込み → 欲しい部位の塊だけ採用。
6. 検証: 既知の2グリッドで塊分割数・各塊のセル数の数値検証。採用/却下/ブラシ加減/適用/アンドゥ。スナップなし候補では適用不可（ガード）。

### 26. 【v9】パーツレイヤーの完全化（疑似パペット強化・リグの穴問題解消）

リグのパーツは現在ベースからの切り抜きで、動かすと後ろに穴が開く（歩きGIFの脚千切れの一因）。パーツを**完全な1枚レイヤー**にする。

1. パーツごとに「レイヤー完全化」操作: そのパーツ矩形のうち、z順で手前の他パーツに隠されているセル + 透明セルのうちパーツ本体に囲まれた穴を対象に、AIへ補完リクエスト（mode: refine 相当の小規模タスク。対象は通常数十セルでグリッドLLMの得意レンジ）。結果は part.layerPixels（矩形サイズの独立レイヤー）として保存。
2. リグ合成は layerPixels があるパーツはそれを使用（無ければ従来の切り抜き）。移動・回転で穴が開かなくなる。
3. 保存/読込で layerPixels 維持。パーツ矩形リサイズ時は再完全化を促す（自動では走らせない）。
4. 検証: 重なりのある2パーツで完全化→片方を移動しても背後に穴が出ないこと（数値: 合成結果に透明セルが増えない）。ダミーCLIで補完リクエストの対象セルが「隠れセルのみ」であること。MOCK配管。退行なし。

### 25.9 実機フィードバック対応（取り込みUX・フレーム数・ピンポン再生）

v2.0.0 実機テストの指摘4件への対応。

1. **微調整を全候補で**: 画像取り込み時の位置合わせ（±ナッジ・ベース半透明重ね）が先頭候補にしか効かない問題を修正。任意の候補を選択すると整列パネルが開き、その候補のオフセットをいつでも調整し直せる（取り込み直後だけでなく後からも）。
2. **候補のフレーム列移動**: 各候補に「◀ / ▶」（またはフレーム番号ドロップダウン）を付け、割り当て先フレーム列を自由に変更できる。シートの分割順が意図とずれたときの救済。移動先列に採用済みがあっても候補としては共存（採用は列ごとに1つの原則のまま）。
3. **ギャラリーでフレーム数Nを直接変更**: プリセット既定（歩き=4等）に固定せず、ギャラリー上部に N の数値入力（1〜12）。増やすと空列追加、減らすと末尾列を畳む（候補が残っている列を消す場合は確認）。**シート取り込みでコマ数がNと不一致のときは「フレーム数を◯に合わせますか？」を自動提案**。確定時のタグ範囲もNに追随。
4. **ピンポン再生**: 再生モード「通常ループ / ピンポン（往復）」を (a) ギャラリーのミニプレビュー (b) 本体のプレビュー再生 に追加（往復は端フレームを重複させない: 1234323…）。(c) GIF書き出しにも「ピンポン」チェック（フレーム列を往復展開して出力）。プロジェクトJSONに再生モードを保存（タグ単位ではなくプロジェクト単位で可）。ヘルプ追記。

検証: 候補2以降のナッジが pixels に反映されること（数値）。列移動後の採用・確定の整合。N変更（増減・確認ダイアログ・シート取り込み時の自動提案）。ピンポンのフレーム順（4フレームで 1,2,3,4,3,2 の6ステップ）をミニプレビュー・本体・GIF（フレーム数=2N-2）で数値検証。退行なし（§25系一式・§16書き出し）。

### 25.10 部位単位の取り込み（差分採用の実運用対応）

実機フィードバック: 差分採用の塊分割は、ポーズ全体が違う候補 vs ベースでは**全身が1つの巨大塊に融合**し「モーション丸ごと採用」しか選べない。ユーザーの実運用は「テイスト・身長感・ポーズの全部が揃う候補は存在しない前提で、**部位ごとに拾って組み立てる**」なので、部位を直接指定できるUIにする。

1. **矩形選択で採用/解除**: 差分採用パネルに「矩形」モードを追加。ドラッグで囲んだ範囲のうち差分があるセルを採用マスクへ追加（Shift または解除モードで除去）。ブラシより高速な主力ツール。
2. **パーツチップ**: プロジェクトのリグにパーツ定義があれば、パネル上部に各パーツ名のチップ（頭/胴/右腕/左脚…）を表示。クリックで**そのパーツ矩形内の差分セルを一括トグル**。リグのパーツ矩形を「取り込み範囲のテンプレート」として再利用する（回転・キーフレームは無関係。矩形だけ使う）。チップの状態表示（全採用/一部/未採用）。
3. 既存の塊クリック・ブラシは残す（局所リテイクでは塊が有効）。パネルの説明文を「部位の取り込み」寄りに更新し、ヘルプに組み立てワークフロー（顔と胴はベースを守り、候補からは手足だけ拾う）を明記。
4. ボタン名「差分採用」→「**部位取り込み**」に改名（機能名は §25.7 のまま）。
5. 検証: 矩形採用のセル集合（範囲内かつ差分ありのみ）、解除、パーツチップのトグルと状態表示（パーツ矩形と差分の積集合）、チップはリグ未定義プロジェクトでは非表示、既存の塊/ブラシ/適用/アンドゥの退行なし。

### 25.11 部位取り込みの合成モード（前面/背面/完全置換）

実機フィードバック: 部位を拾って重ねるとき、どちらが上かを選びたい。取り込みパネルに「取り込み方」ラジオを追加する。

1. **完全置換**（既定・現行挙動）: 採用マスクのセルを候補の値で置換（候補が透明なら消える）。
2. **前面**: 採用マスクのうち**候補が非透明のセルだけ**置換。候補の透明部は現状維持（部位を上に乗せる）。
3. **背面**: 採用マスクのうち**比較先が透明かつ候補が非透明のセルだけ**置換（後ろ髪・マント等を後ろに差し込む）。
4. 合成プレビューは選択モードを即時反映。適用・アンドゥは既存機構のまま。ヘルプ1行追記。
5. 検証: 3モードそれぞれのセル集合の数値検証（重なり・透明の組み合わせを含む既知グリッド）。既存の §25.10 スイート退行なし。

## 27. 【v9】UI再編（機能追加で散らかった右パネルの整理）

実機フィードバック「色々いじって分かりづらくなった」への対応。現状の問題: (1) モーション生成タブで旧方式（一括生成）の設定群が一等地を占め、推奨の候補スタジオが下部に埋没。どの設定がどの生成に効くか不明瞭 (2) リグタブが過密で、ボタン文字が縦書きに崩れる（幅不足の折返し）(3) 再生コントロールが右パネル（ループ再生チェック）と下部バー（再生モード）に重複。

方針: **推奨ワークフロー（取り込み→候補スタジオ→部位取り込み→仕上げ→書き出し）が画面の主役**になり、旧方式・上級機能は折りたたみに退避する。機能の削除はしない。

### 27.1 モーションタブ（旧「モーション生成」）

- 最上部に候補スタジオを主役として配置: プリセット / フレーム数 / 候補数 / **「候補を生成（ギャラリー）」大ボタン** / 「画像から候補追加はギャラリー内から」の1行ヒント。タブ名は「モーション」に短縮。
- 旧方式の一括生成（動きの大きさ・向き・上下バウンス・生成結果の反映・一括モーション生成ボタン・中断）は「▶ 旧方式: 一括生成（上級）」の折りたたみへ移動（既定閉、状態を localStorage 記憶）。
- タブ先頭に1行ガイド「推奨: 候補を生成 → ギャラリーで選別・部位取り込み → 確定」。

### 27.2 パーツタブ（旧「リグ」）

- タブ名を「パーツ」に変更。構成を上から:
  1. **パーツ定義**（登録・AI自動分割・簡易分割・パーツ一覧）+ 説明1行「パーツ矩形は部位取り込み（ギャラリー）のテンプレートとしても使われます」
  2. 「▶ リグアニメ生成（機械合成・上級）」折りたたみ: プリセット/振り幅/バウンス/フレーム数/生成結果の反映/フレーム生成/中断
  3. 「▶ AI描き直し・清書（上級）」折りたたみ: 描き直し/清書ボタン・対象領域ラジオ・全フレーム適用・プロンプト補足
  4. **微調整**（ドラッグ移動・回転・矢印）は現状のまま最下部
- 折りたたみの開閉は localStorage 記憶。

### 27.3 ボタンの縦文字崩れ修正

- ボタン内テキストの縦折返しを禁止（white-space: nowrap + 最小幅、または列コンテナを grid 化して幅を確保）。全タブで「ボタン幅 > ボタン高さ」を検証条件にする（絵文字的な正方形ボタンを除く）。

### 27.4 再生コントロールの一本化

- 右パネルのプレビューから「ループ再生」チェックを廃止し「再生/停止」トグルのみ残す。ループ挙動は下部バーの再生モード（通常/ピンポン）に一本化（プレビューは常に下部の設定に従う）。滲み表示チェックは現状維持。

### 27.5 検証

- Playwright で3タブ+ギャラリーのスクショ（before/after 比較）。縦文字が無いこと（主要ボタンの clientWidth > clientHeight）。折りたたみの開閉と localStorage 復元。旧方式一括生成・リグ生成・描き直しが折りたたみ内で従来どおり動くこと（既存スイートの再実行）。再生プレビューが下部の再生モードに追随（ピンポン実測は既存テスト流用）。ヘルプアイコンの付け直し漏れがないこと。

### 25.13 モーション生成のサーバージョブ化（バックグラウンド放置で結果が消える問題）

実機バグ: モーション候補生成を非アクティブで放置→フォーカスを戻すと大半の完成候補が消えて「生成中」に戻る。原因: 候補生成は完全にクライアント駆動で、ブラウザが N×K 本の SSE を保持し、結果は JS メモリ内のみ（`session.cands[].pixels`）。バックグラウンドでブラウザがタブを throttle/freeze/discard し、discard→refocus で再読込されるとメモリごと消える。「放置して待つ」という intended UX が最も壊れやすい。

**本命: サーバー側バッチジョブ**
- `POST /api/motion-batch` : { project の必要部分（width/height/palette/baseFrame グリッド・トンマナ・lockedRects 等）, specs: [{ index, variant, preset, presetLabel, customText?, instruction?, prevFrameGrid?, nextFrameGrid? }...] } を受け、**jobId** を返す。サーバーは各 spec を既存の CLI/codex 同時実行キューに投入し、**クライアント接続と無関係に**最後まで実行。各結果を `jobs/<jobId>/<index>-<variant>.json`（rows・status・warnings・error）へ書き出す（`jobs/` は自動作成・.gitignore）。
- `GET /api/motion-batch/<jobId>` : { total, done, running, items: [{ index, variant, status, rows?, warnings?, error? }...] } を返す（完了ぶんを都度）。
- `DELETE /api/motion-batch/<jobId>` : 残りの spec をキャンセル（キュー投入前のものを破棄・実行中はベストエフォート）。
- 保持: 完了ジョブは一定時間（例 24h）または上限件数で古いものから削除。サーバー再起動時は jobs/ の未完は done 扱いにせず、クライアントが再ポーリングで欠けを個別再生成できるよう status を残す。

**クライアント**: 「候補を生成」は batch を POST → jobId を localStorage に保存 → 数秒ごとに GET でポーリングし、届いた結果でセルを埋める。**ブラウザのバックグラウンド化・再読込・別デバイス移動があってもサーバーが進めるので影響しない**。再読込時に未完 jobId があれば自動で復帰（ポーリング再開）。中断は DELETE。単発の「再生成／描き直し」は spec 1件の batch として同じ経路。

**保険: セッションの localStorage 永続化**
- 候補が1つ完了するたびに session（メタ + 完了候補の pixels を base64/RLE）を localStorage に保存。未確定 session があればロード時に「前回の生成候補を復元しますか？」。容量ガード（例 5MB 超は古い順に破棄）。確定済みはプロジェクトJSON側にあるので対象外。

**候補削除でジョブファイルも削除**: ギャラリーで候補を「削除」したら、対応するジョブ結果ファイル `jobs/<jobId>/<index>-<variant>.json` も実ファイルごと削除する（`DELETE /api/motion-batch/<jobId>/item?index=&variant=`）。これでディスクにゴミが残らず、再読込時の復帰でも削除済み候補が復活しない。localStorage 側の session からも該当候補を除去。ジョブ全体を確定 or モーダルを閉じた時点で、そのジョブディレクトリごと削除（確定済みフレームはプロジェクトJSONに移っているため）。

検証: MOCK バッチで、ポーリングを一時停止（＝バックグラウンド相当）→再開しても全結果が揃うこと。候補削除でジョブファイルが実際に消えること・再読込で復活しないこと。確定/モーダル閉でジョブディレクトリが消えること。ジョブ実行中にクライアント側 fetch を止めても jobs/ にファイルが揃うこと。再読込→未完ジョブ自動復帰。DELETE で残りが止まること。localStorage 復元。abort・部分失敗・退行（§25 系一式）。実 codex/CLI での1バッチ実測（サーバー継続実行の確認）。README に「候補生成はサーバーが実行するのでブラウザは閉じてよい」を明記。

## 将来メモ（未設計・アイデア置き場）

- **ChatGPT Apps（Apps SDK）への埋め込み**: ChatGPT のサブスクリプション内で GPT-5.5 系モデルを推論に使う案。実現には (a) 本ツールを公開 MCP サーバーとしてホスティングし OpenAI の審査を通す、(b) AI 操作をチャットターン駆動に作り替える（現行の SSE リアルタイム編集ループと相性が悪い）、(c) ローカル EXPORT_ROOT 連携（zombie リポジトリの public/sprites への直接書き出し）を放棄する、という大きなアーキテクチャ変更が必要。**当面は §23（Codex CLI バックエンド、`codex exec` 経由で ChatGPT サブスク認証のまま GPT モデルを使う）を優先**し、本案は Apps SDK のローカル実行・私的配布が緩和されたら再検討する。

## 28. キャンバスのリサイズ（編集）

プロジェクトのキャンバスは作成時サイズに固定で、アイドル姿が収まる箱に全ポーズを押し込む状態。斬り（武器が大きく振れる）・ジャンプ（上に伸びる）・ボスがはみ出して描けない。**足元アンカーを基準にキャンバスを拡大/縮小できる**ようにする。フロント主体（サーバーは width/height 上限のみ関与）。

### 28.1 UI

- ヘッダに「キャンバス」ボタンを追加（新規/画像を開く の近く）。押すと小ダイアログ:
  - 現在サイズ表示、新しい幅・高さの数値入力（**16〜128**。サーバーの編集検証が 8〜128 のため上限128を厳守。超えるとAI編集が通らなくなる旨をヘルプに）
  - **9点アンカーグリッド**（Photoshop の Canvas Size と同じ。既定=**下中央**＝足元基準。既存内容が新キャンバスのどこに載るかを選ぶ）
  - 適用 / キャンセル
- `#canvasSizeLabel`（現 "32 x 32"）はクリックでもこのダイアログを開けるようにする。

### 28.2 挙動

- 拡大: アンカーに従って透明余白を追加。縮小: アンカーに従ってクロップ。**縮小で非透明画素が切れる場合は confirm**（「一部が切り取られます。続けますか？」）。
- 新旧サイズ (W,H)→(W2,H2) とアンカーからオフセット (dx,dy)=旧内容の左上が新キャンバスに載る位置を算出（例: 下中央 → dx=floor((W2-W)/2), dy=H2-H）。
- 更新対象を**すべて**平行移動/リサイズ:
  - 全 `frames[].pixels`（新 Uint 配列へ (dx,dy) でコピー、範囲外は破棄）
  - `baseFrame`
  - `lockedRects`（x+=dx, y+=dy、範囲でクランプ）
  - `rig.parts[].patch`（x+=dx, y+=dy。pivot はパッチ内ローカルなので不変）。part.layerPixels があれば維持
  - `project.width/height` を W2/H2 に
  - currentFrame・選択などの状態は保持
- **アンドゥ対象**（pushUndo してから適用）。
- 保存/読込は width/height が JSON にあるので透過的に維持。

### 28.3 検証

- Playwright（MOCK）: 下中央アンカーで拡大→**足元の既知画素が新キャンバス最下段に残る**ことを数値検証、内容が (dx,dy) 通り移動していること。縮小クロップ＋切れ警告 confirm。角/辺アンカー各種のオフセット数値。lockedRects と rig.parts.patch 座標が追随（パーツが同じキャラ領域を覆い続ける）。Ctrl+Z で旧サイズ・旧画素に完全復元。リサイズ後に AI編集（MOCK）・GIF/PNG/ゲーム書き出しが新サイズで通る。16未満/128超は弾く。退行なし（§25系・§27・リグ）。
- バージョン繰り上げ。

## 29. ライブプロジェクト同期（Codex ⇄ GUI 往復編集）

CodexがプロジェクトJSONを直接編集し、GUIが自動でそれを反映。ユーザーがGUIで手直しした結果はファイルに書き戻してCodexが続きを読める。「Codexが差分生成 → 画面に即反映 → 人がペンで微修正 → Codexが続ける」の往復ループを滑らかにする。

### 29.1 共有ファイル

- `<EXCHANGE_DIR>/live_project.json`（既定は §25.8 の gpt-exchange 配下。Drive 同期パス上に置けばスマホ/別端末とも共有できる）。
- CodexはこのファイルをただのJSONとして読み書きする（ツール側の対応不要）。

### 29.2 サーバー

- `GET /api/live-project` → `{ exists, mtime, project }`（project は parse 済み。無ければ null）。
- `GET /api/live-project?meta=1` → `{ exists, mtime }`（軽量ポーリング用）。
- `POST /api/live-project`（body=プロジェクトJSON）→ **アトミック書き込み**（temp→rename でCodexが途中の壊れたJSONを読まないように）。`{ ok, mtime }` を返す。
- パス安全性: 許可ディレクトリ（EXCHANGE_DIR）配下に限定・トラバーサル禁止・サイズ上限20MB（既存 EXPORT_ROOT/gpt-exchange と同方針）。

### 29.3 クライアント（GUI）

- ヘッダ付近に「ライブ同期」トグル（既定OFF）。
- ON時:
  - 即座に live ファイルを読み込む（あれば projectFromPlain でロード）。
  - `?meta=1` を2〜3秒ごとにポーリング。mtime が増えていたら（＝Codexが外部から書き換えた）:
    - GUIに未保存編集が無ければ**自動リロード**（GET フル → projectFromPlain → store）。
    - 未保存編集があれば「外部で変更されました。読み込み直しますか？（未保存の編集は失われます）」を出し、読込/無視を選ばせる。
  - **エコー抑制**: GUI自身が POST した mtime を記録し、それ以下の mtime のポール結果はリロードしない（自分の書き込みでループしない）。
- **書き戻し（GUI→ファイル→Codex）**: v1は明示ボタン「ライブ保存」（現在のプロジェクトを live ファイルへ POST）。ユーザーが手直しを渡すタイミングを制御できる。debounce自動保存は将来オプション（レース回避のため v1 は明示）。
- 「最終同期 hh:mm:ss」表示。OFF時はポーリング停止＝従来どおり（ファイル選択ロード/ダウンロード保存）。

### 29.4 検証

- MOCK/Playwright: トグルON→liveファイルをロード。外部書き換え（テストが live_project.json を改変）→未編集時に自動リロードで反映（サイズ/画素変化を確認）、編集中はプロンプト（無視=ローカル維持・読込=置換）。GUI編集→「ライブ保存」でファイル内容が一致。エコー抑制（自分の書き込みでリロードしない）。トラバーサル拒否・サイズ上限。トグルOFFでポーリング停止。退行なし（通常のファイル読込/保存・§25.8 exchange 監視と併存）。
- バージョン繰り上げ。

## 30. フレーム別変換設定（サイズ・共有パレットは固定）

画像→ドット変換で、**サイズ(W×H)と共有パレットは全フレーム共通に固定**したまま、それ以外の変換つまみ（背景除去・フチ光彩・輪郭保護・彩度保護・グリッド/セル中心サンプリング・位置合わせ）を**フレームごとに調整**できるようにする。ポーズによって最適な拾い方が違うため。アニメのチラつき防止のためパレットは「色数が同じ」ではなく「中身も同一」を保証する。

### 30.1 変換スタジオの複数フレーム対応

- 入力が複数フレーム（シート分割 or 複数画像 or 既存プロジェクトの再変換）のとき、スタジオに**フレーム選択（サムネイル/前後）**を表示。1枚のときは従来どおり。
- **グローバル操作**（パネル上部・全フレームに効く）: `targetH`（サイズ）, `colors`（色数）。変更で全フレーム再変換＋パレット再抽出。
- **フレーム別操作**（アクティブフレームのみ）: `bgThreshold`, `glowWidth`, `edgeProtect`, `satProtect`, グリッド位相/セル中心などサンプリング系。フレーム切替でそのフレームの値＋ライブプレビュー表示。

### 30.2 共有パレット（重要・二段構え）

- **pass1**: 各フレームを、その**フレーム別つまみ**で RGB サンプリング（背景除去・フチ・グリッド適用済み）→ 全フレームのサンプルを結合し、**1つの共有パレット**（`colors` 色）を頻度重み付き k-means++ で抽出。
- **pass2**: 各フレームのサンプルを共有パレットへ最近色マッピング → フレーム別のインデックス化ピクセル。
- 再抽出のトリガ: `colors` 変更、またはいずれかのフレームのサンプリング系つまみ変更（サンプルが変わるため）。debounce。
- これで**全フレームのパレット配列が完全一致**することを保証。

### 30.3 UI補助・確定・互換

- 「この設定を全フレームに適用」: アクティブフレームのフレーム別つまみを全フレームへコピー。
- 確定: 全フレーム（同一 W×H・共有パレット）でプロジェクト化。`conversionParams` にグローバル＋**フレーム別つまみの配列**を保存（再変換で復元）。
- 後方互換: 単一フレーム変換は従来通り（N=1）。既存の単一 `conversionParams` はグローバル＋1フレーム扱いでロード。

### 30.4 検証

- 2フレームで **bgThreshold を別々**にしても**パレット配列が完全一致**（JSON等値）で、変換結果（インデックス）はフレームで異なることを数値確認。
- `colors` 変更で共有パレット再抽出。フレーム別つまみ変更で該当フレームのみプレビュー更新＋パレット再抽出。
- 「全フレームに適用」でフレーム別つまみが揃う。
- 確定→プロジェクトの全フレームが同一 W×H・単一パレット。保存→読込で conversionParams（フレーム別配列含む）維持。
- 単一画像変換の退行なし。§25.6 候補取り込み（既存パレットへスナップ）と併存。
- バージョン繰り上げ。

## 31. エディタ基本動作の改善（反応・ブラシサイズ・iPad/ペン対応）

ドット絵ツールとしての基本操作性が不足。反応の遅さ、ブラシサイズ変更不可などを改善し、iPad/ペンタブでも描けるようにする。

### 31.1 反応（レスポンス）

- **3方向ジェスチャーは維持**（即離す=1ドット / その場0.7s静止=スポイト / 押下後すぐ移動=線引き。pendingPen）。※当初「即時描画に変更・長押しトグル化」と書いたが、ユーザー指摘によりジェスチャーは変えず、下記の性能改善で対処する方針に訂正。
- **反応の主犯＝ストローク補間の欠如**。線引き中は移動が検知された瞬間に原点セルから描き始め、毎 pointermove で「直前セル→現在セル」を**ブレゼンハムで結んで全セルを塗る**（＝速く引いてもドットが抜けない・粗くならない）。ブラシサイズ>1 は各補間点にブラシを乗せる。消しゴムも同補間。
- **描画/カーソルの軽量化**: カーソル/ホバー表示は専用オーバーレイ（`#cursorCanvas`）で枠だけ描き、フレーム全体の再描画を毎移動で走らせない。ストローク/パン/ズームの `render()` は rAF バッチで1フレーム1回に集約（カーソル追従のもたつき解消）。

### 31.2 ブラシサイズ

- ペン/消しゴムのサイズを選べるUI（ツール欄に 1/2/3/4/8px など）。サイズ分の正方ブラシで塗り/消し。カーソルプレビューにサイズ反映。

### 31.3 iPad/ペンタブ対応（PointerEvents）

- キャンバス描画を mouse系 → **PointerEvents**（pointerdown/move/up、mouse+touch+pen 統一。pointerType で判別）。
- キャンバスに `touch-action: none`・`-webkit-touch-callout: none`（iOS長押しメニュー抑制）。
- **2本指でパン・ピンチでズーム**（1本指=描画のため）。
- 既存のパン（空所ドラッグ）・範囲選択・リグ§22.8ドラッグ・§14.5調整も pointer 統一で動くこと。

### 31.4 検証

- 押下即時に1ドット塗られる（遅延なし）を実測。高速ドラッグで隙間なし。ブラシサイズ2/3で正しいサイズの矩形が塗れる/消せる。スポイトはツール/Altで効き、長押しスポイトはトグルON時のみ。
- Playwright で pointer イベント（touch/pen 相当を dispatch）で描画できること・touch-action・2本指パン/ピンチズーム。
- 退行なし: 既存マウス操作・パン・範囲選択・リグ§22.8/§14.5・§25系。
- バージョン繰り上げ。

## 32. 選択範囲の変形（移動・反転・回転）

現状、矩形選択はAI編集の範囲指定・ロック・パーツ切り出しにしか使われず、**選択したピクセルを動かす/変形する手段が無い**。手作業でのポーズ調整・部品の移動に必須なので追加する（現在フレームのピクセル操作・パレット不変）。

### 32.1 フローティング変形

- 矩形選択がある状態で「移動」操作（選択内をドラッグ、または移動モードボタン）→ 選択領域のピクセルを**フローティングバッファに持ち上げ**、ドラッグで再配置（セルスナップ）。元の領域は透明にクリア。確定（離す/Enter/ツール切替）で現在フレームに焼き込む。Esc で取り消し（復元）。
- **Alt/⌥ ドラッグ = コピー移動**（元を残す）。
- フローティング中は反転・回転を重ねて適用でき、確定でまとめて焼く。

### 32.2 反転・回転（ボタン）

- **左右反転 / 上下反転**: 選択ピクセルをその場で反転（きれい）。
- **90° / 180° 回転**: きれい（90°は選択が正方でなくても内容を回転し、はみ出しは選択枠内でクランプ or 選択枠をリサイズ。実装しやすい方で一貫）。
- **自由角度回転（±15°刻り等・任意）**: 最近傍で回転（**ドットが粗くなる**）。実行時に「回転はドットが粗くなります。90°/反転はきれい。仕上げに部分仕上げ推奨」の警告。既定では出さず、明示操作時のみ。

### 32.3 UI・挙動

- ボタンは「選択解除 / この範囲をロック」付近に: 移動（ドラッグ主体）・⇄反転・⇅反転・⟳90°・(任意)自由回転。
- 現在フレームのみ対象。パレットindexは保存（色は変えない）。全操作アンドゥ対象。
- 既存の選択用途（AI scope=selection・ロック・パーツ登録）は非破壊で維持。

### 32.4 検証

- 移動: 選択を (dx,dy) 動かすと新位置にピクセルが出現・元領域が透明化（数値）。Alt=コピーで元が残る。反転 左右/上下 が正しい。90°回転で向き・寸法が正しい。Esc取消・Enter/離しで焼き込み・アンドゥ復元。高ズームで動作。自由角度回転の警告表示。
- 退行なし: 矩形選択の scope 指定・ロック・パーツ登録、§31 の描画/カーソル/ジェスチャー、§25系。
- バージョン繰り上げ。

## 33. コピー / 切り取り / 貼り付け

§32 のフローティングを土台に、選択範囲のコピペを追加。フレームをまたいだ流用（例: フレーム1の腕をコピー→フレーム3に貼る）に有用。

### 33.1 動作

- **コピー（Ctrl/⌘+C）**: 選択矩形のピクセル（index配列＋w,h）をセッション内クリップボードへ保持。
- **切り取り（Ctrl/⌘+X）**: コピー＋選択領域を透明にクリア（アンドゥ対象）。
- **貼り付け（Ctrl/⌘+V）**: クリップボードを **§32 のフローティング選択**として生成（同じ移動/反転/回転が効く）。初期位置は元の選択位置（無ければキャンバス中央）。ドラッグで再配置→ Enter/枠外クリックで焼き込み（**非透明のみスタンプ**・下地保持、§32と一貫）、Esc で取消。**現在フレームに貼る**（フレーム切替後に貼ればフレーム間コピーになる）。
- クリップボードはセッション内（同一プロジェクト＝同一パレット前提）。アンドゥ対象。

### 33.2 UI

- 選択操作の近くに **コピー / 切り取り / 貼り付け** ボタン（iPad等キーボード無し環境用）。ショートカット（C/X/V）も併設。貼り付けボタンはクリップボードが空なら無効。

### 33.3 検証

- コピー→貼り付けで、置いた位置にピクセルが再現（数値）。切り取りで元領域が透明化。フレーム切替後の貼り付けで別フレームに入る。貼り付けフロートに反転/回転/移動が効く。Enter確定・Esc取消・アンドゥ。ボタン経路（キーボード無し）で全操作可能。空クリップボードで貼り付け無効。
- 退行なし: §32 変形・§31 描画/ジェスチャー・矩形選択の scope/ロック/パーツ・§25系。
- バージョン繰り上げ。

## 34. エディタ拡張（色置換・グリッド表示・ミラー描画・オニオン強化）

ドット絵ツールの定番機能をまとめて追加。すべて現在のパレット/解像度を壊さず、書き出しには影響しない表示系はオーバーレイで実装。

### 34.1 色の入れ替え / 置換

- パレットの色スウォッチから「置換」: **元の色 index → 別の色**へ、該当ピクセルを一括変換。対象スコープ **現在フレーム / 全フレーム**（トグル）。アンドゥ対象。
- UI: スウォッチの右クリック or 「色置換」ボタン→元スウォッチ選択→先スウォッチ選択（or 新色）→スコープ選んで実行。パレット自体は変えず**ピクセルのindexを付け替える**（色数は不変）。

### 34.2 グリッド表示

- **グリッド表示 ON/OFF**（1セル格子）＋ **補助線**（N セル毎に太い線。N は 8/16 など選択）。**オーバーレイ層に描画**（ピクセルcanvas・書き出しには非影響）。トグルと N は localStorage 保持。

### 34.3 左右対称（ミラー）描画

- **ミラー描画トグル**: ON の間、描いたセルを**縦軸（既定=キャンバス中央X）で左右対称**にも同時に描く（ペン/消しゴム/ブラシサイズ/ストローク補間すべてに適用、1ストローク=1アンドゥ）。軸位置は既定中央、任意で調整可。横軸ミラーは任意。

### 34.4 オニオンスキン強化

- 既存オニオン（前フレーム薄表示）を拡張: **前・次フレームの両方**を表示可、**不透明度スライダー**、方向が分かる**色付け**（前=寒色/次=暖色 等のティント）。表示は「前のみ / 前+次 / なし」を選択。オーバーレイ描画（書き出し非影響）。

### 34.5 検証

- 色置換: 元index→先indexで該当ピクセルのみ変換（数値）、現在フレーム/全フレームのスコープ、アンドゥ、パレット色数不変。
- グリッド: ON/OFFで格子表示切替、補助線がNセル毎、書き出しPNGにグリッドが入らない、localStorage復元。
- ミラー: 片側に描くと反対側の対称位置にも入る（数値・ブラシサイズ/補間込み）、1アンドゥで両側取消、OFFで通常。
- オニオン: 前+次表示・不透明度・ティント・書き出し非影響。
- 退行なし: §31描画/ジェスチャー・§32変形・§33コピペ・§25系。
- バージョン繰り上げ。

## 35. レイヤー（軽量v1・フレームごと独立）

非破壊の重ね描き。手作業の合成（部品を別レイヤーに置く等）を可能にする。**フル（アニメ式共有スタック・ブレンド）ではなく軽量v1**: フレームごとに独立したレイヤー、合成は不透明スタック、波及を最小化。**着手は §34 完了後**（editor.js 衝突回避）。

### 35.1 データ構造（波及最小化が肝）

- frame = `{ layers: [{ id, name, pixels: Uint8Array(w*h), visible: bool, opacity: 0..1 }], activeLayer: int, pixels: Uint8Array(w*h) }`。
- **`frame.pixels` は「可視レイヤーの不透明合成」のキャッシュ**として常に同期保持。既存の全読み手（描画・プレビュー・GIF/PNG/シート/ゲーム書き出し・オニオン・差分・変換・候補スタジオ・リグ合成）は `frame.pixels` を読むだけで正しい合成が得られる（改修不要）。
- 合成規則: z順（配列末尾=最前面）で、可視レイヤーの**非透明(index≠0)ピクセルが上から勝つ**。**opacity は編集画面の表示（下敷き）専用**で、`frame.pixels`（＝書き出し・AI・ゲーム）には影響しない（インデックスdrawの整合を保つため）。
- 保存/読込: layers を直列化（pixels キャッシュは保存不要＝読込時に再合成）。**後方互換**: `pixels` のみの旧ファイルは「単一レイヤー」に変換してロード。

### 35.2 編集の対象

- ペン/消しゴム/塗り/選択変形(§32)/コピペ(§33)/色置換(§34.1)/AI編集は**現在フレームのアクティブレイヤー**に書き込む → 書込後に `recomposite(frame)` で `frame.pixels` を更新。
- AI編集(v1): アクティブレイヤーのグリッドを入出力とする（＝AIはアクティブレイヤーのみ見る）。ヘルプに明記。
- キャンバスリサイズ(§28): 全フレームの**全レイヤー**の pixels をリサイズ（＋合成再計算）。
- アンドゥ: レイヤー内容＆レイヤー構成操作を捕捉。

### 35.3 UI（レイヤーパネル）

- レイヤー一覧（上=最前面）: アクティブ選択・**追加/削除/複製/上下並替/名前変更/表示トグル/不透明度スライダー（表示専用）**。任意で「下に結合」。
- 単一レイヤー時は従来と同じ見た目・挙動（パネルは畳んでおける）。

### 35.4 検証

- 旧ファイル（pixelsのみ）→単一レイヤーでロード。保存→読込で layers 往復。合成＝上の可視非透明が勝つ（数値）。
- ペンがアクティブレイヤーのみに書く・他レイヤー不変・合成更新。表示/並替で合成が変わる。opacity は frame.pixels（書き出し）に不影響（数値）。
- 追加/削除/複製/並替/名前/結合、アンドゥ、キャンバスリサイズが全レイヤー対応。
- 書き出し/GIF/プレビュー/変換/候補/リグ が合成で正しく動く（単一レイヤーで退行なし）。
- AI編集がアクティブレイヤー対象。
- 退行なし: §25/§28/§31/§32/§33/§34。
- バージョン繰り上げ。

## 36. 「GPTへ送る」ワンクリック（描いた絵 → gpt-exchange → Codex → 戻り）

API不要の draw→GPT 最短経路の最後の1ピース。現在フレームを直接 out/ へ出す入口を作る（既存の「GPT依頼キット」は候補スタジオ内・ベースフレーム限定なので、エディタ最上位からも送れるようにする）。着手は §35 の後。

### 36.1 動作

- エディタのヘッダ/ツール欄に「**GPTへ送る**」ボタン。押すと:
  - 現在フレームを**合成**（§35対応）→ 拡大PNG（×8等）にして、サーバー経由で `<EXCHANGE_DIR>/out/` へ保存（例 `frame.png`）。
  - §25.6 のキットプロンプト生成を再利用した `prompt.txt` も out/ へ（プリセット/コマ数を選ぶ簡易ダイアログ、または既定＋あとで編集）。プロンプトはクリップボードにもコピー。
  - 保存先フルパスをトースト表示。
- 既存の `POST /api/exchange-kit` を「現在フレーム（合成）も受け付ける」よう拡張して再利用（新エンドポイントを増やさない）。

### 36.2 経路の完成（既存部品と接続）【実測反映で更新】

- 実測結果: Codex の image_gen は参照忠実度が低く不採用（30〜35点）。**正**は (a) ChatGPT Work（ローカルの out/ 読み・in/ 書きが実測で成功）(b) ChatGPTアプリ手動ドラッグ、の2経路。
- キットに **work_instruction.txt** を追加生成: 「gpt-exchange/out/ の reference.png と prompt.txt を読んで生成し、gpt-exchange/in/ に <preset>_1.png〜 の名前で保存」という ChatGPT Work 貼り付け用の1行。ダイアログ（プリセット/コマ数/補足・前回値記憶）から文面を組み立てる。
- 書き出し完了時に §38 の open-folder で out/ を自動オープン。
- ④ツール: `in/` 自動取り込み（§25.8・実装済み）で候補スタジオに反映。

### 36.3 検証

- 「GPTへ送る」でサーバーが out/ に PNG＋prompt.txt を書く（ファイル存在・合成PNGの内容・プロンプト要素）。クリップボードコピー。パス安全性（EXCHANGE_DIR配下）。
- in/ 自動取り込みとの往復（§25.8）が引き続き動く。
- 退行なし: 既存の候補スタジオ内キット書き出し。
- バージョン繰り上げ。

## 37. フローティングパレット＋使用色の自動パレット

手描き作業の実用性向上。プロジェクトパレットは64〜128色になり得るが、実際に使う色は少数。**使われている色だけの自動パレット**を、**キャンバス上の好きな位置に置ける小窓**で提供する。

### 37.1 フローティングパレット窓

- トグル（ツール欄 or ショートカット P）で表示/非表示。**タイトルバーをドラッグでキャンバス上を自由に移動**（画面内クランプ）。位置・表示状態・タブ選択は localStorage 保持。
- スウォッチクリックで描画色に（既存パレット選択と同期。現在色をハイライト）。右上に「×」で閉じる。iPad でも操作できるサイズ。

### 37.2 内容（タブ or セクション3つ）

1. **使用中**: 現在フレームで実際に使われている色を**出現数順**で自動集計（編集のたび更新・軽量に集計は rAF/デバウンス）。トグルで「全フレーム集計」へ切替。
2. **最近**: 最近使った色（選択/スポイトで拾った順・最大16・重複除去）。
3. **全色**: プロジェクトパレット全部（既存の左パネルと同じ内容のコンパクト版）。

### 37.3 検証

- 窓のドラッグ移動・クランプ・localStorage 復元。スウォッチ選択が描画色に反映（左パネルと同期）。
- 使用中タブ: 既知フレームで使用色と順序（出現数順）が正しい・描くと更新される・全フレーム切替。最近タブ: 拾った順・上限・重複除去。
- キャンバス操作（描画/パン/選択）と窓ドラッグが競合しない。iPad（pointer）で操作可能。
- 退行なし: 既存パレットUI・§31〜§34。
- バージョン繰り上げ。

## 38. サーバー保存＋「保存したフォルダを自動で開く」

ブラウザのダウンロード保存では保存先フォルダを開けない（ブラウザ制約）。ローカルサーバー経由の保存に「フォルダを開く」を付け、主要な書き出しをサーバー保存対応にする。

### 38.1 サーバー

- `POST /api/open-folder` … `{ target: "exchange"|"export"|"saves" }` を受け、**ホワイトリストのディレクトリのみ**を OS のファイラーで開く（win32: `explorer.exe <dir>` / darwin: `open` / linux: `xdg-open`。spawn・非同期・失敗は警告のみ）。任意パスは受け付けない（トラバーサル不可）。
- `POST /api/save-file` … `{ target: "saves", name, dataBase64 }` を `<プロジェクトルート>/saves/`（自動作成・.gitignore）へアトミック保存。name はサニタイズ（英数・-_・拡張子 json/png/gif のみ）。サイズ上限 50MB。応答に保存先フルパス。

### 38.2 クライアント

- 保存(JSON)・PNG書き出し・GIF書き出しに**「フォルダへ保存」動作を追加**（既存のダウンロード保存も残す。ドロップダウン or 隣に小ボタン）。サーバー保存後、トーストに保存先パス＋**自動でフォルダを開く**。
- ゲーム書き出し（§16, EXPORT_ROOT）と GPT依頼キット/gpt-exchange 書き出し（§25.8/§36）の完了時にも「フォルダを開く」ボタン（または自動オープン設定）。
- 「保存後にフォルダを自動で開く」トグル（既定ON・localStorage）。

### 38.3 検証

- save-file: saves/ に保存・パス応答・name サニタイズ（../ 拒否・不正拡張子拒否）・上限。open-folder: ホワイトリスト外 400。（クラウド検証では xdg-open が無くても「spawn が呼ばれた」ことをモックで確認）
- UI: フォルダへ保存 → トースト＋open-folder 呼び出し。トグルOFFで開かない。既存ダウンロード保存の退行なし。
- バージョン繰り上げ。

## 39. ギャラリーを生成なしで開く（画像取り込みの入口修正）

実機フィードバック「画像素材からパーツを拾って増やしたいが1枚しか読み込めない」。原因: 複数画像の取り込み口（§25.6 画像から候補追加）はギャラリー内にあるが、**ギャラリーを開く唯一の手段が「候補を生成」＝AI生成の実行**になっており、画像だけ取り込みたいユーザーが入口に辿り着けない。

1. モーションタブに **「ギャラリーを開く」** ボタンを追加: AI生成を一切走らせず、**空のセッション**（プリセット/コマ数は現在の入力値・候補ゼロ）でモーダルを開く。画像から候補追加・依頼キット・採用/部位取り込み/確定など全機能が使える。既にセッションがあれば（確定済み含む）それを再表示（生成済み候補や採用状態を保持）。
2. **受信箱バッジ（「受信箱にN枚」）クリックでもギャラリーを開く**（開けば §25.8 の自動取り込みが即走る）。
3. ボタン配置: 「候補を生成（ギャラリー）」の隣。ヘルプに「画像だけ取り込みたいときはこちら」を明記。

4. **取り込みプレビュー（拾わない選択）**: 「画像から候補追加」でシート分割・変換した直後、**確定前にコマ一覧（サムネイル＋チェックボックス、既定=全チェック）**を表示し、チェックを外したコマは取り込まない。「取り込む(N)」/「キャンセル」。単一コマ画像でも同UI（1枚だけ表示）。gpt-exchange の自動取り込みは従来どおり全取り込み（自動性を優先。不要分は候補の削除で対応）とし、ヘルプに明記。

検証: 生成なしで開ける（/api/edit が呼ばれない）・空ギャラリーで画像から候補追加→**プレビューで2コマ外して残りだけ取り込まれる（数値）**→採用→確定が通る・キャンセルで0件・既存セッションの再表示（候補保持）・バッジクリックで開いて自動取り込み（プレビューなし全取り込み）・「候補を生成」経路の退行なし。バージョン繰り上げ。

## 40. 元画像の無劣化保存（再変換の劣化修正）

実機フィードバック: 「再変換」で開く元画像が劣化している。原因: §18.2 で sourceImage を**最大512pxに縮小した dataURL**として保存しているため、高解像度素材（1300〜2300px級）では再変換の入力自体が劣化コピーになる。

1. **原本保持**: 変換確定時の `sourceImage` は、スタジオに渡された**元の dataURL をそのまま**保存する（縮小・canvas再エンコードなし）。上限ガードのみ: dataURL が 12MB を超える場合だけ長辺2048pxへ縮小し、その旨をトースト。
2. 再変換（openStudio(p.sourceImage)）はその原本から従来どおり動く（変換ロジック不変）。
3. 後方互換: 旧プロジェクトの縮小済み sourceImage はそのまま動く（復元は不可能。ヘルプに「昔のプロジェクトは一度『画像を開く』で元ファイルから読み直すと以降無劣化」と明記）。
4. 検証: 高解像度PNGで確定→保存JSON内の sourceImage が入力 dataURL と同一（バイト一致）→再変換で元解像度のまま表示（studioSrcCanvas のサイズ）。12MB超のみ縮小+トースト。保存/読込往復。旧形式ロード退行なし。バージョン繰り上げ。

## 41. ギャラリー候補の変換調整（取り込み後のつまみ）

実機フィードバック: ギャラリーの画像候補は既定値で自動変換されるだけで、「画像を開く」時の変換スタジオのような調整メニューが無い。候補ごとに同系のつまみを提供する。

1. **原本の保持**: 画像取り込み時、各候補に**元画像の該当コマ領域（原寸クロップの dataURL）**を `srcRegion` として保持（§40 と同方針・再調整の入力源）。
2. **「調整」ボタン**: 画像由来の候補カードに「調整」を追加。押すとパネル（モーダル内サブパネル）が開き、**§30 のフレーム別つまみと同じ項目**（背景除去閾値・フチ光彩・輪郭保護・彩度保護・グリッド/セル中心系）＋ライブプレビュー。変更のたび srcRegion から再変換 → **プロジェクトパレットへスナップ → 足元/重心整列**（既存 §25.6 パイプライン再利用）。ナッジ済みオフセットは維持。
3. **取り込みプレビュー（§39-4）にも同つまみへの入口**: プレビュー時点で「この設定で変換」を変えられる簡易つまみ（全コマ共通でよい）を置き、取り込み後は候補ごとの「調整」で個別詰め、の二段構え。
4. パレットは常にプロジェクトパレット固定（色数・中身とも不変。§30 の共有パレット原則）。
5. 検証: srcRegion 保持（取り込み後も原寸）・調整パネルで bgThreshold 変更→候補ピクセルが変わる（数値）・パレットはプロジェクトと同一のまま・整列/ナッジ維持・採用/部位取り込み/確定と共存・グリッド生成候補（画像由来でない）には調整ボタン非表示・§25.6/§39 退行なし。バージョン繰り上げ。

## 42. ギャラリー再設計: プール＋選択順＝フレーム順

実機フィードバック。現行の「フレーム列×候補」モデルは使いづらい。**プール（全画像を1つの場所に並べる）＋クリック選択の順番がそのままフレーム順**というモデルに置き換える。

### 42.1 モデル

- ギャラリーは列分けを廃止し、取り込んだ候補すべてを**1つのプール**としてグリッド表示（選択済み＝番号バッジつき、未選択＝バッジなし。従来の「採用」概念は「選択（番号付与）」に統合）。
- **選択**: 候補クリックで選択列の末尾に追加（次の番号）。もう一度クリックで解除し、後続の番号は**自動で繰り上げ**。番号バッジは大きく明瞭に。
- **取り込み時の自動選択**: 選択が0件の状態でまとめて取り込んだコマは**取り込み順に自動選択（1..M）**。既に選択がある場合は未選択で追加。
- **フレーム数N は選択数で自動決定**（固定Nの入力は生成用パラメータとしてのみ残す）。ミニプレビューは選択順で再生。**確定**は選択順にフレーム化してタイムラインへ（タグ範囲=選択数）。
- 廃止/変更: 列移動◀▶は廃止。「反転で補完」→「**選択した候補の反転コピーをプールに追加**」（未選択で追加）。グリッド生成（候補を生成）の結果もプールに未選択で入る（phaseHint は生成パラメータとしては維持）。
- **部位取り込み**: 比較先を「ベースフレーム（既定）/ 番号付きの任意の1枚（ドロップダウン）」から選ぶ。適用で生成される合成候補はプールに追加（元候補の選択状態は維持）。
- §41 の「調整」・§39 の取り込みプレビュー・受信箱自動取り込み（未選択で追加）はプール上でそのまま動く。

### 42.2 検証

- 5枚取り込み→自動で1..5・プレビューが順番どおり（色サンプリング）。追加5枚→未選択→クリックで6..・解除で繰り上げ（番号の数値検証）。確定→タイムラインに選択順どおり（画素検証）・N=選択数。反転コピー追加。部位取り込みの比較先切替と合成候補のプール追加。§39プレビュー除外/§41調整/受信箱との共存。旧「候補を生成」経路がプールに入る。既存スイート（test36/39等）は本節に合わせて更新。
- ヘルプ更新（プールモデルの説明）。バージョン繰り上げ。

## 43. 取り込み時の自動調整（フレーム別オートチューニング）

実機要望: 画像取り込み時、全フレームについて「元の絵に一番近い見栄え」になる変換設定を最初から自動で入れてほしい。§41 のつまみ探索を機械化する。

### 43.1 スコア関数（元絵との近さ）

- 候補パラメータで変換（§25.6 パイプライン: 背景除去→変換→プロジェクトパレットスナップ）→ 結果を最近傍で srcRegion サイズへ拡大し、元画像クロップと比較:
  1. **色近似**: 非背景領域の平均色距離（低いほど良い）
  2. **輪郭一致**: 簡易エッジ（隣接差分）の一致率
  3. **欠け/残りペナルティ**: キャラ画素数が元より大きく減る（背景除去しすぎ）／背景側に孤立画素が残る（除去不足）を減点
- 重みは固定でよい（実装時に実素材=gunman 等でチューニングし、根拠をコメントに）。

### 43.2 探索

- パラメータ空間: bgThreshold（粗く5〜6点）× glowWidth（0..2）を全探索 → 上位に対し edgeProtect / satProtect を粗く（2〜3点ずつ）詰める二段階。**1フレームあたり上限 ~40 変換**・非同期・プールに進捗表示（「自動調整中… 3/5コマ」）。
- キャッシュ: 同一 srcRegion+パラメータの再計算を避ける。

### 43.3 UI

- **取り込みプレビュー（§39-4）に「自動調整」トグル（既定ON）**: ONなら取り込み確定時に各コマを自動チューニングしてから候補化（プレビューのサムネイルにも反映）。OFFなら従来（既定値 or 簡易つまみ値）。
- §41 調整パネルに **「自動」ボタン**: そのコマだけ再探索して最良値をつまみにセット（以降手で微調整可）。
- 適用された各値は cand.convParams に入り、調整パネルを開くと見える（何が選ばれたか分かる）。

### 43.4 検証

- 既知素材（純色シート＋ノイズ背景を人工合成）で、自動調整が「正解に近い bgThreshold」を選ぶこと（スコア関数の単調性を数値検証）。gunman 実素材で自動 vs 既定値のスコア改善を数値レポート。上限回数・進捗表示・キャンセル。トグルOFFで従来動作。§41 パネルとの整合（自動→手動微調整）。§42 プール・§39 プレビューとの共存。既存スイート退行なし。バージョン繰り上げ。

## 44. ギャラリー完成形: スタジオ流用の調整＋既存フレームのプール表示

実機フィードバック2件。(1) §41 の簡易つまみでは項目が全然足りない — 「画像を開く」と同じ変換スタジオUIをそのまま候補調整に流用する。(2) §42 の初期要望「今採用してるフレームも全部表示して並べてほしい」が未実装 — ギャラリーを開いても既存フレームがプールに出ず「選択 0件」になる。

### 44.1 調整 = 変換スタジオの候補モード

- §41 の `#mcAdjustPanel`（簡易つまみ）を廃止し、候補の「調整」で**本物の変換スタジオ（public/studio.js）を候補モードで開く**: 入力 = cand.srcRegion（原寸）、初期つまみ = cand.convParams。
- 候補モードの差分: 解像度（高さ）= プロジェクトサイズに固定（変更UI無効）・色数/パレット = プロジェクトパレットへスナップ（スタジオ内はプレビュー用に自前変換してよいが、確定時に必ずスナップ）。それ以外の全機能（背景除去・フチ・セルサイズ±・位相・グリッド再推定・輪郭/彩度保護・候補3×3・同期ズーム）は通常と同じ。
- 確定: プロジェクト化ではなく**候補へ反映**（変換→スナップ→snapAndAlign 再整列・ナッジ差分維持・convParams 保存）してギャラリーへ戻る。キャンセルで無変更。
- §43 の「自動」ボタンは候補モードのスタジオ内に置く（探索結果をスタジオつまみへセット）。取り込みプレビューの自動調整トグルは §43 のまま。

### 44.2 既存フレームのプール表示と確定の置き換え動作

- **ギャラリーを開いたとき、現在のタグ（「全体」なら全フレーム）のフレームをプールに source:"frame" として並べ、フレーム順に選択済み（1..N）で初期化**する。新規取り込みは従来どおり（選択が既にあるので未選択追加）。
- **確定 = タグ範囲の置き換え**: プールがフレーム由来で初期化されたセッションでは、確定時に選択順の内容で**そのタグのフレーム範囲を置き換える**（数が変われば splice し、後続タグの範囲・rig.generatedAt 等の index を補正。基準フレーム frame 0 を含むタグでは frame 0 の置き換えに confirm）。フレーム由来アイテムが選択から外れていれば、そのフレームは削除される（確定前に「N フレーム削除を含む」確認）。
- 新規（フレームなし・従来の生成/取り込みから始めた）セッションは従来どおり末尾追加+新タグ。
- source:"frame" アイテム: 調整ボタンは非表示（元画像が無いため。再変換はプロジェクトの「再変換」で）。部位取り込み・反転コピー・削除（=選択解除と同義ではなく、プールから除外）は可能。
- ミニプレビュー・番号バッジ・繰り上げは §42 のまま。

### 44.3 検証

- 3フレーム+タグのプロジェクトでギャラリーを開く→プールに3件が選択1..3・支配色一致（数値）。新規取り込みが未選択追加。並べ替え（選択し直し）→確定でタグ範囲の画素が新順序に置き換わる（数値）・フレーム数増減時の後続タグ index 補正・削除confirm。新規セッションの従来動作維持。
- 候補モードスタジオ: srcRegion 原寸で開く・解像度固定・確定で候補ピクセル更新（パレットはプロジェクトと同一・数値）・ナッジ維持・convParams 往復・キャンセル無変更・candidate 3×3 が動く。通常スタジオ（画像を開く/再変換）の退行なし（test30系）。
- §43 自動ボタンのスタジオ内動作。既存スイート（test36/39/41/42）を本節仕様に更新して全PASS。バージョン繰り上げ。

## 45. 背景色の変更（透明部分・キャンバス周りの表示）

実機要望: 背景が固定で見づらい。透明部分とキャンバス背景の表示色を切り替え可能にする（表示専用・書き出し非影響）。

1. **設定UI**: 左パネル（ズーム付近）に「背景」セレクタ+カラーピッカー: 市松（明/暗）/ 単色（プリセット: ダーク・ライトグレー・白・黒・緑 #00B140・マゼンタ + 任意色ピッカー）。localStorage 保持。
2. **適用範囲**: メインキャンバスの透明部分、右パネル再生プレビュー、モーションギャラリーのサムネ/ミニプレビュー、タイムラインサムネ。キャンバス外周（#canvasWrap の余白）も同系で僅かに暗く/明るく差を付けて境界が分かるように。
3. **書き出し非影響**: PNG/GIF/シート/ゲーム書き出しは従来どおり透明のまま（数値検証）。
4. 検証: 各プリセット/任意色で透明セル描画色が変わる（画素）・市松の2色パターン・localStorage 復元・書き出しPNGのアルファ不変・§31 カーソル/グリッドオーバーレイと共存・退行なし。バージョン繰り上げ。

## 46. プロジェクト差し替え時のギャラリーセッション破棄（バグ修正）

実機バグ: 画像を読み込んで新しい作業を始めると、ギャラリーに前プロジェクトのプール/候補が残って壊れる。原因: モーションギャラリーの session がプロジェクト差し替え（新規/画像を開く（変換確定）/読込JSON/ライブ同期ロード）をまたいで生存し、旧プロジェクトのサイズ・パレット前提の候補が新プロジェクトに適用されうる。

1. **プロジェクト世代（epoch）**: store にプロジェクト差し替えで増える世代カウンタを導入（新規・変換確定によるプロジェクト化・JSON読込・ライブ同期の外部ロードで increment。フレーム編集では増えない）。
2. **ギャラリー**: session に生成時 epoch を記録し、開く/操作時に不一致なら**セッションを黙って破棄して作り直す**（開いていたモーダルは閉じる or 空で開き直し。トースト「プロジェクトが変わったため候補をリセットしました」）。確定済みバー・部位取り込みパネル等の残留UIもクリア。受信箱バッジは維持（in/ のファイルはプロジェクト非依存のため。取り込みは新プロジェクトのパレットでスナップされる）。
3. 同様に旧プロジェクト参照が残りうる箇所を点検: 部位取り込みの merge 状態・§41/§44 候補スタジオが開いたまま・送るダイアログ。epoch 不一致時は安全側（閉じる/破棄）。
4. 検証: プロジェクトA でプール作成（選択あり）→ 画像を開いて別プロジェクトB に差し替え → ギャラリーを開くと**Bのフレームでシードされた新セッション**（Aの候補ゼロ・数値）・トースト表示。JSON読込・新規でも同様。フレーム編集では session 維持。merge/スタジオ開きっぱなしからの差し替えで例外なし（コンソールエラー0）。既存スイート退行なし。バージョン繰り上げ。

## 47. フレーム選択とギャラリー/調整の連動

実機要望: タイムラインのフレーム選択とギャラリー・調整が繋がっていない。両方向を軽く繋ぐ。

1. **タイムライン→ギャラリー同期**: ギャラリーモーダルが開いている間、タイムラインでフレーム選択（store.state.currentFrame の変化）が起きたら、プール内の対応する source:"frame" アイテムを**ハイライト（一時的な強調枠）＋scrollIntoView**。対応アイテムが無い場合は何もしない。
2. **フレームから直接調整**: 「再変換」でスタジオを開くとき（sourceImage+frameParams のあるプロジェクト）、**現在選択中のフレームに対応するコマのタブを初期アクティブ**にする（§30 のフレーム選択バー）。フレーム→シートコマの対応は conversionParams.split の boxes 順（取り込み順）で対応付け、対応が取れない場合は従来どおり先頭。
3. 検証: ギャラリー表示中にタイムラインクリック→該当カードに強調枠+可視位置へスクロール（DOM/数値）・対応なしで無反応。再変換→選択フレームのタブがアクティブ（§30 UI）・対応不能時は先頭・単一フレームプロジェクトの退行なし。既存スイート退行なし。バージョン繰り上げ。

## 48. Web版（GitHub Pages・静的モード）

実機要望: ツールを外から使いたい。AI機能は不要で、手描き編集・変換・書き出しができれば良い。フロントエンドはブラウザ内で完結する部分が大きいので、public/ をそのまま GitHub Pages に公開し、サーバー前提の機能だけを無効化した「静的モード」を導入する。

### 48.1 静的モードの検知

- 起動時の `GET /api/config` が失敗（fetch reject または非2xx）したら `staticMode = true`。判定はアプリ起動フローの1箇所で行い、各モジュールから参照できるようにする（グローバル or store）。ビルドフラグは持たない（同じファイル一式がローカルでもPagesでも動くこと）。
- Pages はサブパス配信（`https://<user>.github.io/AI_MEGLIO/`）なので、**index.html と全モジュールの資産参照・fetch が相対パスで壊れないこと**を点検する（`/api/...` は絶対で良い＝404になって検知に使う。CSS/JS/画像の絶対参照 `/...` があれば相対に直す）。
- フッターのバージョン表示は config 由来のため、静的モードでは `vX.Y.Z (web)` にフォールバック（バージョン文字列は起動時に読み込む静的な version.json を public/ に置き、push 前のバージョン繰り上げで package.json と一緒に更新する。取得失敗時は "web" のみ）。

### 48.2 静的モードで隠す・無効化するUI

サーバーが必要な機能はボタンごと非表示（`hidden` + 既存の `[hidden]{display:none}` 保険に倣う）:

- ヘッダー: GPTへ送る・ライブ同期/ライブ保存・📁サーバー保存×3・「保存後にフォルダを開く」・プロファイル行
- AI編集パネル全体（`<section>` ごと。トンマナ・修正/モーション/パーツの3タブすべて＝AI実行・整える・配色・輪郭・候補生成・AI分割・リグ清書を含む）
  - ただし「ギャラリーを開く」（AI不要の画像取り込み）は残したいので、モーションタブごと消える代わりに、**ギャラリーを開くボタンをヘッダー（画像読込の隣）に静的モード時だけ表示**する
- モーション候補スタジオ内: GPT用依頼キット書き出し・候補生成の進捗UI（画像から候補追加・反転で補完・部位取り込み・確定は動くので残す）。受信箱ポーリング（/api/exchange-inbox）は静的モードでは開始しない
- ゲーム書き出しダイアログ: 出力先「ゲームリポジトリ」を非表示（ダウンロードのみ）。/api/profiles の取得はスキップ（「プロファイルなし」のみ）
- 履歴ログ等、サーバー往復を前提としない表示はそのまま

動作を維持するもの（すべてブラウザ内完結）: ペン等の編集一式・レイヤー・選択変形・コピペ・色置換・ミラー/グリッド/オニオン/背景色・変換スタジオ（画像読込/再変換/自動調整含む）・ギャラリーの手動画像取り込み〜確定・タグ/フレーム/再生・PNG/GIF/ゲーム書き出しのブラウザダウンロード・JSON保存/読込。

### 48.3 デプロイ

- `.github/workflows/pages.yml`: 作業ブランチへの push（+ workflow_dispatch）で `public/` を artifact 化して actions/deploy-pages で公開。actions/configure-pages は enablement 付き（初回に Pages を自動有効化）。permissions: pages:write, id-token:write。
- Node ビルドは無し（public/ をそのまま上げる）。

### 48.4 検証

- APIの無い静的ファイルサーバー（サブパス `/AI_MEGLIO/` 配信を模擬）で起動: コンソールエラー0・エラートースト0・48.2 の隠すUIがすべて hidden・ヘッダーに「ギャラリーを開く」出現。描画/レイヤー/変形/色置換が動く（画素数値）・変換スタジオで画像→プロジェクト化・ギャラリー手動取り込み→確定・PNG/GIF/JSON のダウンロード生成（バイト非空）・フッターが `vX.Y.Z (web)`。
- 通常モード（サーバーあり）の退行なし: 既存スイート（test30_ui/42/44/45/46/47）全PASS・隠したUIが通常時は全部見えている。バージョン繰り上げ。

## 49. スマホレイアウト（サイドパネルのドロワー化＋キャンバスズームUI）

実機要望: スマホでも使いたい。「メニューを両サイド自動で降りて溜まる系（=普段は隠れて、呼んだら出てくる引き出し）にすればそこそこいける」「あとはキャンバスのズームイン/アウト」。

### 49.1 モバイルモードの判定

- CSS メディアクエリ `(max-width: 820px)` を単一の閾値とし、JS 側も `matchMedia` の同じクエリで判定（リサイズ/回転で追従）。デスクトップ/iPad横のレイアウトには一切影響しないこと。

### 49.2 サイドパネルのドロワー化

- モバイルモードでは左パネル（ツール/パレット/レイヤー）と右パネル（AI編集※通常モードのみ・履歴・プレビュー・ゲームビュー）を**オフキャンバスのドロワー**にする: 普段は画面外（transform: translateX）に隠れ、左右端中央の**つまみボタン**（縦長・半透明・44px以上のタップ領域）で開閉。
- 開いている間はキャンバス側に半透明バックドロップを敷き、**バックドロップのタップ（=キャンバス外タップ）で自動で閉じる**。同時に開くのは片側のみ（片方を開いたらもう片方は閉じる）。
- ドロワー内は縦スクロール可。ヘッダーは横スクロール可（ボタンの折返しで画面を潰さない）。タイムラインは下部のまま高さ compact。
- z-index はモーダル群（ギャラリー・スタジオ・書き出し 500〜700、トースト 999）より下に置き、既存モーダルの操作を妨げない。ドロワー開閉状態はリロードで初期化（保存しない）。

### 49.3 キャンバスズームUI

- モバイルモードのみ、キャンバス右下にフローティングの **「＋ / − / ⤢全体」** ボタン（縦積み・半透明）。＋/−は既存 zoom を1段階（±2）増減、⤢は zoomAuto（全体フィット）に戻す。既存の2本指パン/ピンチ（§31）はそのまま。
- ボタンは描画の邪魔にならない位置・サイズ（44px・カーソルオーバーレイより前面、モーダルより背面）。デスクトップでは非表示。

### 49.4 検証

- Playwright viewport 390×844（+ hasTouch）: 両ドロワーが初期状態で画面外（getBoundingClientRect で数値確認）・つまみで開く・バックドロップタップで閉じる・片側排他。ズーム＋/−で store.zoom が増減・⤢で zoomAuto 復帰・ドロワー越しでもペン描画が動く（画素数値）。ギャラリー/スタジオ/書き出しモーダルがドロワーより前面で操作可能。
- デスクトップ viewport（1280×800）で従来レイアウトの退行なし（ドロワーつまみ・ズームボタン非表示・既存スイート test30_ui/42/44/45/46/47/48 全PASS）。静的モード（§48）でも同様に動く。バージョン繰り上げ。

### 49.5 モバイルのクイックアンドゥ（追補）

実機フィードバック: スマホですぐアンドゥできるUIが見当たらない（ドロワーを開かないと戻せない）。

- モバイルモードのみ、キャンバス**左下**にフローティングの **「↩ / ↪」**（元に戻す/やり直し）ボタンを追加（§49.3 のズームボタン群と左右対称・44px・半透明・縦積み）。既存の undo/redo と同じ処理を呼ぶ。
- 履歴が無いときは disabled 表示（既存の undoBtn/redoBtn の活性状態と同期）。左端のドロワーつまみ（中央）とは位置が被らないこと。デスクトップでは非表示。
- 検証: 390×844 で描画→↩で画素が戻る/↪で再適用（数値）・disabled 同期・つまみ/ズームボタンと重ならない（getBoundingClientRect）・デスクトップ非表示・test49 に追加して既存項目とともに全PASS。バージョン繰り上げ。

### 49.6 モバイルのスクロール監査（追補）

実機フィードバック: 上のヘッダーメニューや各所がスクロールできない。キャンバス用ジェスチャの touch-action / preventDefault、または overflow 指定がタッチスクロールを塞いでいる箇所を一掃する。

- **原則**: タッチでのスクロール禁止はキャンバス（描画面）だけ。それ以外のスクロールコンテナは全て指でスクロールできること。
- 監査対象と要求:
  - ヘッダー: 横スクロール可（overflow-x:auto + touch-action:pan-x。縦に潰れて折返さない）
  - 左右ドロワー内: 縦スクロール可（touch-action:pan-y）。ドロワーを開いた状態で最下部の項目（レイヤー等）まで届くこと
  - タイムライン: サムネ列の横スクロール可
  - モーダル内: ギャラリー（プールのグリッド）・変換スタジオ（つまみ列）・ゲーム書き出し・キャンバスリサイズの各内容が viewport 内に収まらないとき縦スクロール可（max-height + overflow-y:auto）
  - 全体: html/body 側で touch-action や overflow が広域に none/hidden になっていないか点検。ポインタハンドラの preventDefault はキャンバス系要素に限定されているか点検（editor.js §31）
- 検証: 390×844 + hasTouch で各コンテナに Playwright のタッチスクロール（またはwheel + scrollTop数値）を与え scrollLeft/scrollTop が実際に動く（数値）。キャンバス上のスワイプは従来どおりスクロールでなく描画/パンのまま。デスクトップ退行なし・test49 に追加して全PASS。バージョン繰り上げ。

### 49.7 モバイル実機バグ第2弾（追補）

実機フィードバック: (a) アンドゥが効かない時がある。(b) ヘッダーメニューが基本見えておらず、偶然のスクロールでたまに見える。(c) 読み込み調整画面（変換スタジオ）でピンチアウトできない。

1. **ヘッダーの作り直し（モバイル）**: ヘッダーのボタン群が折返しで複数行になり、見えない行が切れている/レイアウトが崩れていると推定。モバイルでは**1行固定（flex-wrap:nowrap・高さ固定）＋横スクロール保証（overflow-x:auto, touch-action:pan-x）**に再構成し、全ボタンがスクロールで必ず到達できることを実タッチで数値検証する。プロファイル行・タグバーも同様に点検。
2. **アンドゥ不発の根因調査と修正**: 実タッチ（CDP）で「描画→即↩」「連続ストローク→連打↩」「ドロワー開閉後の↩」等を再現し、根因（disabled 同期の遅れ/タップ取りこぼし/ストローク未確定タイミング等）を特定して修正。修正後、上記シーケンスをテスト化。
3. **変換スタジオのピンチズーム**: スタジオのプレビューキャンバスに**2本指ピンチでズーム・1本指ドラッグでパン**を追加（この画面は描画操作がないため1本指パンで安全）。既存の全体表示ボタン・ズーム挙動・グリッドオーバーレイと整合。§44 候補調整モードでも同様に動くこと。
4. 検証: 390×844+hasTouch で 1〜3 の実タッチ数値検証・デスクトップ1280退行なし・test49 追加全PASS・回帰 test30_ui/48。バージョン繰り上げ。

### 49.8 iOS Safari でヘッダーが画面外に出る問題（追補）

実機スクショ（iPhone・v2.22.2）: ホーム状態でヘッダーがステータスバーの裏に隠れ、上メニューに行く術がない。ページ全体がズーム/パンされた状態で固定されていると推定（「偶然スクロールでたまに見える」証言とも整合）。

1. **ページ自体のズーム禁止**: viewport meta を `width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no, viewport-fit=cover` に。あわせてダブルタップズーム誤爆防止に UI 全域へ `touch-action: manipulation` 系の点検（キャンバス/スタジオの自作ピンチは PointerEvents 実装のため影響しないことを確認）。
2. **実表示高さ基準のレイアウト**: アプリのルートグリッドの高さを `100vh` → `100dvh`（フォールバック付き）にし、iOS Safari の下部バーの伸縮でヘッダー/タイムラインが画面外に出ないようにする。`overscroll-behavior: none` でページのゴムスクロールも抑止。
3. **セーフエリア**: `viewport-fit=cover` に伴い、ヘッダー上端に `env(safe-area-inset-top)`、タイムライン下端に `env(safe-area-inset-bottom)` のパディングを追加（ノッチ/ホームバーとの重なり防止）。
4. 検証: 390×844 でヘッダーが常に viewport 内・visualViewport スケール固定の確認（メタ内容の検証＋タッチ操作後もヘッダー rect が画面内）・デスクトップ退行なし・test49 追加全PASS。バージョン繰り上げ。

### 49.9 モーダル画面のSafariズーム抑止（追補）

実機フィードバック: 画像取り込み（変換スタジオ）の画面でまだSafariのページズームが効いてしまう。iOS Safari は user-scalable=no を無視するため、実際の抑止は touch-action と gesture イベントの preventDefault で行う必要がある。

1. **iOS専用 gesture イベントの抑止**: document に `gesturestart`/`gesturechange`/`gesturedown` の preventDefault（passive:false）を敷き、Safariのページピンチズームをアプリ全域で止める。PointerEvents ベースの自作ピンチ（キャンバス/スタジオ）は gesture イベントと独立のため影響なし。
2. **モーダル/パネルの touch-action**: `.modal`（ギャラリー・書き出し・リサイズ）・`#studioPanel`・取り込みプレビュー等のコンテナに `touch-action: pan-x pan-y`（スクロールは許可・ピンチは不許可）を敷く。自作ピンチ対象のキャンバス（touch-action:none）はそのまま。
3. 検証: 390×844 で studioPanel/ギャラリー/書き出しの computed touch-action 確認・gesture リスナーの存在確認（機能検証はJSDOMでのgestureイベント発火）・モーダル内スクロール（§49.6）とスタジオの自作ピンチ（§49.7）の退行なし・test49 追加全PASS・回帰 test48。バージョン繰り上げ。

### 49.10 グリッド微調整の刻み幅をセルサイズに比例させる（追補）

実機フィードバック: 高解像度の拡大ドット絵（1ドット≒20px前後）を取り込むと、「ドットズレを直す」の←→↑↓や±0.25が効いていないように見える。原因: 刻み幅が元画像のピクセル絶対値（offset±1px・size±0.25px）のため、セルが大きい画像では1タップがドットの数%にしかならず視認できない。

1. **刻み幅の比例化**: 1タップの移動量を推定セルサイズ s に比例させる。offset: `max(1, round(s/4))` px（=1タップ約1/4ドット）。size: `max(0.25, s*0.02)` px。表示ラベルは従来どおり実px値（小数2桁）。
2. **互換性**: knobs/conversionParams に保存する値の単位は従来どおり実px（ステップだけ動的化）。§30 のフレーム別 offsetDX/DY・§43 自動調整・再変換の復元に影響しないこと。
3. **再推定時**: 再推定でセルサイズが変わったら以降のタップ幅も追従（nudge 時に現在の grid.s を参照するだけで良い）。
4. 検証: 大セル画像（s≈20 を合成）で←タップ→サンプリング枠が約 s/4 px 動き変換結果の画素が変わる（数値）・小セル画像（s≈3）で従来相当の1px刻み・ラベル表示・保存/復元の互換・test30_ui 退行なし。バージョン繰り上げ。

### 49.11 モバイルのクイックブラシサイズ（追補）

実機要望: ツール（ブラシ）サイズも右側に並べて常時アクセスできるようにする。

1. モバイルモードのみ、キャンバス右側・ズームボタン群（§49.3）の**上**にブラシサイズ 1/2/3/4/8 の縦積みフローティングボタンを追加（40〜44px・半透明・§49.3/49.5 と同デザイン）。
2. 左パネル内の既存 brush-size-btn と**状態同期**（どちらを押してももう一方の is-active も更新）。ズームボタン・右ドロワーつまみ・↩/↪と重ならないこと。画面高が足りない場合は縦スクロールでなく間隔を詰めて収める。
3. デスクトップでは非表示。touch-action: manipulation。
4. 検証: 390×844 でタップ→editor のブラシサイズが変わり描画幅が変わる（画素数値）・パネル側との相互同期・非重複（rect）・デスクトップ非表示・test49 追加全PASS・回帰 test48。バージョン繰り上げ。

### 49.12 解像度指定モードでオフセット/サイズ±が無効なバグ修正（追補）

実機フィードバック: 別の画像でもオフセットが無反応（±100でも変化なし）。根因: convertImage / convertSheetImage の `targetH > 0`（解像度(高さ)指定・既定）分岐では `cs = bbox高さ/targetH, gx0 = x0, gy0 = y0` と外接矩形固定で、**ox/oy（オフセット）と sizeDelta を完全に無視**している。オフセットが効くのは 1:1 モード（targetH=0）と §30 多フレーム経路のみだった。

1. **params に offsetDX/offsetDY を明示的に追加**（knobsToParams から渡す。従来の ox/oy＝grid位相+offset は 1:1 用にそのまま維持）。
2. **targetH>0 分岐で適用**: `gx0 = x0 + offsetDX, gy0 = y0 + offsetDY`、`cs = bbox高さ/targetH + sizeDelta`（cs は 0.5 以上にクランプ。幅128クランプとの順序に注意）。convertImage と convertSheetImage の両方。
3. §49.10 の刻み幅比例化はそのまま有効（targetH モードでは grid.s ではなく現在の cs 相当で刻むのが理想だが、grid.s ≒ cs のため簡易に grid.s 基準のままで良い）。
4. 検証: targetH=64 の単一画像でオフセット1タップ→変換結果の画素が変わる（数値）・サイズ±も変化・1:1 モードの従来動作維持・多フレーム経路（test30系）退行なし・conversionParams 往復互換。バージョン繰り上げ。

## 50. スマホファーストUIUX強化（Web版）

§49 で「使える」に達したモバイルを「気持ちよく描ける」に引き上げる。実装は4便に分割（§50.1+50.2 → §50.3 → §50.4+50.6 → §50.5）し、便ごとにバージョン繰り上げ。

### 50.1 自動保存＆復元

- プロジェクトを**IndexedDB へデバウンス自動保存**（編集操作後 2秒アイドルで書き込み。sourceImage 込みで数MBになるため localStorage は不可）。世代は直近1件で良い（+「復元用」と「明示保存」を区別しない）。
- 起動時に自動保存が存在し、かつ現在が空プロジェクト相当なら**「前回の続きから再開しますか？」**バナー（再開/破棄）。破棄で自動保存を消す。
- 新規プロジェクト作成時は自動保存もリセット。ライブ同期（§29）中は自動保存を停止（二重管理防止）。サーバー版/静的版共通で動作。
- 検証: 描画→リロード→再開バナー→復元で画素一致（数値）・破棄で消える・新規でリセット・数MB級プロジェクト（sourceImage付き）の保存/復元・プライベートブラウズ等 IndexedDB 不可環境で例外なく無効化。

### 50.2 PWA化

- `manifest.webmanifest`（name/short_name/display:standalone/theme_color/background_color/icons 192&512。アイコンは新規作成のシンプルなドット絵ロゴPNG）と Service Worker を追加。**サブパス配信（/AI_MEGLIO/）前提で相対パス**で書く。
- SW 戦略: 静的資産は cache-first + バージョンキー（version.json の値でキャッシュ名を切替・旧キャッシュ削除）。`/api/*` は**常にネットワークのみ**（キャッシュ禁止。サーバー版の SSE/ポーリングを壊さない）。オフライン時は静的資産のみで起動（=静的モード相当）。
- 検証: manifest の妥当性・SW 登録・2回目ロードがキャッシュから・version.json 更新で新キャッシュに切替わる・/api がキャッシュされない・サーバー版での SSE 退行なし。

### 50.3 描画ジェスチャ強化（モバイル）

- **2本指タップ＝Undo・3本指タップ＝Redo**（タップ判定: 300ms以内・移動 10px 未満。既存の2本指パン/ピンチと衝突しないこと＝移動が始まったらタップ扱いしない）。
- **スポイト拡大鏡**: 長押しスポイト作動中、指の上方に周辺ピクセルの拡大鏡（≈9×9セル・中心マーカー・吸い取り色プレビュー付き）を表示。離すと確定して消える。
- **指先オフセットモード（オプション）**: ONで描画点を接触点の上方 ≈24px（キャンバス座標換算）にずらす。カーソルオーバーレイで実描画点を常時表示。トグルは左パネル（ツール設定）+ 状態を localStorage。既定OFF。ペン/消しゴム/塗り/スポイトすべてに適用。
- 検証: 実タッチで 2本指タップ→undo・3本指→redo・パン/ピンチ誤発火なし（数値）・拡大鏡の表示/追従/色一致・オフセットONで接触点の上に描画される（画素座標数値）・デスクトップ退行なし。

### 50.4 導線短縮（モバイル）

- **フローティングツールバー**: ペン/消しゴム/塗り/選択/スポイトの5ボタンをキャンバス下端中央（タイムラインの直上）に横並びで常時表示。左パネルの tool-btn と相互同期（§49.11 のブラシサイズと同じ流儀）。
- **現在色チップ**: フローティングツールバーの右端に現在色の丸チップ。タップでフローティングパレット窓（§37）を開閉。色変更に追従。
- **タイムライン折りたたみ**: タイムラインバーに折りたたみつまみ（∨/∧）。折りたたむとタグ/サムネ/操作列が隠れて高さ最小（再生ボタンとつまみだけ残す）。状態は localStorage。モバイルのみで良い。
- 検証: ツールタップで store.tool が変わり左パネルと同期・色チップが現在色に追従しタップでパレット窓・折りたたみで #timelineBar の高さが縮みキャンバス領域が広がる（数値）・全フローティングUI（§49.3/49.5/49.11/50.4）同士の非重複・デスクトップ退行なし。

### 50.5 取り込み系のモバイル最適化

- **変換スタジオのモバイルCSS**: つまみ（range）のトラック/サムを指向けに拡大（高さ≈32px・サム≈24px）・行間拡大・ラベルとスライダーの縦積み化。ギャラリー/書き出しダイアログのボタンも 40px 以上に。
- **写真から取り込み**: 「画像を開く」「画像から候補追加」の file input はそのまま（iOS は既定でカメラ/フォトライブラリ選択が出る）ことを確認し、accept に image/* が入っているか点検。ヘッダーの「ギャラリーを開く」隣に補足は不要。
- 検証: 390×844 でスタジオの各スライダーの computed 高さ・タップ操作で値が変わる・input の accept 属性・デスクトップ退行なし。

### 50.6 キャンバスの外枠余白（モバイル）

実機フィードバック: 端のドットを打つときフローティングUIと重なって難しい。

- **フィット時の余白**: ⤢（全体表示）と初期表示のフィットズーム計算に**上下左右 ≈48px の余白**を確保（フローティングUIと被らない目安）。
- **パン可動域の拡張**: 2本指パンのクランプを緩め、**キャンバスの任意の端を画面中央付近まで動かせる**ようにする（キャンバスが完全に画面外へ消えない下限だけ残す）。端のドットをUIの無い場所に持ってきて描けるように。
- デスクトップは従来どおり。検証: フィット時にキャンバス rect が各フローティングUI rect と非交差・パンで端セルを画面中央へ移動→そこで描画できる（画素数値）・過剰パンでも完全喪失しない・デスクトップ退行なし。

## 51. マジック選択ツール（しきい値つきマジックワンド）

実機要望: しきい値を設定できるマジック選択ツール。矩形選択では拾えない形状（髪・マント等）をドット単位で選択する。§4.2 パーツ収穫（部位ごとの採用）の editor 側の土台にもなる。

### 51.1 ツールと選択モデル

- ツール「🪄 マジック選択 (W)」をツールパネルと §50.4 モバイルツールバーに追加。
- **マスク選択の導入**: 選択状態を従来の rect に加えて **mask（キャンバスサイズの Uint8Array）+ bbox** で持てるようにする。マスク選択の表示は選択色のオーバーレイ＋境界線（cursorCanvas/オーバーレイ系に描画。マーチングアントは不要、半透明塗り+輪郭で良い）。
- タップ/クリックで、その位置の**合成表示色**を基準に選択（編集対象はアクティブレイヤーだが、選択の見た目は合成で判定した方が直感的。ただし操作＝移動/コピー等は従来どおりアクティブレイヤーに適用）。

### 51.2 オプション（ツール選択時に表示）

- **しきい値**: 0〜96 のスライダー（色距離: パレット色の RGB ユークリッド距離）。0=同一色のみ。値を変えたら**直前のタップ点で即再選択**（プレビュー的に効く）。localStorage 保持。
- **連結のみ**（既定ON）: ONはタップ点から4近傍で繋がる範囲のみ。OFFは画面全体の色一致セル。
- **モード**: 新規（既定）/ 足す / 引く。モバイルでも押しやすいトグル群。透明セルのタップは（連結ONなら）透明領域選択として扱って良い。

### 51.3 既存操作との統合

- マスク選択で使えるもの: 選択解除・移動（§32 floating: マスク画素のみ持ち上げ）・⇄⇅反転/⟳90°/⟳自由（floating化後は既存挙動）・コピー/切り取り/貼り付け（§33: マスク外は透明でコピー）・AI「整える」等の選択範囲スコープは **bbox+マスク**（allowedMask に渡せる形。まずは bbox でも可、マスク対応が素直ならマスクで）。
- 範囲ロック（§34）は当面 rect のみ（マスク非対応で良い。ボタンは disable）。
- 矩形選択ツールで新規選択したらマスクは破棄（相互排他）。Esc/選択解除でクリア。

### 51.4 検証

- しきい値0でタップ→同色連結領域のみ選択（マスク数値）・しきい値を上げると隣接色まで拡大・連結OFFで全体一致・足す/引く・移動/コピー/ペースト/反転がマスク画素だけに効く（画素数値）・矩形⇄マスクの相互排他・アンドゥ整合・モバイルツールバーからの操作（実タッチ）・デスクトップ/既存スイート退行なし（test49/50系）。バージョン繰り上げ。
