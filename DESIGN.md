# AI Meglio — ドット絵アニメーション AI編集スタジオ 設計書

## 1. コンセプト

ブラウザで動くドット絵アニメーションエディタ。ユーザーはキャンバス上で「細かい部分」（矩形範囲・フレーム）を指定し、日本語で修正指示を出す。Claude API がその範囲のドットをピンポイントで書き換え、ループ再生中のプレビューに即座に反映される。手描き編集とAI編集を行き来しながら仕上げるツール。

**ドット絵がAI精密編集に向く理由**: 小さいグリッド × 限定パレットなので、画像を「パレット番号のテキストグリッド」として完全に表現できる。Claudeには理解用のPNG画像と編集用のテキストグリッドの両方を渡し、出力は構造化JSON（差分パッチ）で受け取る。再生成ではなく確定的な差分編集になる。

## 2. 技術構成

- **サーバー**: Node.js 18+ / ESM / `node:http`（Expressなし）。依存は `@anthropic-ai/sdk` のみ。
- **フロント**: ビルドステップなしの vanilla JS + Canvas API。`public/` 配下に配置。
- **CDN・外部リソース禁止**（フォント・ライブラリすべてローカル/自前実装）。

### ファイル構成

```
package.json          # type: module, dep: @anthropic-ai/sdk, scripts.start: node server.js
server.js             # 静的配信 + /api/edit (SSE) + /api/config
public/
  index.html
  style.css
  app.js              # エントリ・状態管理・Undo/Redo
  editor.js           # キャンバス描画・ツール・選択
  timeline.js         # フレーム一覧・再生プレビュー
  ai.js               # AIパネル・SSE受信・パッチ適用
  gif.js              # GIFエンコーダ（自前実装、依存なし）
README.md             # セットアップ・使い方（日本語）
DESIGN.md             # 本書
```

（ファイル分割は目安。合理的な範囲で統合・分割してよいが、1ファイル2000行を超えない）

## 3. データモデル

```js
project = {
  width: 32, height: 32,        // 8〜96。デフォルト 32×32
  fps: 8,                        // 1〜24
  palette: ["#00000000", "#1a1c2c", ...],  // 最大32色。index 0 は常に透明
  frames: [
    { pixels: Uint8Array(width*height) },  // 値 = パレットindex
    ...
  ]
}
```

- Undo/Redo はプロジェクト全体のスナップショットスタック（JSON化して保持、上限50件）。
- 保存/読込: プロジェクトをJSONでダウンロード / ファイル選択で復元。

### テキストグリッド表現（プロンプト用）

1ピクセル1文字。透明(index 0) = `.`、index 1〜31 = `1`〜`9`, `a`〜`v`（base36的割当て、大文字不使用）。1行 = グリッド1行。フレームごとに行ブロック。この表現はプロンプトとレスポンスの両方で共通に使う。

## 4. UI レイアウト

```
┌──────────┬──────────────────────────┬──────────────┐
│ ツール      │  キャンバス（ズーム表示・       │ AIパネル       │
│ ・ペン      │  グリッド線・オニオンスキン）    │ ・対象表示      │
│ ・消しゴム   │                          │ ・指示入力      │
│ ・塗りつぶし │                          │ ・実行ボタン     │
│ ・矩形選択   │                          │ ・履歴ログ      │
│ ・スポイト   │                          │ ・再生プレビュー  │
│ パレット     │                          │  (実寸+ループ)   │
├──────────┴──────────────────────────┴──────────────┤
│ タイムライン: フレームサムネイル列 [+追加][複製][削除][←→並替] fps▸ 再生/停止 │
└─────────────────────────────────────────────────────┘
```

### エディタ要件

- ズーム: セルサイズ自動（キャンバス領域にフィット）+ ホイールで調整。グリッド線はズーム時のみ。
- ツール: ペン / 消しゴム / 塗りつぶし(flood fill) / 矩形選択 / スポイト。ドラッグ描画対応。
- 矩形選択: 点線でハイライト。選択中はAIパネルに「対象: フレームN の (x,y)〜(x2,y2)」と表示。選択なし = フレーム全体が対象。
- オニオンスキン: 前フレームを半透明表示（トグル）。
- 市松模様の透明背景。
- パレット: 色クリックで選択、ダブルクリックで `<input type="color">` により変更、[+]で追加（32色まで）。
- 再生プレビュー: AIパネル下部に実寸〜2倍表示で常時ループ再生（トグル可）。**編集が適用された瞬間に反映される**こと（フレームデータ共有で自然に実現）。

### AIパネル要件

- 対象スコープ選択: 「選択範囲のみ」「現在のフレーム」「全フレーム」ラジオ。
- 指示テキストエリア（Cmd/Ctrl+Enter で実行）。
- 実行中: ストリーミング進捗（受信文字数 or スピナー）+ 中断ボタン（fetch abort）。
- 適用時: 変更されたセルを1秒程度フラッシュハイライト。
- 履歴ログ: 指示文と結果（適用セル数 / 追加フレーム数 / エラー）を時系列表示。
- 適用は自動。気に入らなければ Undo (Cmd/Ctrl+Z)。

### 指示例（プレースホルダに記載）

「腕の振りをもっと大きく」「輪郭のジャギーを滑らかに」「フレーム1と2の間に中割りを追加」「全体を夕暮れの色調に」「この範囲の剣を炎の剣にして」

## 5. サーバー API

### `GET /api/config`
`{ model, effort, mock }` を返す。

### `POST /api/edit` → SSE

リクエストJSON:

```js
{
  project: { width, height, fps, palette, framesGrid: ["....\n1122\n...", ...] },  // テキストグリッド
  scope: "selection" | "frame" | "all",
  frameIndex: 0,                       // scope=selection/frame のとき対象フレーム
  selection: { x, y, w, h } | null,    // scope=selection のとき
  instruction: "腕の振りをもっと大きく"
}
```

SSEイベント（`data: {...}\n\n`）:

- `{ type: "delta", text: "..." }` — 進捗表示用（JSONの断片。クライアントは表示のみに使い、蓄積して最後にパースはサーバー側で行うため使わない）
- `{ type: "result", patch: {...}, usage: {...} }` — パース・検証済みパッチ
- `{ type: "error", message: "..." }`

サイズ上限 5MB。バリデーション（範囲・型）必須。

### パッチ形式（Claudeの構造化出力スキーマ）

```json
{
  "type": "object",
  "properties": {
    "edits": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "frame": { "type": "integer" },
          "x": { "type": "integer" }, "y": { "type": "integer" },
          "rows": { "type": "array", "items": { "type": "string" } }
        },
        "required": ["frame", "x", "y", "rows"],
        "additionalProperties": false
      }
    },
    "newFrames": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "insertAfter": { "type": "integer" },
          "rows": { "type": "array", "items": { "type": "string" } }
        },
        "required": ["insertAfter", "rows"],
        "additionalProperties": false
      }
    },
    "paletteChanges": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "index": { "type": "integer" },
          "color": { "type": "string" }
        },
        "required": ["index", "color"],
        "additionalProperties": false
      }
    },
    "note": { "type": "string" }
  },
  "required": ["edits", "newFrames", "paletteChanges", "note"],
  "additionalProperties": false
}
```

- `edits[].rows`: パッチ矩形の行テキスト（グリッド表現と同じ文字割当て。`?` = そのセルは変更しない）。
- `newFrames[].rows`: フルサイズのフレーム。
- サーバー側で検証: frame範囲、座標がキャンバス内、rows の文字が正当、scope=selection のときは選択矩形外への edits を**切り捨てて**適用（警告をresultに含める）。ただし `newFrames` と `paletteChanges` は scope に関係なく許可。
- `note`: 何をしたかの一言（履歴ログに表示）。

## 6. Claude API 呼び出し（server.js 内・重要）

SDK は `@anthropic-ai/sdk` 最新（0.110系で動作確認済みのAPI形状）。**以下の形を正とし、古い記憶で書き換えないこと**:

```js
import Anthropic from "@anthropic-ai/sdk";
const client = new Anthropic(); // ANTHROPIC_API_KEY を環境変数から解決

const stream = client.messages.stream({
  model: MODEL,                              // 既定 "claude-opus-4-8"（env MODEL で上書き可）
  max_tokens: 64000,
  thinking: { type: "adaptive" },            // budget_tokens は使わない（400になる）
  output_config: {
    effort: EFFORT,                          // 既定 "medium"（env EFFORT）
    format: { type: "json_schema", schema: PATCH_SCHEMA },  // 構造化出力
  },
  system: [
    { type: "text", text: SYSTEM_PROMPT, cache_control: { type: "ephemeral" } },
  ],
  messages: [{ role: "user", content: userContent }],
});

stream.on("text", (delta) => /* SSE delta 転送 */);
const final = await stream.finalMessage();
// final.stop_reason === "refusal" / "max_tokens" はエラーとしてSSEで返す
// 正常時: content の text ブロックを JSON.parse → 検証 → { type:"result", patch } を送出
```

- `temperature` / `top_p` / `top_k` は**送らない**（対象モデルでは400）。
- アシスタントprefillは**使わない**（400）。
- クライアント切断時 `stream.abort()`。
- エラーは SDK の型付き例外で分岐（`Anthropic.AuthenticationError` → 「ANTHROPIC_API_KEY を設定してください」等の日本語メッセージ）。

### userContent の構成

`content` は配列で、**画像ブロック + テキストブロック**:

1. 対象フレーム（scope=all なら全フレーム、それ以外は対象フレームと前後1フレーム）を**8倍最近傍拡大したPNG**の base64 画像ブロック。サーバー側で生成する必要はない — **クライアントがcanvasで生成して data URL をリクエストに含め、サーバーはそれを** `{ type: "image", source: { type: "base64", media_type: "image/png", data } }` **に変換**する（サーバーに画像ライブラリを入れないため）。リクエストJSONに `images: [{ frame: n, dataUrl } ]` を追加。
2. テキストブロック: パレット一覧（index: hex）、全フレームのテキストグリッド、キャンバスサイズ、fps、対象スコープと選択矩形、編集指示。

### SYSTEM_PROMPT の要点（日本語で書く）

- 役割: ドット絵アニメーションの精密編集エンジン。
- グリッド文字割当ての説明（`.` = 透明、`1`-`9a-v` = パレット index、`?` = 変更しない）。
- 最小差分の原則: 指示に必要なセルだけを edits に含める。無関係なセルは `?`。
- アニメーションの一貫性: 複数フレームにまたがる編集では動きの連続性を保つ。中割り指示では前後フレームを補間。
- 画像はプレビュー参考、**正はテキストグリッド**。
- 出力はスキーマに従うJSONのみ。

## 7. MOCKモード

`MOCK=1` で起動するとAPIを呼ばず、選択範囲（なければフレーム全体の中央8×8）をパレット最後の色で塗るパッチを2秒かけて疑似ストリーミングで返す。UI・SSE・パッチ適用の動作確認用。READMEに記載。

## 8. GIFエクスポート

- `gif.js` に GIF89a エンコーダを自前実装（LZW圧縮含む。数百行で書ける定番実装）。パレット≤32色・256色以下なのでグローバルカラーテーブルで単純に実装可。透明は disposal method 2 + transparent color flag。
- 「GIF書き出し」「スプライトシートPNG書き出し」（横並び1枚、canvas.toBlobで生成）「プロジェクトJSON保存/読込」をヘッダのメニューに置く。

## 9. 見た目

- ダークテーマ（エディタ系ツールの定番）。アクセント1色。システムフォントでよいが等幅数字。
- 素朴でよいが、ボタン・パネルの余白と階層は整えること。日本語UI。

## 10. 初期データ

起動時に 32×32・8色・2フレームのサンプル（簡単なキャラが上下にバウンドする程度。ハードコードした配列でよい）を読み込み、すぐ再生・AI編集を試せる状態にする。

## 11. 検証（実装後に必ず行う）

1. `npm install && MOCK=1 npm start` でサーバー起動。
2. `curl` で `/api/config` と `/api/edit`（MOCK）のSSE応答を確認。
3. Playwright（`/opt/pw-browsers/chromium`、`PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers`。`playwright install` は実行しない）でページを開き、コンソールエラーがないこと、キャンバス・タイムライン・AIパネルが描画されること、MOCK編集を実行してパッチが適用されることをスクリーンショットで確認。
4. GIF書き出しが有効なGIFヘッダ（`GIF89a`）を持つBlobを生成すること（ページ内で検証）。

## 16. 【v4】ゲーム制作特化レイヤー

ツールを「単発のGIF作成」から「ゲームアセットの量産ライン」に特化させる。対象ゲーム（例: tanity0/zombie）への接続は §16.6 のプロファイルで吸収し、本体はエンジン非依存に作る。

### 16.1 アニメーションタグ（最重要・Aseprite方式）

1キャラ = 1プロジェクトの中に複数モーションを持てるようにする。

```js
project.tags = [ { name: "walk", start: 0, end: 3, fps: 8, loop: true }, ... ]
```

- フレーム列は従来どおり単一の配列。タグは範囲参照（start/end はフレームindex、両端含む）。
- タイムライン上部に**タグバー**を追加: タグの作成（現在の選択範囲 or 範囲入力）、リネーム、fps/loop個別設定、削除（フレームは消さない）、タグクリックでそのタグだけをループ再生・書き出し対象に。
- モーション生成・リグ生成の結果は**新しいタグとして末尾に追加**（タグ名はプリセット名から自動命名、重複時は walk_2）。
- フレーム挿入/削除時はタグ範囲を自動補正。タグに属さないフレームがあってもよい。
- 保存JSONに含める。既存プロジェクトの読込時は tags 無し→「all」タグを自動生成。

### 16.2 パレットスワップ量産（mode: "palette"）

色違いの敵バリエーション（通常ゾンビ→毒ゾンビ→炎ゾンビ等）を高速量産する専用AIモード。

- 新しい `mode: "palette"`: AIへは**パレットとベースフレーム1枚のグリッドのみ**を送り、出力は paletteChanges のみの専用スキーマ（edits/newFrames なし）。トークンが小さいのでCLIバックエンドでも数秒〜十数秒で返る。
- UI: AIパネル「修正」タブに「配色バリエーション」ボタン → 指示（例「毒々しい緑基調に」）→ 結果は**プレビュー付きの候補として表示**し、「別名で書き出しに追加」or「このプロジェクトに適用」を選ぶ。
- バリエーションはプロジェクトに `variants: [{name, palette}]` として保存し、書き出し時に全バリエーション分のシートを出力できる。

### 16.3 ゲームプロファイル

書き出し先とフォーマットの規約を JSON で定義し、UIがそれに従う。

```js
// public/profiles/ 以下 + ユーザー定義（プロジェクトJSONに埋め込み可）
{
  "name": "zombie",
  "exportDir": "assets/sprites",        // ゲームリポジトリ相対
  "format": "sheet+json",               // "sheet+json" | "strip-per-tag" | "frames"
  "scale": 1,                            // 整数拡大して書き出し
  "naming": "{char}_{variant}",         // ファイル名テンプレート
  "requiredTags": ["idle", "walk", "attack", "death"],  // キャラに必要なモーション一覧
  "mirror": "runtime"                    // "runtime"(ゲーム側で反転) | "export"(左右反転タグも書き出す)
}
```

- UI: ヘッダにプロファイル選択。**必要タグのチェックリスト**を表示（idle✓ walk✓ attack✗ death✗）— 「このキャラに足りないモーション」が一目で分かり、✗クリックでそのプリセットのモーション生成/リグ生成に直行。
- `mirror: "export"` の場合、各タグの左右反転版（walk_left 等）を自動生成して書き出しに含める（単純水平ミラー。非対称キャラ向けにタグ単位でON/OFF可）。

### 16.4 書き出し形式（ゲームが読める形）

- **sheet+json**: 全タグを1枚のシートPNG（行=タグ、列=フレーム）+ メタデータJSON。JSONは Aseprite の `--format json-array` 互換形状（frames[{filename, frame:{x,y,w,h}, duration}], meta.frameTags[{name, from, to, direction}]）。大抵のJSエンジン・Godot・Unityのインポータがこの形を読める。
- **strip-per-tag**: タグごとに横並びストリップPNG（`{char}_{tag}.png`）。
- **frames**: 連番PNG（`{char}_{tag}_{n}.png`）。
- `scale` 指定時は最近傍整数拡大で書き出し。
- **ゲームリポジトリへの直接書き出し**: `POST /api/export`（multipart不要、body に files: [{path, dataUrl}]）。サーバーは `EXPORT_ROOT` 環境変数（未設定なら機能無効）配下のみに書き込みを許可（パストラバーサル拒否）。これで `EXPORT_ROOT=C:\...\zombie BACKEND=cli npm start` と起動すれば「書き出し」ボタン一発でゲームのアセットフォルダが更新される。ブラウザダウンロード方式も従来どおり残す。

### 16.5 ゲームビュープレビュー

- 背景画像（ゲームのスクリーンショット等）を読み込み、その上で選択タグのアニメを**ゲームスケール（×1/×2/×3/×4）で移動ループ再生**する簡易プレビューパネル（歩きなら左右に往復）。実機の見え方（小ささ・視認性）の確認用。背景はプロジェクトJSONに含めない（セッション限り）。

### 16.6 zombieリポジトリ固有プロファイル（確定版）

実機調査結果: zombie は Pixi 主体で、`public/sprites/<name>.png` の**単体PNG**を `spritePath(name)` 経由でロードし、`src/pixi/pixiTextures.ts` の standalone 配列がロード対象の台帳。`atlas.png` は矩形切り出し（`ATLAS_RECTS` / `spriteAtlas.ts`）。キャラのフレームアニメは現状ゲーム側に無い（静止画1枚をエンティティごとに使用）。カテゴリ別サブフォルダあり（`stage3-enemies/`, `fx/`, `npc/` 等）。

これを踏まえた `profiles/zombie.json`:

```json
{
  "name": "zombie",
  "exportDir": "public/sprites",
  "format": "frames",
  "scale": 1,
  "naming": "{char}",
  "requiredTags": ["idle", "walk", "attack", "death"],
  "mirror": "runtime",
  "primaryExport": "first-frame-of-idle"
}
```

- `primaryExport: "first-frame-of-idle"`（プロファイル新フィールド）: 書き出し時に **idleタグの先頭フレームを `{char}.png` として単体出力**する。現行ゲームの「1エンティティ=静止PNG 1枚」にそのまま差し込める（`EXPORT_ROOT=<zombieリポジトリ> ` で `public/sprites/enemy2.png` などを直接更新）。
- 同時に全タグの連番PNG（`{char}_{tag}_{n}.png`）と sheet+json も出力しておき、ゲーム側が将来フレームアニメ対応したときにそのまま使えるようにする。
- 注記（READMEに記載）: 新規キャラ名で書き出した場合、ゲーム側では `pixiTextures.ts` の standalone 配列への追記が必要（ツールの範囲外。書き出し完了メッセージに「ゲーム側で pixiTextures.ts への登録が必要な場合があります」と表示する）。

---

## 17. 【v5】トンマナ参照（スタイルアンカー）

「世界観・デフォルメ感・ドットの打ち方」を既存の1枚の参考画像に合わせるための機能。ゲームの既存スプライト（例: zombie の player.png や atlas.png の一部）をアップロードし、以降のAI編集・生成すべてのトンマナ基準にする。

### 17.1 データモデル

```js
project.styleRef = {
  imageDataUrl: "data:image/png;base64,...",   // 参考画像（縮小済み・最大256×256に正規化して保持）
  guide: "頭身: 2頭身 / 輪郭: 黒1px常時 / ...", // スタイルガイド（テキスト。編集可能）
  enabled: true
}
```

プロジェクトJSONに含めて保存する。

### 17.2 スタイル解析（mode: "style"）

参考画像をアップロードした直後に1回だけ実行する軽量AI呼び出し。

- 入力: 参考画像（8倍拡大せず原寸のまま画像ブロックで送る。CLIバックエンド時は画像が送れないため、クライアント側で参考画像を**グリッド化できる場合**（≤96×96・≤32色に量子化可能）はテキストグリッド+パレットとして送り、できない場合はこの解析機能を「APIバックエンド時のみ有効」とし、ガイドの手書き入力を促す）
- 出力スキーマ: `{ guide: string, note: string }`。guide は以下の観点を1行ずつ: 頭身・デフォルメ度 / 輪郭線の有無・太さ・色 / シェーディング段数とディザの有無 / ハイライトの入れ方 / 彩度・明度の傾向 / 代表色 / ピクセルの打ち方の癖（角の丸め方、1pxディテールの密度）
- 結果はUIのテキストエリアに表示し、ユーザーが自由に編集できる（**ガイドテキストが正**であり、以降のリクエストにはこのテキストを使う）

### 17.3 リクエストへの組み込み

- `styleRef.enabled` のとき、すべての edit / motion / segment / cleanup / palette リクエストに:
  - テキスト: 「## トンマナ基準（厳守）」としてガイドテキストをシステム相当部に追記
  - 画像: APIバックエンドでは参考画像ブロックを1枚追加（CLIバックエンドではテキストのみ）
- システムプロンプトに追記: 「トンマナ基準が与えられた場合、ベースフレームのテイストとトンマナ基準が矛盾するときは**編集対象の絵をトンマナ基準へ寄せる方向**で修正する。ただし指示が明示的に求めない限り、一度の編集で全面的に描き直さず、指示された範囲内でのみ寄せる」
- 新規キャラをトンマナに合わせて起こす用途のため、「修正」タブの指示例に「この空のキャンバスに、トンマナ基準に合う◯◯を描いて」を追加。

### 17.4 UI

- AIパネル上部に「トンマナ」折りたたみセクション: 参考画像サムネイル / アップロードボタン / 「AIで解析」ボタン（スピナー付き）/ ガイドテキストエリア（編集可）/ 有効トグル。
- 有効中は実行ボタン付近に小さく「トンマナ適用中」バッジ。

### 17.5 検証

- MOCK の style モード（固定ガイドを返す）でアップロード→解析→ガイド表示→編集可能を Playwright で確認。
- styleRef.enabled で edit リクエストのプロンプトにガイドが含まれること（MOCKサーバー側でリクエスト内容を検査 or ダミーCLIで受信プロンプトをファイルに落として確認）。
- CLIバックエンドで画像なし・テキストのみになること。
- プロジェクトJSON保存/読込で styleRef が往復すること。
- 既存経路の退行なし。

### 16.7 検証

- タグ: 作成・範囲再生・生成結果の自動タグ化・フレーム削除時の範囲補正を Playwright で確認。
- palette モード: MOCK（固定の paletteChanges を返す）で候補プレビュー→適用/バリエーション保存の両経路。
- 書き出し: sheet+json の JSON が frameTags を含み、フレーム矩形がシート上の実位置と一致すること（ページ内で読み戻して検証）。strip-per-tag / frames / scale=2 / mirror:"export" も各1ケース。
- /api/export: EXPORT_ROOT 配下への書き込み成功、`../` を含むパスの拒否、EXPORT_ROOT 未設定時は 403 を確認。
- 必要タグチェックリストの ✓/✗ 表示と ✗→生成タブ直行。
- 既存機能（§11/§13/§14/§15 スモーク）の退行なし、コンソールエラー0件。

## 18. 【v6・最重要】高忠実度変換スタジオ（ドット絵風→本物ドット絵）

**このツールの最重要課題。品質に妥協しない。** AI生成などの「ドット絵風画像」（不均一グリッド・数百色・アンチエイリアス入り）を、雰囲気をできる限り保ったまま本物のドット絵（整グリッド+インデックスパレット）に変換する。32×32では崩れる素材のために、**解像度は最大128×128、色数は最大256色**まで対応する。

### 18.1 カラーモデル/キャンバスの拡張（土台）

- パレット上限を 32 → **256色**（index 0=透明は不変）に拡張。`Uint8Array` のフレームデータはそのまま。GIFエンコーダは既に256色対応（要確認・調整）。パレットUIはスクロール式グリッドに。
- キャンバス上限を 96 → **128** に拡張。
- **グリッド文字表現の拡張**: パレットが32色以下のプロジェクトは従来の1文字表現（後方互換）。**33色以上のときは1ピクセル=2文字の16進表現**（`..` = 透明、`01`〜`ff` = index 1〜255）に自動切替。サーバーはリクエストの `palette.length` で判定し、システムプロンプトの「グリッド表現」節を該当表現の説明に差し替える。パッチ `rows` の `?` は2文字表現では `??`。検証・変換関数（`charForIndex`/`indexForChar` 相当）を両対応に。
- 既存プロジェクトJSONは無変更で読める（32色以下はすべて従来どおり）。

### 18.2 変換スタジオ（インポートウィザードv2）

「画像を開く」を、確定前に調整できる**変換スタジオ画面**に置き換える（従来の単純インポートは32色以下の真ドット絵で自動判定成功時のショートカットとして残す）。

処理パイプライン:

1. **背景除去**: 外周フラッドフィル（近黒・近白・単色背景の自動判定 + スポイトで背景色を手動指定可）。フチ光彩（例: 紫グロー）は「背景に隣接する彩度の浮いた1〜3px帯」として除去オプション。
2. **疑似グリッド推定（品質の要）**: セルサイズ `s`（2.0〜40.0、0.25刻み）と位相オフセット `(ox, oy)` を総当たりし、**セル内分散の総和が最小**になる組を選ぶ（ダウンサンプルした作業解像度で粗探索→近傍を細探索）。これにより「描かれた時のドット」に格子を合わせる。信頼度が低い場合はその旨表示し、手動スライダー（サイズ・オフセット）で微調整できる。
3. **セル代表色**: セル内の**支配色**（最頻色。同率は中心寄り優先）を採用。平均は使わない（濁りの原因）。セルの非背景率<40%で透明。
4. **減色**: k-means（RGB距離でよいが、初期値は頻度加重の k-means++）で指定色数に量子化。**色数スライダー: 8〜256**。肌・差し色の保護のため、量子化前に「使用頻度は低いが色相が孤立している色」をシード優先する。ディザは行わない（ドット絵の文法に合わないため）。
5. **候補ギャラリー（選ぶ→追い込むの起点）**: 「候補を生成」で、パラメータ空間からバリエーションを**9候補（3×3グリッド）**一括生成して並べる（例: 解像度3段 × サンプリング/減色の組合せ3種。各候補にパラメータをラベル表示）。候補クリックで元画像との**並列比較ビュー**（同倍率・同期ズーム/パン）に拡大。「これをベースにする」でその候補のパラメータが全つまみに反映される。
6. **つまみで微調整（確定前ならいつでも候補に戻れる）**: すべてのつまみはリアルタイム再変換（粗探索結果をキャッシュして使い回し）。つまみ一覧:
   - **解像度**（高さ16〜128、または「1:1（グリッド推定サイズそのまま）」）
   - **色数**（8〜256）
   - **グリッド微調整**: セルサイズ（±0.25刻み）・オフセットX/Y（±1px刻み）— 自動推定がズレたときの追い込み
   - **代表色の取り方**: 支配色⇔平均のブレンド率（0=カリカリ〜1=なめらか）と、セル中心重視度
   - **輪郭保護**: セル内に暗い輪郭色が一定割合あれば代表色より優先する強さ（0でOFF）— 線の途切れ対策
   - **彩度保護**: 減色時に孤立色相（肌・差し色）をシード優先する強さ
   - **背景除去**: 閾値と、フチ光彩除去の帯幅（0〜3px）
7. **確定**: プロジェクト化（ベースフレーム設定、§18.3のメインパレット抽出を自動実行）。**確定後は通常のペン・消しゴム・スポイトでそのまま手動修正できる**（ここが最終仕上げの主戦場。ツール側は何も特別なことをしなくてよい—既存エディタがそのまま使える解像度・色数になっていることが§18.1の要件）。プロジェクトには選択した全パラメータを `conversionParams` として保存し、「元画像から再変換」で同じつまみ状態から再開できる。

- 元画像はプロジェクトに `sourceImage`（縮小版dataURL、最大512px）として保存し、後から変換スタジオで**再変換**できる（「元画像から再変換」メニュー）。
- **AI輪郭リファイン（任意・変換後のボタン）**: 変換で甘くなった輪郭・ハイライトを、選択範囲不要の専用清書として実行。allowedMask = 「透明と非透明の境界から2px以内」に限定した cleanup 呼び出し（既存機構の再利用）。

### 18.3 メインパレット抽出（トンマナの核）

256色のフルパレットから、**トンマナの主役となる「メイン色」（既定32色、8〜64で可変）を自動抽出**する。

- アルゴリズム: フルパレットを使用ピクセル数で重み付けし、k-means でメイン色数にクラスタリング。各フルカラー index はいずれかのメイングループに属する（`project.mainPalette = { colors: [hex...], groups: [mainIndex per full index] }`）。
- UI: パレットパネルに「メイン」段（抽出結果のストリップ）と「全色」段の2段表示。メイン色クリックで所属グループの全色をキャンバス上でハイライト。「再抽出」ボタンと色数指定。
- 用途:
  1. **AIプロンプト**: 33色以上のプロジェクトでは、システムプロンプトにフルパレット全列挙の代わりに「メインパレット（index→hex、所属グループの代表）+ 総色数」を渡し、トークンを節約しつつ色の意図を伝える（グリッド自体は16進2文字でフル情報）。
  2. **トンマナ解析（§17）**: スタイルガイド生成の入力にメインパレットを使う。
  3. **グループ単位パレットスワップ**: `mode:"palette"` を拡張し、AIは**メイングループ単位の色変更**を返す（`groupChanges: [{mainIndex, color}]`）。サーバーがグループ内の全色へ**相対シフト**（HSLで hue/sat/lum の差分を各色に適用）で展開。「赤系统をまとめて青系へ」が階調を保ったまま一手でできる。従来の個別 paletteChanges も併存。

### 18.4 AI連携の運用ガイド（高解像度・多色時）

- 128×128・256色のフレーム全体をAIで直接編集するのは実用外（トークン・時間）。UIで誘導する: 33色以上または65px以上のプロジェクトでは、修正タブに「大きな素材では矩形選択での指示が高速です」のヒントを常設表示。モーション生成タブには「この解像度ではリグの使用を推奨」のバナー。
- AI清書・選択範囲修正・グループパレットスワップは高解像度でも実用（対象が小さい/出力が小さいため）— ここが主戦場になる。
- リグ・タグ・書き出し・ゲームビューは色数/解像度に依存しないため無変更で動くことを確認する。

### 18.5 検証

- **受け入れテスト（実素材）**: `/root/.claude/uploads/6658078f-4eaf-56cc-ac9f-ba16c5ddeeba/c8541e06-IMG_6478.png`（ガンマンキャラ・1580×2304のドット絵風）を変換スタジオで高さ64・128色に変換し、(a) グリッド推定が位相まで合いエッジが立っていること、(b) 肌色・赤ベスト・白シャツが別色として残ること、(c) 旧簡易変換（ブロック平均+上位28色）との並列比較スクリーンショットで明確に改善していること。
- 2文字グリッド表現: 33色以上のプロジェクトで edit/cleanup のMOCK+ダミーCLE往復（`??` の扱い含む）、32色以下プロジェクトの従来表現が不変であること。
- メインパレット: 256色変換後に32メイン色抽出、グループハイライト、グループスワップ（MOCKで固定groupChanges→全色相対シフトの数値検証）。
- 変換スタジオUI: スライダー変更でのライブ再変換、並列比較、手動グリッド調整、再変換メニュー、AI輪郭リファイン（MOCK）。
- 退行なし: 32色以下の既存プロジェクト（§11/§13〜§17スモーク）、GIF/ゲーム書き出し（256色GIFの実デコード検証を1ケース追加）。

## 19. 【v6】部分仕上げ（mode: "refine"）

選択した範囲だけを「トンマナに合った綺麗なドットの打ち方」に清書する機能。変換直後の粗い部分の仕上げにも、**アニメ生成後にペンで大雑把に直した部分（例: 髪の動き）の清書**にも使う。ワークフローの要: 「生成 → ペンでラフに意図を描く → 整えるボタン → 完成」。

### 19.1 意味論（cleanup との違い・最重要）

- **ユーザーのラフ編集が「意図」であり、正である。** ベースフレームに引き戻してはならない。refine ではベースフレーム・アンカリングを適用せず、代わりに「**現在のフレームの選択範囲内のシルエット・形の意図を保ったまま、打ち方（輪郭の連続性、シェーディング段数、ハイライト、ジャギー）だけをトンマナと周囲に合わせて清書する**」という原則をシステムプロンプトに与える。
- 変更許可セル = 選択矩形 + 外周1px（境界のなじませ用）。許可外への edits はサーバーで破棄（cleanup と同じ機構）。paletteChanges / newFrames は破棄。
- 入力: フレーム全体のグリッド + 選択矩形 + （あれば）トンマナガイド(§17) + **前後フレームの同じ矩形の切り出し**（アニメーションの流れを保つため。存在するフレームのみ）。指示テキストは任意（省略時は「輪郭とシェーディングを整えて」相当の既定文）。

### 19.2 UI

- 矩形選択中、修正タブの実行ボタンの隣に「**整える**」ボタンを表示（選択が無いときは非活性 + ツールチップ「範囲を選択してください」）。
- 適用範囲オプション: 「このフレームのみ」/「現在のタグの全フレーム（同じ矩形）」。全フレーム時はフレームごとに並列リクエスト（既存キュー・進捗表示・部分失敗は警告）。
- 指示欄が空でも実行可能（既定文を使用）。書けばニュアンス指定になる（「髪の流れを進行方向へ」等）。
- 結果は通常どおり差分ビュー・逸脱メーター・Undoが効く。

### 19.3 実装

- サーバー: `mode:"refine"` を追加。リクエストは cleanup と同形（allowedMask をクライアントが選択矩形+1pxで生成）+ `neighborContext: [{frame, rows}]`（前後フレームの矩形切り出し、0〜2件）。スキーマは PATCH_SCHEMA の edits のみ利用（cleanup と同じ運用）。システムプロンプトに §19.1 の原則を「部分仕上げ（refineモードのとき適用）」節として追記。
- MOCK: 選択範囲内の非透明セルをそのまま返す（配管確認用）+ note「MOCK: refine」。
- CLIバックエンドでも対象が小さいため実用速度が見込める（選択矩形が主戦場）。

### 19.4 検証

- MOCK で「選択→整える」の単フレーム/タグ全フレーム両経路、許可外破棄の警告、指示欄空でも実行できること。
- ダミーCLIで受信プロンプトに (a) refine 原則、(b) 前後フレームの矩形切り出し、(c) トンマナガイド（有効時）が含まれることを実測。
- ペンでラフに描いた領域が refine 後もベースに引き戻されていないこと（MOCKエコーで機械的に確認 + 実CLIで1ケース目視）。
- 退行なし（cleanup・修正タブ・§18スモーク）。

## 20. 【v7】マルチポーズシート分割インポート

AI画像生成が出力する「同一キャラの複数ポーズが横（または格子状）に並んだ1枚画像」を、**ポーズごとに分割してアニメーションフレームとして読み込む**。実例: 紫背景に歩き3ポーズが並んだ1676px幅のシート。

### 20.1 検出

- 変換スタジオで画像を開いた時、背景除去後のアルファから**連結成分のバウンディングボックス**を抽出（8近傍・面積が最大成分の5%未満のノイズは無視）。横並び・格子いずれも対応（boxを行にクラスタリング→行内でx順）。
- 2体以上検出されたら、スタジオ上部に選択UI: 「**1枚として変換 / N体をフレームとして変換**」（既定はフレーム）。自動検出がうまくいかない素材向けに「均等分割: 横N×縦M」の手動指定も併設。

### 20.2 フレーム化の要件

- **共通キャンバス**: 全ポーズのbox最大幅・最大高さ + 既存のモーション用パディングで1つのキャンバスサイズを決める（上限128に §18 のクランプ適用）。
- **接地アライン**: 各ポーズは**下端中央合わせ**で配置（歩きポーズの足位置が揃う。オプションで中央合わせ）。
- **共通パレット**: 減色は全ポーズのピクセルをまとめて k-means（フレーム間で色がブレない）。
- **共通グリッド**: セルサイズ・位相は最大ポーズで推定し全ポーズに適用。
- 候補ギャラリー・つまみは従来どおり動作（プレビューは選択中ポーズ or 1体目。ギャラリーのサムネは1体目でよい）。
- 確定時: Nフレームのプロジェクト（frame 0 = 1体目 = ベースフレーム）+ 全フレームを含む「all」タグ。conversionParams に分割情報（mode, boxes）も保存し再変換可能に。

### 20.3 検証

- ImageMagickでガンマン単体画像から「紫背景に3体横並び」の合成シートを作り（受け入れ素材の再現）、(a) 3体が自動検出されること、(b) 「3体をフレームとして変換」で3フレーム+共通パレットのプロジェクトになること、(c) 下端中央アラインでプレビュー再生したとき足元が揃っていること、(d)「1枚として変換」経路と手動均等分割も動くこと。
- 1体のみの画像では従来どおり選択UIが出ないこと（退行なし）。

## 21. 【v7】設定ヘルプ（？アイコン）

すべての設定項目・つまみ・ボタンの意味を、その場で確認できるようにする。

### 21.1 仕様

- 各設定項目のラベル横に小さな「**?**」アイコンを付ける。**マウスオーバーで表示、クリック/タップでも表示**（タッチ環境対応。もう一度タップ or 外側タップで閉じる。同時に開くのは1つ）。
- 実装: 共通コンポーネント `public/help.js`（新規）。`data-help="キー"` を持つ要素の横に ? を自動挿入し、ポップオーバー（吹き出し、画面端で位置反転）を出す。ヘルプ文は `help.js` 内の辞書1箇所に集約（キー→テキスト）。CSSは既存テーマに合わせる。
- 文体: 1〜3文の日本語。「何が変わるか」+「どういう時に使うか」。例—「輪郭保護: セル内に暗い輪郭色が含まれるとき、代表色より輪郭色を優先します。線が途切れる・細部が消えるときに上げてください」。

### 21.2 対象（漏れなく）

変換スタジオの全つまみ（解像度/色数/グリッド微調整/代表色ブレンド/輪郭保護/彩度保護/背景除去閾値/フチ光彩除去/1:1）、分割インポートの選択肢（§20）、トンマナ欄（参考画像/解析/ガイド/適用トグル）、修正タブ（スコープ/ロック/整える/配色バリエーション）、モーション生成タブ（プリセット/フレーム数/振り幅/バウンス/向き/反映方法）、リグタブ（パーツ登録/pivot/z/親/AI自動分割/プリセット/振り幅/バウンス/フレーム数/AI清書/微調整）、タグ（fps/ループ）、書き出しパネル（形式/scale/ミラー/バリエーション/書き出し先）、プロファイル/チェックリスト、オニオンスキン/差分ビュー/逸脱メーター（タイムラインの%表示にも）。

### 21.3 検証

- Playwrightで (a) 代表的な5箇所のホバー表示とクリック/タップ表示・排他動作、(b) 画面端での位置反転、(c) `data-help` を持つ全要素に ? が付与されヘルプ辞書にキーが存在すること（ページ内で網羅チェックを実行し、辞書漏れ0を確認）、(d) コンソールエラー0。

## 12. スコープ外（今回は作らない）

- レイヤー機能、Asepriteファイル入出力、APNG、タイルマップ、共同編集
- 128×128超のキャンバス（UIの上限で96に制限）

---

## 13. 【コア要件・最優先】元画像ベースのモーション生成とテイスト保持

**このセクションは §1〜12 に追加される要件であり、優先度は最上位。** ユースケースの中心は「手持ちのドット絵キャラ画像をアップロードし、そのテイストを一切崩さずに歩き・走り・攻撃などのモーションを生成し、おかしい部分（足の左右など）を範囲指定で高速に直す」こと。

### 13.1 PNG画像インポート（必須機能）

ヘッダメニューに「画像を開く」を追加。PNG/GIF(1枚目)/WebP を受け付け:

1. canvasに描画してピクセルを読む。
2. **拡大率の自動検出**: 元画像が例えば 320×320 でも実体が 32×32 のドット絵である場合が多い。各行・各列の同色ラン長のGCDからドット1個のブロックサイズを推定し、最近傍で実寸グリッドにダウンサンプルする（検出結果はダイアログで「32×32として読み込みます」と確認、手動上書き可）。
3. **パレット抽出**: 出現色を頻度順に列挙。33色以上なら頻度上位31色+透明に量子化（各ピクセルは最近色へ。RGB距離）。抽出結果が実寸≦96×96・パレット≦32色に収まらない場合は縮小を促すメッセージ。
4. 読み込んだ画像は **frame 0 = ベースフレーム** としてセットし、`project.baseFrame`（Uint8Arrayのコピー）としても保持する。ベースフレームはタイムライン上で「基準」バッジ表示。

### 13.2 テイスト保持の5つの機構（このツールの存在理由）

1. **パレットロック（構造的保証）**: AI出力はパレットindexのグリッドのみ。色は定義上1色も変わらない。モーション生成モードでは `paletteChanges` をサーバー側で**破棄**する。
2. **ベースフレーム・アンカリング**: すべてのAIリクエストにベースフレームのグリッドを「唯一の正」として含める。システムプロンプトに追記: 「新規フレームは白紙から描くのではなく、ベースフレームのコピーから始めて、動きに必要なピクセルだけを移動・変更する。輪郭の太さ、シェーディングの段数、ドットの打ち方の癖を厳密に踏襲する」。
3. **ロック領域（変更禁止マスク）**: 矩形選択ツールに「この範囲をロック」ボタンを追加。ロック領域（複数可、半透明赤で表示、解除可）は `project.lockedRects` に保持し、リクエストに含める。サーバーはロック領域内セルへの edits / newFrames の該当セルを**ベースの値で強制上書き**して適用する（例: 顔はロックして体だけ動かす）。
4. **逸脱メーター**: 各フレームサムネイルの下に「ベースフレームとの差分率 %」を常時表示。モーションとして妥当な差分（手足周辺）か、テイスト崩れ（全面書き換え）かを一目で判別できる。40%超は警告色。
5. **差分ビュー**: トグルで「現在フレーム − ベースフレーム」の変更セルだけをハイライト表示（変更 = マゼンタ枠）。どこをAIが触ったかを即確認できる。

### 13.3 モーション生成モード（AIパネルに追加）

AIパネルを2タブにする: 「修正」（既存の §4 のパッチ編集）と「モーション生成」。

モーション生成タブ:

- **プリセット**: 歩き / 走り / 攻撃 / 待機 / ジャンプ + 自由入力欄
- **つまみ**（すべてプロンプトに文章として織り込む）:
  - フレーム数: 2〜12（既定: 歩き4、走り6、攻撃3、待機2、ジャンプ4）
  - 動きの大きさ: 小 / 中 / 大
  - 上下バウンス: あり / なし
  - 向き: そのまま / 横（右向き）/ 横（左向き）
- 実行するとベースフレームを基に `newFrames` で全フレームを生成し、既存フレーム（frame 0 のベース以外）を置き換えるか追記するかを選べる。
- 生成後は既存の「修正」タブ + 手描きツールで逐次修正するワークフロー。

**歩きモーション用のプロンプト知識をシステムプロンプトに埋め込む**（ユーザーが毎回指示しなくて済むように）: 4フレーム歩行 = コンタクト→ダウン→パッシング→アップ、左右の足は前後が入れ替わること、接地フレームで体が最も低いこと、腕は足と逆位相、など定石を明記。走り・攻撃・待機・ジャンプも同様に1〜2行の定石を書く。

### 13.4 リクエスト形式の変更

`POST /api/edit` のボディに追加:

```js
{
  mode: "patch" | "motion",
  baseFrameGrid: "....\n...",       // ベースフレームのグリッド（常に送る）
  lockedRects: [{x,y,w,h}, ...],
  motion: {                          // mode="motion" のとき
    preset: "walk" | "run" | "attack" | "idle" | "jump" | "custom",
    customText: "",
    frames: 4, magnitude: "medium", bounce: true, facing: "keep"
  },
  // 既存: project, scope, frameIndex, selection, instruction, images
}
```

画像ブロックは mode="motion" のときベースフレームのPNG（8倍拡大）を必ず含める。

### 13.5 期待値の明示（READMEに書く）

一発でプロ品質のモーションは出ない前提のツール設計であること。価値は「テイストが構造的に崩れない土台の上で、生成 → 逸脱メーターで確認 → 範囲指定して修正指示 → 手描き微調整、のループを数秒単位で回せる」ことにある、と README に明記する。

---

## 14. 【v2】2Dパーツリグモード（v1完成・検証後に着手）

動きの機構的な正しさを骨格（リグ）で保証し、絵は元素材のドットをそのまま使い、AIには継ぎ目の清書だけをさせるモード。「AIが足の左右を描き間違える」問題を原理的に排除する。**v1のコミット・検証が完了してから実装を開始すること。**

### 14.1 データモデル追加

```js
project.rig = {
  parts: [
    {
      id: "arm_r", name: "右腕",
      patch: { x, y, w, h, pixels: Uint8Array },  // ベースフレームから切り出したドット（切り出し元はロック領域同様に記録）
      pivot: { x, y },      // 回転の支点（パッチ内座標。肩・股関節など）
      z: 0,                 // 描画順（小さいほど奥）
      parent: "torso" | null
    }, ...
  ],
  // モーション = フレームごとの各パーツの変換
  keyframes: [ { partId: { dx, dy, rot } , ... }, ... ]   // rot は度。15°刻みに丸める
}
```

### 14.2 パーツ切り出し

- 矩形選択 →「パーツとして登録」。名前・pivot（キャンバスクリックで指定）・z順・親を設定するミニダイアログ。
- **AI自動分割**（補助機能）: ベースフレームのグリッド+画像を送り、「頭/胴/右腕/左腕/右脚/左脚/武器」のパッチ矩形とpivot候補をJSONで返させる（新スキーマ、`mode: "segment"`）。結果はユーザーが調整可能な下書きとして反映。
- 切り出したパーツの重なり・隙間はそのままでよい（合成時にz順で解決。隙間はAI清書対象）。

### 14.3 モーションプリセット（キーフレームテーブル）

歩き(4f)・走り(6f)・攻撃(3f)・待機(2f)・ジャンプ(4f)を **ハードコードされたキーフレームテーブル**（フレーム×パーツ→{dx,dy,rot}）として実装する。歩きの定石（左右脚の逆位相、接地フレームで胴体が最低位置、腕は脚と逆位相）をテーブル値として直接埋め込む。つまみ:

- 振り幅: テーブルの rot/dx を 0.5×/1.0×/1.5× にスケール
- バウンス: 胴体の dy 成分の有効/無効
- フレーム数: テーブルの補間（線形）で増減

### 14.4 レンダリングと清書

1. 各フレーム: パーツをz順に、親の変換を継承しつつ pivot 周りに回転（最近傍）・平行移動して合成 → 通常のフレームグリッドに書き出す（リグはあくまで生成器。生成後は普通のフレームとして v1 の全機能で編集可能）。
2. 回転で生じるジャギー・パーツ継ぎ目の隙間は、**AI清書パス**（`mode: "cleanup"`）で修正: 変更許可セルを「合成時に変化したセルの周囲2px」に限定し、パレット内・最小差分で継ぎ目を繋ぐ。サーバー側で許可セル外の edits を破棄。
3. 清書はフレームごとに並列リクエストしてよい（進捗表示）。

### 14.5 UI

- タブ「リグ」を追加: パーツ一覧（表示/名前/z/親/pivot編集・削除）、プリセット選択+つまみ、「フレーム生成」ボタン、生成後に「AI清書」ボタン。
- フレームごとの手動微調整: リグタブでフレームを選び、パーツをドラッグ移動 / ハンドルで回転 → 再合成。
- 生成結果はタイムラインに newFrames として入り、逸脱メーター・差分ビュー・修正タブがそのまま使える。

### 14.5.5 AI自動分割の軽量化とフォールバック（実機タイムアウト対策）

実測: 51×60・29色で約88秒、H96変換後の大型・多色（2文字hex表現）素材では300秒超でタイムアウト。対策:

1. **分割専用の軽量グリッド**: mode:"segment" ではフル解像度グリッドを送らない。クライアント（rig.js）がベースフレームを**高さ≤48に最近傍で間引き、色を最大8色に量子化した1文字グリッド**に変換して `segmentGrid` + `segmentScale`（縮小率）として送る。サーバーはsegment時に segmentGrid をプロンプトに使い、縮小率を明記。**返ってきた矩形・pivot はクライアントで元解像度へスケール**（丸めはbox外接方向、pivotは比率維持）。プロンプトが数分の1になり、CLIバックエンドでも実用速度になる。
2. **簡易分割（AIなし）ボタン**: リグタブに追加。非透明bboxから人型ヒューリスティックで即時に下書きを生成——頭=上25%、胴=中央40%（bbox中央帯）、右腕/左腕=胴帯から左右にはみ出す領域、右脚/左脚=下35%を中央で左右分割。pivot は定石位置（首元・肩・股関節）、z・親も定石通り。1秒未満で下書きが出るのでAI不要・オフラインでも使える。生成後は従来どおり一覧で調整可能。
3. segment のタイムアウト・エラー時のメッセージに「簡易分割（AIなし）をお試しください」を追記。

検証: (a) ダミーCLIで segment リクエストのプロンプト文字数がフル送信時より大幅減（1/3以下）になること、(b) 縮小→復元で矩形が元解像度のキャラを正しく覆うこと（ガンマン実素材）、(c) 簡易分割がガンマンで6パーツの妥当な下書きを即時生成すること、(d) 既存のsegment MOCK経路の互換。

### 14.6 API追加

`POST /api/edit` の `mode` に `"segment"` と `"cleanup"` を追加。それぞれ専用の構造化出力スキーマ（segment: parts配列、cleanup: v1と同じedits形式）。システムプロンプトに用途別の追記。

---

## 15. 【v3】Claude Code CLI バックエンド（APIキー不要モード）

Claudeサブスクリプション（Claude Code）ユーザーがAPIキーなしでAI機能を使えるようにする。サーバーのAI呼び出し部分を抽象化し、環境変数 `BACKEND` で切り替える。

### 15.1 切り替え

- `BACKEND=api`（既定）: 現行どおり `@anthropic-ai/sdk`。
- `BACKEND=cli`: ローカルにインストール・ログイン済みの `claude` CLI を `child_process.spawn` で呼ぶ。
- `MOCK=1` は従来どおり最優先。
- `/api/config` に `backend` を追加し、UIのフッター等に表示（「バックエンド: API / Claude Code CLI / MOCK」）。

### 15.2 CLI呼び出し仕様

```js
spawn("claude", [
  "-p",                        // 非対話モード
  "--output-format", "json",   // 結果をJSONエンベロープで受ける
  "--model", CLI_MODEL,        // env CLI_MODEL、既定 "sonnet"
], { stdio: ["pipe", "pipe", "pipe"] })
// プロンプト全文（システム相当 + ユーザー相当を連結したもの）を stdin に書いて閉じる
```

- 構造化出力APIは使えないため、プロンプト末尾に「出力は次のJSON Schemaに厳密に従うJSONのみ。コードフェンス禁止」としてスキーマ(JSON文字列)を埋め込む。
- 応答: `--output-format json` のエンベロープから `result` フィールド（テキスト）を取り出し、コードフェンス除去 → `JSON.parse` → **既存のパッチ検証をそのまま通す**（検証機構は共通なので、モデル出力が乱れてもサーバーで守られる）。パース失敗時は1回だけリトライ（「JSONのみで再出力」と付けて再実行）、それでも失敗ならエラーをSSEで返す。
- 画像は渡さない（テキストグリッドのみ）。プロンプト組み立て関数に `includeImages` フラグを追加して分岐。
- ストリーミング: CLIモードでは本文ストリームは不要。SSEでは30秒ごとに `{type:"delta", text:"…"}` 相当の進捗ハートビートだけ流し、完了時に `result` を返す（クライアント側は現行実装のまま動く）。
- タイムアウト 180秒。`claude` コマンドが見つからない場合（ENOENT）は「Claude Code CLI が見つかりません。`npm install -g @anthropic-ai/claude-code` の上 `claude` にログインしてください」を返す。プロセス終了コード非0は stderr の先頭200文字を添えてエラー。
- 同時実行: cleanup の並列リクエストが来るため、CLIモードではサーバー側で同時実行数を2に制限するキューを入れる。

### 15.3 実装配置

- `server.js` の Anthropic 呼び出し部を `callBackend({ systemText, userText, images, schema, onDelta })` に抽出し、`api` / `cli` / `mock` の3実装を持たせる。既存のバリデーション・SSE整形・モード分岐（edit/motion/segment/cleanup）は一切変えない。
- README に CLI モードのセットアップ（Claude Code のインストール・ログイン・`BACKEND=cli npm start`）と、サブスク利用上限を消費する旨・画像を渡さない旨を追記。

### 15.5 実機フィードバック対応（広範囲修正のタイムアウト対策）

Windows実機での検証結果（1セル修正=約18秒で成功 / 「キャラの輪郭を黒くして」=180秒タイムアウト）を受けた改善。

1. **タイムアウトの延長と環境変数化**: 既定を 300 秒に延長し、`CLI_TIMEOUT`（秒）で上書き可能にする。
2. **広範囲変更のプロンプト緩和**: システムプロンプトの「最小差分の原則」に追記 —「変更が対象領域の大部分に及ぶ場合は、`?` による差分化に固執せず、対象矩形全体（またはフレーム全体）を書き直した rows を返してよい。差分の厳密さより応答の速さを優先する」。`?` の差分計算がモデルの推論時間を浪費するのを防ぐ。
3. **scope=all のフレーム分割**: `scope:"all"` かつ mode が edit のリクエストは、サーバー側で**フレームごとの個別バックエンド呼び出しに分割**する（既存の同時実行キューで並列2）。各フレームの指示文には「全フレーム共通の指示を、このフレームに適用せよ。他フレームとの一貫性を保て」と付記。結果の edits をフレーム番号を差し替えて統合し、1つのSSE resultで返す。1呼び出しあたりの出力量と推論時間を分割する狙い。分割時は進捗を `{type:"delta"}` で「フレーム 2/5 完了」のように流す。newFrames を含む応答は分割モードでは破棄して警告（中割りは scope=frame で行う運用）。
4. **ハートビート強化**: CLIモードの進捗ハートビートを10秒間隔にし、経過秒数を含める（クライアントは受信テキストをそのまま進捗表示にできる）。UI側は実行中に経過秒を表示。
5. **タイムアウト時のエラーメッセージ改善**: 「選択範囲を狭める / CLI_TIMEOUT を延ばす / CLI_MODEL=haiku を試す」の3つの対処を含める。
6. README の CLIモード注記に「広範囲の修正は時間がかかる。矩形選択で範囲を絞るのが最速」を追記。

検証: ダミーCLI（応答に2秒かかる版）で scope=all 分割の並列実行・進捗delta・結果統合を確認。タイムアウト（CLI_TIMEOUT=1 で強制発火）時のメッセージ内容を確認。既定バックエンド(api)の非分割経路が退行していないこと。

### 15.4 検証

- `claude` が無い環境でも検証できるよう、**ダミーCLI**（`scratchpad/fake-claude`: stdin を読み、固定パッチJSONを `--output-format json` エンベロープで出力するNodeスクリプト）を作り、`PATH` 先頭に置いて `BACKEND=cli` の全経路（edit/motion/segment/cleanup、パース・検証・SSE完了）を curl で確認する。
- ENOENT経路（PATHから外して起動）でエラーメッセージが返ることを確認。
- コードフェンス付きJSONを返すダミーで除去→パース成功を確認。
- 既存の §11 / §13 / §14 検証が `BACKEND=api`（既定）で退行していないことをスモーク確認。
