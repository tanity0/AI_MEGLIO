# ムーブパック形式（motion pack）仕様 v1

クイック生成に「必要なモーションのパターン一式」を一括登録するためのJSONファイル形式です。
制作側はこのファイルを用意するだけで、クイック生成のムーブ選択欄にカードが並び、
AI生成・プレビュー・スプライトシート/アトラス書き出しまで全自動で流れます。

## 使い方（ツール側）

クイック生成の「ステップ2 ムーブセットを選ぶ」→「📚 ムーブパック読込」からこのJSONを選択。
読み込んだパックは端末に記憶され、次回以降も表示されます（再読込で差し替え）。

## ファイル形式

- UTF-8 の JSON ファイル（拡張子 .json）
- 1ファイル = 1パック（最大24ムーブ）

```json
{
  "format": "aimeglio-motion-pack",
  "version": 1,
  "name": "基本アクション（近接キャラ用）",
  "author": "制作チーム",
  "moves": [
    {
      "key": "slash",
      "label": "剣振り（横薙ぎ）",
      "icon": "⚔️",
      "frames": 5,
      "fps": 12,
      "loop": false,
      "prompt": "剣を右から左へ大きく横薙ぎする攻撃モーション",
      "phases": [
        "構え: 剣を右後方に引き、腰をひねる。体重は後ろ足",
        "振り始め: 剣が体の右側を通過。体が回転しはじめる",
        "ヒット: 剣が正面で最大リーチ。体は深く前傾し、シルエットが最も横に広い",
        "振り抜き: 剣が左側へ抜ける。勢いで体が流れる",
        "戻り: 構えへ戻る途中。剣を引き寄せる"
      ],
      "notes": "剣の軌跡エフェクト・残像は描かない。剣は常に両手持ち"
    }
  ]
}
```

## 各フィールドの意味とルール

### パック全体

| フィールド | 必須 | 内容 |
|---|---|---|
| `format` | ✔ | 固定文字列 `"aimeglio-motion-pack"`（判別用） |
| `version` | ✔ | 固定 `1` |
| `name` | ✔ | パック名（UIに表示） |
| `author` |  | 作成者メモ（ツールは使用しない） |
| `moves` | ✔ | ムーブの配列（1〜24個） |

### 各ムーブ

| フィールド | 必須 | 内容・ルール |
|---|---|---|
| `key` | ✔ | **半角英数字と `_` のみ・1〜24文字・パック内で一意**。タグ名・アトラスのアニメーション名・GIFファイル名になるので、ゲーム側で使う名前をそのまま書く（例: `slash`, `jump_double`, `hit`, `death`）。既定ムーブ名（idle/walk/run/jump/attack/custom）は使用不可 |
| `label` | ✔ | UI表示名（日本語OK。例: 剣振り（横薙ぎ）） |
| `icon` |  | ムーブカードに出す絵文字1つ（省略時 🎞） |
| `frames` | ✔ | コマ数 2〜8 |
| `fps` |  | このムーブの再生速度（1〜24）。アトラス/プロジェクトJSONに出力。省略時は画面のFPS設定 |
| `loop` |  | ループ再生か（既定 true）。攻撃・死亡など単発は false |
| `prompt` | ✔ | **ムーブ全体の説明**（〜300字）。何のモーションか、体の使い方、雰囲気。AIへの指示にそのまま使われる |
| `phases` | 推奨 | **コマ別の局面説明**の配列（各〜200字）。`frames` と同数が理想（多少ズレても自動配分）。1コマ=1行で「そのコマで体・武器がどうなっているか」を書く。**これの質が生成品質を最も左右します** |
| `notes` |  | 禁止事項・補足（〜200字）。例:「エフェクトは描かない」「足の位置は固定」 |

## 書き方のコツ（制作側向け）

1. **phases はアニメーターの中割り指示のつもりで**。「構え→予備動作→ヒット→フォロースルー→戻り」のように、
   各コマの体重移動・シルエットの広がり・武器の位置を具体的に。
   - 良い例:「ヒット: 剣が正面で最大リーチ。体は深く前傾し、シルエットが最も横に広い」
   - 悪い例:「剣を振る途中」
2. **日本語でOK**（そのままAIに渡ります）。英語で書ける場合は英語の方がわずかに安定します。
3. **接地・浮遊を明記**。ジャンプ系は「両足が地面から離れる」「最高点」「着地で膝が沈む」を必ず書く。
4. **notes に「描いてほしくないもの」**（エフェクト・残像・影・武器の持ち替え等）を書くとブレが減ります。
5. ループもの（歩き・待機）は最後のコマが最初のコマに自然に繋がる説明にする。

## サンプル（コピーして使ってください）

```json
{
  "format": "aimeglio-motion-pack",
  "version": 1,
  "name": "基本アクションセット",
  "moves": [
    {
      "key": "slash", "label": "剣振り（横薙ぎ）", "icon": "⚔️", "frames": 5, "fps": 12, "loop": false,
      "prompt": "剣を右から左へ大きく横薙ぎする攻撃モーション",
      "phases": [
        "構え: 剣を右後方に引き、腰をひねる。体重は後ろ足",
        "振り始め: 剣が体の右側を通過。体が回転しはじめる",
        "ヒット: 剣が正面で最大リーチ。体は深く前傾し、シルエットが最も横に広い",
        "振り抜き: 剣が左側へ抜ける。勢いで体が流れる",
        "戻り: 構えへ戻る途中。剣を引き寄せる"
      ],
      "notes": "剣の軌跡エフェクトは描かない"
    },
    {
      "key": "jump_double", "label": "二段ジャンプ", "icon": "🌀", "frames": 6, "fps": 12, "loop": false,
      "prompt": "空中でもう一度跳ぶ二段ジャンプ。1回転を入れる",
      "phases": [
        "1段目の上昇: 体は伸び、両足が地面から離れている",
        "空中で膝を抱え込み、前方回転の始まり",
        "回転の頂点: 体が丸まり上下逆さま",
        "回転の終わり: 体がほどけて再び上昇の姿勢",
        "2段目の頂点: 体を大きく伸ばす",
        "落下開始: 足を下に向け着地に備える"
      ]
    },
    {
      "key": "hit", "label": "被弾", "icon": "💢", "frames": 3, "fps": 12, "loop": false,
      "prompt": "正面から攻撃を受けてのけぞる被弾モーション",
      "phases": [
        "衝撃の瞬間: 上体が後ろへ弾かれ、髪や装備が前へ流れる",
        "のけぞり最大: 片足が浮き、腕が跳ね上がる",
        "踏みとどまる: 前傾に戻りかけ、体勢を立て直す"
      ],
      "notes": "ダメージエフェクトや点滅は描かない"
    },
    {
      "key": "death", "label": "戦闘不能", "icon": "💀", "frames": 4, "fps": 8, "loop": false,
      "prompt": "力尽きて崩れ落ちるモーション",
      "phases": [
        "膝から力が抜けてがくりと沈む",
        "膝立ちで上体が前へ倒れはじめる",
        "地面に手をつき、うなだれる",
        "完全に倒れ伏す（横たわり・目を閉じる）"
      ]
    }
  ]
}
```

## AI（ChatGPT / Codex 等）にパックを作らせる場合のプロンプト雛形

制作をGPTに任せる場合は、以下を貼り付けて `【】` を埋めてください。

```
あなたは2Dアクションゲームのアニメーション設計者です。
以下の仕様に厳密に従う JSON（ムーブパック）だけを出力してください。説明文・コードブロック記号は不要です。

# 出力仕様
- ルート: {"format":"aimeglio-motion-pack","version":1,"name":string,"moves":[...]}
- 各ムーブ: key(半角英数と_のみ・一意・idle/walk/run/jump/attack/customは禁止),
  label(日本語表示名), icon(絵文字1つ), frames(2〜8の整数), fps(1〜24), loop(真偽),
  prompt(そのモーション全体の説明・300字以内),
  phases(framesと同数の配列。各コマの体重移動・シルエット・武器や手足の位置を具体的に。各200字以内),
  notes(描いてはいけないもの・補足。200字以内)
- phases はアニメーターの中割り指示として書く。「〜する途中」のような曖昧表現は禁止。
  接地/滞空、体の傾き、シルエットが最大になるコマを必ず明示する。
- 攻撃・被弾・死亡など単発のモーションは loop:false、待機・移動系は loop:true。
- notes には「エフェクト・残像・地面の影は描かない」を必ず含める。

# 作ってほしいムーブ
【例: 剣の横薙ぎ(5コマ), 突き(4コマ), 二段ジャンプ(6コマ), 被弾(3コマ), 死亡(4コマ), ダッシュ(4コマ)】

# キャラクターの前提
【例: 剣と盾を持った騎士。重い鎧で動きはやや鈍重】
```

出力されたJSONを `.json` ファイルとして保存し、クイック生成の「📚 ムーブパック読込」で読み込めば完了です。
形式エラーがある場合は読込時に日本語で理由が表示されるので、そのままGPTに貼って直させてください。
